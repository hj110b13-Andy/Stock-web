@AGENTS.md

# 每次對話的品質保證流程（使用者要求，務必嚴格遵守）

這三項規則彼此搭配運作，回報「更新完成」之前**一步都不能省略**：

## 規則一：問題卡關升級流程（規則二、規則三修正問題時的子流程）

- 針對「同一個問題／同類相似問題」自己嘗試修正，若**連續失敗 2 次**：
  - 進入第 3 次嘗試時，**立刻同步**用 Agent 工具分派 2 個不同模型（例如 Opus + Haiku，或 Sonnet + Opus 等組合，用 `model` 參數指定）的 agent 上網查詢各種可能解法——這是跟我的第三次嘗試**同時**進行，不是等我失敗了才開始查。
  - 若我第三次嘗試仍然失敗，就依照這兩個 agent 查到的解法繼續嘗試；同時再請這兩個 agent 繼續尋找新的解法，讓「我嘗試」與「agent 查詢」交疊、不間斷地進行，直到問題解決為止——不能出現「我試完在等 agent」或「agent 查完在等我」的閒置空檔。
- **失敗次數只針對同一類問題累計**：換成不同的新問題（前一個已經解決）時，失敗次數歸零重新計算，不會一遇到任何問題就馬上動用 agent，只有反覆卡在同一個問題時才升級。

## 規則二：自己的全面檢查修正循環

1. **第一次（只需一次）**：**實際**打開網站（部署後的網址，例如 https://stock-web-blond.vercel.app）、用瀏覽器工具（Playwright）操作每一項功能——不是只看程式碼、不是只用 curl 確認 API 回 200，而是像真實使用者一樣點擊、瀏覽、驗證畫面內容。地毯式全面檢查整個網站：每個分頁、每個功能、每個狀態（含各種篩選/切換/登入登出/邊界情況）。
2. 把發現的所有問題統整列出來，**全部**修正完成（過程中若卡關，套用規則一的升級流程）。
3. 之後只需針對「這次發現並修正的問題」逐一重新驗證是否真的修好——不必再重新地毯式檢查整個網站——直到這些問題都確認修好為止。
4. 進入規則三。

## 規則三：Opus agent 的獨立驗證循環

1. **第一次（只需一次）**：用 Agent 工具另外派 **1 個 Opus** agent（不用 Haiku，也不用其他模型；若當下 Opus 額度/速率限制暫時用不了，才臨時換其他模型頂替一次，額度恢復後仍以 Opus 為準），實際打開網站、操作各項功能，地毯式全面檢查一遍。
2. Opus agent 把發現的問題回報給我。
3. 我先自己嘗試修正這些問題；**如果是這個 Opus agent 抓到的問題，而我自己解決不了，就直接讓 Opus agent 自己動手修正程式碼**（不套用規則一「找不同模型查解法」那套，直接讓 Opus 來修）。其他我自己能修好的問題，卡關時一樣套用規則一的升級流程。
4. 之後只需再派這個 Opus agent 針對「這次發現並修正的問題」逐一重新驗證，不必要求 Opus 重新地毯式全站檢查，直到這些問題都被 Opus 確認修好為止。

這些問題都確認修好後，才可以回覆使用者「更新完成」。

## 規則四：每次回覆都要附上網址

每次回覆使用者（尤其是回報更新/完成狀況時），都要附上網站網址（例如 https://stock-web-blond.vercel.app），讓使用者可以直接點開，不用自己去翻找或回想網址。

## 規則五：跨裝置接續——每次回覆前都要更新 PROGRESS.md 並 push

使用者會用不同裝置（本地端、雲端等各種環境）的 Claude Code 接續同一個專案，彼此看不到對方的對話紀錄。`PROGRESS.md`（repo 根目錄）就是唯一的跨裝置記憶：記錄每個資料夾/檔案的功能、目前所有功能、重要慣例與限制、品保流程摘要、按時間排列的工作日誌、目前已知問題。

- **不限於「更新完成」時才做**：只要這次對話對程式碼、規則、或專案現況有任何實質變動（修 bug、加功能、改 CLAUDE.md 規則、發現新問題但還沒修好等），在回覆使用者之前都要：
  1. 更新 `PROGRESS.md`——把這次做的事、修的問題、目前狀態反映進去（尤其是「工作日誌」要補新的一筆、「目前已知問題」章節要跟上最新狀態）。
  2. commit 並 push 到目前使用的分支，確保其他裝置下一次接手時 `git pull` 就拿得到最新版本。
- 單純的問答（沒有修改任何檔案、沒有新增進度）不用觸發這條規則。
- 每次接手一個新對話／新裝置時，**先讀 `PROGRESS.md` 了解現況**，不要每次都從頭重新摸索整個 codebase。