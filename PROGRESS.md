# PROGRESS.md — 專案現況與工作日誌

> **給接手這個專案的 Claude Code：** 這份文件是為了讓你在完全沒有先前對話紀錄的情況下，
> 也能看懂這個 repo 每個資料夾/檔案在幹嘛、目前做到哪、之前修過什麼問題。
> 每次修改完程式碼準備回覆使用者之前，**先讀這份文件掌握現況，回覆前再照 CLAUDE.md
> 的規定更新這份文件並 push**（見下方「跨裝置接續規則」）。

## 這是什麼專案

**股情雷達 StockRadar**——台股／美股股票研究網站。即時查詢報價、互動 K 線圖、篩選排行、
AI 問答。Next.js (App Router) + TypeScript + Tailwind CSS，圖表用
[lightweight-charts](https://github.com/tradingview/lightweight-charts)。

**2026-09-11 起改為私人／家人使用**（見下方工作日誌）：網站現在有進入密碼保護
（`src/proxy.ts` + `/unlock`），不再是對外公開服務；AI 問答也因此改成會直接給明確的
個人看法/推薦，不再強制客觀中立、不再附加免責聲明——**這個決定的前提是「網站已經真的做了
存取限制」，如果之後又要把網站改回公開，AI 問答的系統提示詞（`lib/ai/ask.ts`）務必要
改回客觀描述、不做買賣建議的版本，順序不能顛倒。**

- **正式站**：https://stock-web-blond.vercel.app（進入密碼預設寫在 `src/app/api/unlock/route.ts`，或看有沒有設定 `SITE_PASSWORD` 環境變數蓋掉它）
- **GitHub**：hj110b13-Andy/Stock-web
- **開發分支**：`claude/relaxed-curie-c69kp0`（所有工作都在這個分支上，push 上去 Vercel 會自動部署）
- **功能完整說明、環境變數設定、資料來源限制**：見 `README.md`，這份文件不重複列，只補「檔案功能地圖」跟「工作日誌」。

## 資料夾與檔案功能地圖

```
src/
├─ app/                              Next.js App Router 頁面與 API
│  ├─ page.tsx                       首頁：大盤指數、AI 每日快報、焦點排行、我的關注清單
│  ├─ layout.tsx                     全站 layout；deferred 深色模式偵測 inline script（避免 FOUC）
│  ├─ not-found.tsx                  404 頁
│  ├─ robots.ts / sitemap.ts         SEO：/robots.txt、/sitemap.xml
│  ├─ globals.css                    全站設計 token（CSS 變數）：淺色/深色配色、紅漲綠跌色票
│  ├─ stock/[symbol]/page.tsx        個股頁：報價 + K線 + 技術訊號 + 基本面 + AI問答 + 關注按鈕
│  ├─ search/page.tsx                搜尋/篩選頁（外殼，實際邏輯在 components/SearchClient.tsx）
│  ├─ highlights/page.tsx            每日焦點榜單：漲幅/跌幅/成交量榜 + 技術訊號共振股
│  ├─ news/page.tsx                  重大新聞頁：AI 置頂大消息 + 無限捲動瀏覽近期所有新聞
│  ├─ action/page.tsx                今日建議頁：給沒有股市背景的人看的白話「今天該怎麼做」
│  └─ api/
│     ├─ quote/[symbol]/route.ts     GET 單檔即時報價
│     ├─ chart/[symbol]/route.ts     GET 歷史 K 線（range: 1m/3m/6m/1y）
│     ├─ search/route.ts             GET 篩選/排序股票清單（市場、產業、價格區間、漲跌幅）
│     ├─ sectors/route.ts            GET 指定市場的產業分類選單
│     ├─ indices/route.ts            GET 大盤指數
│     ├─ ask/route.ts                POST AI 問答（支援多輪對話歷史）
│     ├─ daily-brief/route.ts        GET 每日快報，給首頁 DailyBriefCard client-side fetch用，
│     │                              支援 ?refresh=1 強制略過快取重算
│     ├─ news-feed/route.ts          GET 重大新聞分頁用，支援 ?offset=&limit= 分頁、
│     │                              ?refresh=1 強制重算，第一頁才會帶 pinned 陣列
│     ├─ action-brief/route.ts       GET 今日建議，架構同 daily-brief（?refresh=1、
│     │                              client-side fetch）
│     ├─ watchlist/route.ts          GET/PUT 登入後自選股跨裝置同步
│     ├─ auth/[...nextauth]/route.ts NextAuth（Google 登入）
│     ├─ cron/daily-brief/route.ts   Vercel Cron 排程觸發：預先生成當天 AI 快報
│     ├─ cron/warm-cache/route.ts    背景預熱：由 .github/workflows/warm-cache.yml 每 5 分鐘
│     │                              呼叫一次，預先算好 search/highlights/momentum/indices，
│     │                              讓真正訪客幾乎都吃到現成資料
│     └─ momentum/route.ts           GET 技術訊號共振股（給 highlights 頁的 client-side fetch用）
│
├─ components/                       UI 元件（React, 'use client' 除非明確是 server component）
│  ├─ StockChart.tsx                 K 線圖 + 成交量（lightweight-charts），會讀 CSS 變數換色
│  ├─ ChatWidget.tsx                 右下角 AI 問答浮動視窗，多輪對話
│  ├─ AskAboutButton.tsx             個股頁「問AI關於這檔股票」按鈕，會設定聊天視窗的 grounded symbol
│  ├─ MarketTabs.tsx                 台股/美股分頁切換共用元件（全站涉及雙市場的地方都用它，不並排）
│  ├─ StockTable.tsx                 股票清單表格（排行/搜尋結果共用）
│  ├─ MomentumTable.tsx              技術訊號共振股清單
│  ├─ SearchClient.tsx               搜尋頁的篩選邏輯（產業多選、價格區間、debounce）
│  ├─ SignalTags.tsx                 個股頁技術訊號標籤（爆量/創新高低/均線/連漲跌）
│  ├─ FundamentalsCard.tsx           基本面卡片（本益比/股價淨值比/殖利率/市值）
│  ├─ ChipsCard.tsx                  籌碼面卡片（三大法人買賣超/融資融券）+ 近期重大訊息公告，
│  │                                 僅台股頁面渲染（`quote.market === "TW"` 才顯示），美股
│  │                                 沒有對應的公開資料源
│  ├─ LiveQuoteHeader.tsx            個股頁報價 header，盤中每 20 秒自動刷新
│  ├─ LiveIndices.tsx / IndexCard.tsx 首頁大盤指數卡片，同樣有自動刷新
│  ├─ MarketStatusBadge.tsx          「盤中／已收盤」狀態徽章
│  ├─ DailyBriefCard.tsx             首頁 AI 每日快報卡片，client component 掛載後才 fetch
│  │                                 （避免冷快取當天拖慢首頁），預設收合 220px + 「展開全文」，
│  │                                 不依賴 AI 每次都乖乖控制在系統提示詞要求的字數範圍內
│  ├─ NewsFeedList.tsx               重大新聞頁清單：AI 置頂大消息區塊 + 無限捲動主清單，
│  │                                 IntersectionObserver 觸發載入下一頁
│  ├─ WatchlistButton.tsx            加入/移除關注清單的星號按鈕
│  ├─ WatchlistSection.tsx           首頁「我的關注」清單區塊（排序、匯出 CSV）
│  ├─ WatchlistTable.tsx             關注清單表格（取代 StockTable）：可編輯持有股數/平均成本，
│  │                                 即時算損益，寫入用 useSyncExternalStore 要注意快照函式不能
│  │                                 用 .filter()/.map()（見下方工作日誌無限迴圈那次教訓）
│  ├─ WatchlistSync.tsx              登入後把本機 localStorage 清單與雲端清單合併同步
│  ├─ PriceAlertForm.tsx             個股頁「到價提醒」表單：設定/列出/刪除該股的到價提醒
│  ├─ PriceAlertWatcher.tsx          掛在全站 layout，每 30 秒檢查所有未觸發的到價提醒，
│  │                                 觸發時右下角跳提醒（目前只有網頁內顯示，還沒接 LINE 通知）
│  ├─ MarkdownLite.tsx               輕量 Markdown 渲染（粗體/條列），給 AI 回覆/每日快報用，
│  │                                 不是完整解析器，只處理 AI 實際會用到的語法
│  ├─ ThemeToggle.tsx                深色模式手動切換開關，寫 data-theme + localStorage
│  ├─ SiteHeader.tsx                 全站頂部導覽列（含 ThemeToggle、AuthButton）
│  ├─ AuthButton.tsx                 Google 登入/登出按鈕
│  └─ AuthProvider.tsx               NextAuth SessionProvider 包裝
│
└─ lib/
   ├─ data/                          資料層——所有股票資料的唯一進出口
   │  ├─ index.ts                    對外統一介面：getQuote/getChart/getIndices/searchStocks/
   │  │                              getFundamentals/getEarnings/getMultiSignalStocks/detectMarket/normalizeSymbol
   │  │                              等，全部「抓不到資料就回 null，絕不產生假資料」
   │  ├─ twse.ts                     台股（上市）資料抓取：TWSE 即時報價/K線/OpenAPI 基本面(含P/B)/月營收/
   │  │                              季報EPS/三大法人買賣超(legacy T86)/融資融券(MI_MARGN)/
   │  │                              每日重大訊息(t187ap04_L，注意「主旨 」欄位名尾端有個空格)；
   │  │                              也 export `TW_INDUSTRY_NAMES`（產業代碼→名稱表）給 tpex.ts 共用
   │  ├─ tpex.ts                     **2026-09-14 新增**：台股（上櫃 TPEx）資料抓取，架構與涵蓋範圍
   │  │                              完全比照 twse.ts（報價/K線/基本面/月營收/季報EPS/三大法人/
   │  │                              融資融券/重大訊息/公司清單），見下方工作日誌詳細說明欄位差異
   │  │                              與 TPEx 端點自身的不穩定性（大檔案偶爾回傳中斷，已加重試）。
   │  ├─ taifex.ts                   **2026-09-14 新增**：台指期（TX，大台指）夜盤近月合約報價，
   │  │                              資料源是 TAIFEX 官方免費看盤網站 mis.taifex.com.tw/futures/
   │  │                              自己的 getQuoteList API（不需登入/金鑰），見檔案開頭完整研究
   │  │                              記錄跟下方工作日誌；export `describeTaifexNightFutures()`
   │  │                              給 AI 問答/快報組 grounding 文字共用
   │  │                              興櫃不在範圍內，明確不做。
   │  ├─ us.ts                       美股資料抓取：Yahoo Finance 報價/K線/基本面(含P/B)/季度財報（皆需 crumb+cookie 認證）
   │  ├─ news.ts                     新聞資料：Google 新聞 RSS 搜尋（zh-TW + en-US 雙版面，美股
   │  │                              相關查詢會混合中英文來源），給 AI 問答「資訊面」用；
   │  │                              也有 fetchNewsFeedPool()（重大新聞頁用，12 個主題關鍵字
   │  │                              合併去重排序）跟 link/來源標籤解析（見下方工作日誌）
   │  ├─ articleExtract.ts           **2026-09-14 新增**：把 news.ts 給的 Google 新聞 RSS 連結
   │  │                              （本身是 Google 導轉頁、不是真正出版商網址）解回真正的
   │  │                              文章網址並擷取內文全文，給 newsfeed.ts 的一般新聞摘要用
   │  │                              （見下方工作日誌「新聞摘要改抓全文」，含真實擷取成功率）
   │  ├─ universe.ts                 股票清單：台股動態抓 TWSE 官方上市清單＋TPEx 官方上櫃清單並合併
   │  │                              （`UniverseEntry.exchange` 內部欄位標記 "TWSE"/"TPEx"，只用於
   │  │                              路由到正確的資料源，不影響對外的 Market="TW"/"US" 型別），
   │  │                              各自分開設批次查報價用的清單上限（TWSE 500／TPEx 300，見下方
   │  │                              工作日誌），美股約 150+ 檔精選跨產業大型股種子清單
   │  ├─ cache.ts                    共用 TTL 快取：cached()/cachedMap()/peekCached()/writeCached()，
   │  │                              Redis 優先、記憶體備援、single-flight 去重複、null 值也會被
   │  │                              正確快取（見工作日誌，這層踩過最多坑）
   │  ├─ volumeHistory.ts            **2026-09-14 新增**：searchStocks()「價量關係」推論（價漲/
   │  │                              跌量增，見下方工作日誌）的成交量歷史記錄+均量計算，piggyback
   │  │                              在既有批次報價快取上、零額外上游請求，收盤後才記錄避免污染
   │  │                              歷史，見檔案內註解跟工作日誌完整說明
   │  ├─ kv.ts                       Redis 連線設定（Vercel KV / Upstash）——**2026-09-11 起正式站
   │  │                              已經設定好並連接（Upstash 免費方案），不再是「選用但沒開」
   │  │                              的狀態；程式碼邏輯仍然保留「沒設定就退回記憶體快取」的容錯，
   │  │                              本機開發沒設這兩個環境變數一樣能跑，只是不會有跨伺服器共用
   │  └─ types.ts                    Market / Quote / Chart 等共用型別，含 SearchItem.volumeTrend/
   │                                 volumeRatio（**2026-09-14 新增**，價量關係推論，非真實買賣單資料）
   │
   ├─ ai/                            AI 問答與每日快報
   │  ├─ provider.ts                 共用的 Gemini→Claude fallback 呼叫邏輯（callAiProviders），
   │  │                              處理對話歷史裁切、開頭必須是 user、合併連續同角色 turn
   │  ├─ ask.ts                      /api/ask 的問答邏輯：組 system prompt、抓相關股票資料當 RAG context
   │  ├─ brief.ts                    每日 AI 快報生成邏輯，以台北時間日期當快取 key，4 段式
   │  │                              （大盤與台美連動/台股焦點/美股焦點/近期重點回顧）
   │  ├─ newsfeed.ts                 重大新聞頁邏輯：getNewsFeed() 抓 fetchNewsFeedPool() 的
   │  │                              新聞後，讓 AI 從最新 60 則裡挑出真正「可能影響大盤」等級
   │  │                              的消息當 pinned（回傳清單編號，不重寫標題，避免 AI 改寫
   │  │                              走樣；沒有夠格的消息就回傳空陣列，不硬湊）；一般項目的
   │  │                              summarizeItems()/summarizeBatch() **2026-09-14 起改抓
   │  │                              articleExtract.ts 擷取的文章全文來摘要**（原本只給標題，
   │  │                              摘要等於換句話重複標題，見下方工作日誌），擷取失敗才退回
   │  │                              原本的標題摘要，NewsFeedItem.summaryKind 標記兩者區別
   │  ├─ gemini.ts                   Gemini API 呼叫封裝（含自動探測可用模型名稱、
   │  │                              finishReason===MAX_TOKENS 截斷偵測）
   │  └─ types.ts                    ChatTurn 等共用型別
   │
   ├─ auth.ts                        NextAuth 設定（Google Provider）
   ├─ watchlist.ts                   自選股清單邏輯（localStorage），含可選的 costBasis/shares
   │                                 持股成本欄位跟 updateHolding()
   ├─ watchlistStore.ts              登入後自選股的伺服器端 Redis 儲存
   ├─ priceAlerts.ts                 到價提醒的 localStorage CRUD，架構跟 watchlist.ts 一致
   ├─ signals.ts                     客觀技術訊號計算（不是預測，只描述當下數據狀態），含
   │                                 均線/爆量/創新高低/連漲跌，以及 RSI/MACD 黃金死亡交叉
   ├─ marketStatus.ts                判斷台股/美股目前是否為交易時段
   ├─ format.ts                      數字/價格格式化（含台股紅漲綠跌慣例）
   ├─ theme.ts                       深色模式相關輔助（讀 CSS 變數、fallback 色票）
   ├─ chatEvents.ts                  跨元件溝通用的事件（例如 AskAboutButton 通知 ChatWidget 换聚焦股票）
   └─ site.ts                        網站名稱/網址常數（SEO metadata 用）

├─ proxy.ts                          Next.js 16 的 proxy（原 middleware）：全站密碼保護閘門，
│                                     沒有 site_unlocked cookie 一律導去 /unlock，含 API 路由
└─ app/
   ├─ unlock/page.tsx + UnlockForm.tsx  密碼輸入頁
   └─ api/unlock/route.ts               驗證密碼、設定 cookie（預設密碼寫死在這個檔案）

根目錄:
├─ README.md          完整功能說明、環境變數設定、資料來源與限制、專案結構、設計慣例
├─ CLAUDE.md           使用者要求的品保流程規則（每次對話都要照做，見下方摘要）
├─ AGENTS.md            Next.js 版本提醒（這個版本跟訓練資料的 Next.js 可能有差異，寫程式前看 node_modules/next/dist/docs/）
├─ vercel.json          Vercel Cron 排程設定（每天觸發每日快報預生成，注意：這個一天一次的頻率
│                       不夠用來預熱 search/highlights，那個改用下面的 GitHub Actions）
├─ .github/workflows/warm-cache.yml  每 5 分鐘呼叫 /api/cron/warm-cache 預熱快取（見工作日誌）
└─ .env.example         需要的環境變數範例（AI API key、Redis、Google OAuth、SITE_PASSWORD，全部選用；
                        Redis 這幾個雖然程式碼邏輯上是選用，但正式站現在已經設定好了，見下方
                        「重要慣例與限制」）
```

## 目前所有功能（快速索引，細節見 README.md）

首頁大盤指數/AI快報/關注清單、個股頁報價+K線+技術訊號（均線位置/均線多空排列/RSI/
MACD含0軸強弱判讀/KD/布林通道）+
基本面（含P/B）+籌碼面（三大法人買賣超/融資融券，僅台股）+重大訊息公告（僅台股）+
AI問答+到價提醒、搜尋/篩選頁（產業多選+價格區間自訂+漲跌幅自訂區間+成交量區間+
「價量關係」偏多/偏空/中性多選篩選，**2026-09-14 新增後三項**，價量關係是技術分析
的價漲/跌量增推論、非真實買賣單資料，見下方工作日誌與已知問題）、每日焦點榜單（漲跌幅/成交量/技術
訊號共振）、**重大新聞頁**（AI 置頂可能影響大盤的重大消息+白話摘要+無限捲動瀏覽近期
所有新聞與真實籌碼/技術資料卡，卡片分成摘要跟連結兩塊，不會整張卡片直接跳走）、
**今日建議頁**（給沒有股市背景的人看的白話「今天該怎麼做」，專有名詞都會順帶解釋）、
AI 多輪問答（任何個股問答都會附上自己的技術訊號、不只技術訊號共振股才有；可分析整個
關注清單、依持股成本算損益、綜合基本面/財報/籌碼面/消息面/技術面給出明確看法，且不
只套用升息單一角度分析漲跌關聯；會整合重大新聞頁已篩選的置頂事件；可引用真實新聞與
財報——台股月營收年增率/季報EPS、美股季度EPS，新聞來源 Google 新聞 RSS 中英雙語；
回答刻意設計成新手也看得懂，術語會順帶白話解釋）、每日 AI 快報（4 段式：大盤與台美
連動/台股焦點/美股焦點/近期重點回顧，Vercel Cron 排程）、關注清單＋持股成本追蹤
（localStorage，登入後跨裝置同步）、到價提醒（目前僅網頁內顯示）、深色模式、共用 Redis 快取
（**2026-09-11 起正式站已設定，Upstash 免費方案**）、背景自動預熱（GitHub Actions 每 5 分鐘）、
Google 登入（選用）、全站密碼保護（`SITE_PASSWORD`）、全站 SEO metadata。**全站不使用
任何示範/假資料**——抓不到就誠實顯示「資料暫缺」。**網站現在是密碼保護的私人工具，不是對外公開
服務**，AI 問答會直接給明確推薦/看法，這點跟一般公開股票網站的「不做投資建議」慣例不同，見
「重要慣例與限制」。

## 重要慣例與限制

- **紅漲綠跌**：台灣/中國市場慣例，跟美股常見的反過來，全站配色與 `format.ts` 都照這個做。
- **AI 問答的建議尺度跟網站是否公開綁在一起，不是單純的文案風格選擇**：網站現在有密碼保護
  （見下方工作日誌 2026-09-11、`src/proxy.ts`），只有使用者跟家人用，所以 `lib/ai/ask.ts`
  的系統提示詞允許直接給明確個人看法/推薦。**這個前提很重要**：台灣證券投顧法規管的是「對不
  特定多數人」提供投資推介，判斷基準是網站有沒有做存取限制，不是使用者自己覺得是不是私人
  用途。如果之後把密碼保護拿掉、或網站又變成任何人都能進來，AI 問答務必要改回客觀數據描述、
  不做買賣建議/漲跌預測的版本（`getMultiSignalStocks` 在 `lib/data/index.ts` 的註解本來就是
  照這個原則設計的）——**不要因為看到這裡的舊版指示殘留就假設現在還是客觀中立的版本，先看
  `lib/ai/ask.ts` 目前實際的系統提示詞內容再判斷。**
- **雙市場一律用分頁，不並排**：`MarketTabs` 元件，全站慣例。
- **這台本機開發環境（Windows PC）對外網路正常、能連到正式站**：跟這份文件更早版本記錄的
  「sandbox 連不到外部」不同（那是另一台/另一個環境的限制，不是這台）。這台機器上已經裝好
  Node.js（`winget install OpenJS.NodeJS.LTS`）跟 Playwright（裝在系統暫存資料夾，不在專案
  `node_modules` 裡），可以真的開瀏覽器測試正式站，也能 `npm install && npm run build &&
  npm run start` 在本機起服務打真實上游資料驗證修改。如果換了一台新環境，還是要重新確認一次
  網路/工具限制，不要照抄這段。

## 品保流程（詳細規則見 CLAUDE.md，這裡只摘要）

使用者要求每次對話回報「更新完成」前要走完：
1. **規則一**：同一類問題卡關連續失敗 2 次，第 3 次要同步派 2 個不同模型 agent 上網查解法，交疊進行不間斷。
2. **規則二**：自己先做一次完整的實測（build+start+curl，模擬瀏覽器操作）地毯式檢查，抓到的問題全部修好；之後只要針對這次修的問題複查即可，不必每次重新全站掃。
3. **規則三**：規則二做完後，派 1 個 Opus agent（額度不夠才臨時換模型頂替，之後仍改回 Opus）做一次完整地毯式檢查；抓到的問題自己修，修不好就讓 Opus 直接動手；之後只需針對這次修的問題再複查，直到 Opus 確認「沒有發現問題」才能回報「更新完成」。
4. **規則四**：每次回覆使用者都要附上網站網址 https://stock-web-blond.vercel.app 。**網址跟密碼絕對不能寫在同一行/緊接在一起**（會被通訊軟體自動連結辨識吞掉變成壞連結，這件事已經真實發生過不只一次），務必分開兩行。
5. **規則五（跨裝置接續）**：每次回覆使用者之前，都要更新這份 PROGRESS.md 並 push，讓其他裝置的 Claude Code 接得上。
6. **規則六**：只要在等待背景工作完成（部署、下載、agent 執行等）導致一段時間沒有新回應，每最多 5 分鐘要在對話視窗主動回報一次目前狀態，不能整段沉默、也不能只依賴「完成才通知」的機制悶著頭等。

## 工作日誌（新到舊，只列有意義的變更；commit hash 對應 `git log`）

### 2026-09-15（晚間）：全台股上櫃(TPEx)股票報價系統性地卡在「昨天收盤」——嚴重bug，已修好

使用者提供 Yahoo股市 對照截圖回報「宏碁資訊(6811) 248.0 +9.98%」跟 Yahoo 顯示的
「247.0 -0.40%」對不起來，並強調「很多都錯了，還在維持昨天的」。追查發現這不是快取或
輪詢問題（前一則工作日誌修的即時輪詢對這個 bug 沒有幫助），而是**資料源本身選錯**：

- `lib/data/tpex.ts` 原本抓上櫃股票報價用的是 TPEx 官方 OpenAPI
  `https://www.tpex.org.tw/openapi/v1/tpex_mainboard_quotes`。直接查詢這個端點發現
  6811 的 `Date` 欄位是 `"1150914"`（民國115年9月14日＝**昨天**），而查詢當下已經是
  9月15日盤中——證實這個端點其實是**盤後資料**，一整個交易日都要等到收盤後才會更新
  成當天的數字。全台股所有上櫃股票（約1000檔，佔台股將近一半）在開盤期間全部都受
  這個問題影響，只有上市(TWSE)股票（走 `mis.twse.com.tw` 真即時來源）不受影響。
- 修法：改用跟 TWSE 完全同一台伺服器 `mis.twse.com.tw` 的即時報價端點，只是把
  `ex_ch=tse_xxxx.tw` 換成 `ex_ch=otc_xxxx.tw`（直接 curl 驗證過，這個端點對上櫃股票
  回傳的 `y`/`o`/`h`/`l` 都是當下這個交易日的真實數字，`trade` 子物件裡還有真實的
  盤中成交時間戳）。`fetchTpexQuote`/`fetchTpexQuotesBatch` 整個重寫成跟 `twse.ts`
  幾乎一樣的寫法（同一種批次查詢、同樣的 MIS row 格式、同樣的「`z` 常常是 `-`，退回
  買賣五檔中間價」邏輯），差別只在 `ex_ch` 前綴跟型別/函式名稱獨立一份（延續這個
  專案「每個交易所自己保留一份小工具函式」的既有慣例）。原本「TPEx 報價必須整包
  下載全市場」的限制也一併解除——MIS 端點本來就支援跟 TWSE 一樣的批次查詢語法，
  不用再抓整包 ~1000 檔資料才能查一檔。
- `npx tsc --noEmit`、`npx next build` 皆過。待 push 部署後要在正式站重新驗證
  6811（以及其他幾檔上櫃股票）的即時數字是否跟 Yahoo 股市對得上。
- **教訓**：官方 OpenAPI 端點不能假設是即時資料——TWSE/TPEx 的 OpenAPI 系列（例如
  之前用過的 `t187ap05_L` 月營收、`t187ap06_L_ci` 季報、`BWIBBU_ALL` 本益比）大多是
  排程批次產生的「盤後/隔日」資料集，適合拿來做財報/籌碼這類本來就是隔日才公布的
  資料，但絕對不能拿來當作報價這種需要盤中即時更新的資料源——這次會選錯，就是因為
  沒有先確認這個特定端點的更新頻率就直接拿來用。之後任何新增資料源，只要是要拿來
  顯示「即時股價/即時漲跌幅」，都要先實際查一次該端點的時間戳／更新頻率再決定能不
  能用，不能只看到「官方 OpenAPI」幾個字就假設它是即時的。

### 2026-09-15（傍晚）：規則三獨立複查 Chrome Claude 那 8 個問題——7 項確認修好，另外抓到 1 個新的圖表載入提示遮擋瑕疵並修掉

這一輪是針對 `612bfe5`（修好 Chrome Claude 地毯式測試抓到的 8 個問題）做的**獨立第二輪複查**，
不是重新地毯式全站檢查。用 Playwright 實際驅動 Chromium 打開正式站
（https://stock-web-blond.vercel.app ，密碼 1118），逐項操作驗證。

複查環境備註：這台機器沒有 Playwright MCP 工具，但 `%LOCALAPPDATA%\ms-playwright\chromium-1243`
已經有下載好的 Chromium，所以在 scratchpad 裝 `playwright-core` 並用 `executablePath` 指到
那顆 Chromium 來驅動。解鎖後把 `storageState` 存成 `state.json` 重複使用，避免每支腳本都要重新輸密碼。
下次接手如果也需要真的開瀏覽器，可以用同一招。

逐項複查結果：

1. **【嚴重】AI 把「三大法人合計」講成「外資」——確認修好。** 問「台積電跟聯發科比較，誰比較好？」，
   回答明確寫「三大法人合計大賣 10,462 張、外資也大砍 11,191 張」，跟 /stock/2330 籌碼面卡片的
   真實數字（三大法人合計 -10,462,299 股、外資 -11,191,022 股）逐一對得上，兩個數字沒有互相替換。
   聯發科那段（三大法人合計買超 852 張、外資也在買超）同樣區分正確。
2. **【中等】個股頁沒有財報區塊——確認修好。** /stock/2330 標題順序為
   `基本面 | 財報 | 籌碼面 | 近期重大訊息 | 到價提醒`，位置正確；內容是真實數字
   （2026年8月年增率 +53.32%、115年Q2 EPS 49.33元）。/stock/AAPL 也有財報卡
   （2Q2026 EPS 2.02、EPS優於預期 +6.74%、下次公布 2026-10-29；月營收年增率「資料暫缺」是
   美股本來就沒有月營收公告，合理）。
3. **【中等】搜尋頁產業下拉被裁切——確認修好。** 在 1280 / 900 / 768 三種寬度（含當初回報出事的
   894px 附近）首次點開「產業：全部」，量測面板 rect 分別是 left=430/78/99、right=634/282/303，
   全部 `left ≥ 0 且 right ≤ 視窗寬`，完整在畫面內、往右展開、文字清楚可讀。
4. **【中等】AI 無法用「Apple」辨識美股——確認修好（但有一個殘留小瑕疵）。** 輸入
   「Apple現在股價多少？」正確回答 AAPL 333.08 USD，跟 /stock/AAPL 頁面數字一致；
   全小寫「apple」也能正確回答同一個價格。**殘留小瑕疵**：小寫那次多補了一句
   「你提到的另一檔『APPLE』因為不在本站美股精選的 150 多檔範圍內，所以查不到資料」——
   把同一個詞當成兩檔標的解析了。原始 bug（完全查不到）已修好，這只是多餘的贅語，
   不影響答案正確性，暫時列為已知小瑕疵沒有動它。
5. **【輕微】切換圖表範圍沒有 loading 提示——確認修好，但複查中發現新瑕疵並一併修掉（見下）。**
   在冷門股 /stock/1717（三次 API 都是 `x-vercel-cache: MISS`，確實是冷資料）點「1個月」→「10年」，
   用 25ms 高頻輪詢抓到：點「10年」後第 80ms 就出現「載入中…」提示，持續約 4.4 秒到資料載入完成；
   點「5年」約 1.5 秒、點「1個月」約 0.7 秒。舊 K 線資料全程保留，沒有變空白。
6. **【輕微】今日建議頁文字拼接錯亂——這次沒有復現，確認緩解有效。** /action 頁全文讀過一遍，
   語句都通順；原本出事的那個位置（MACD 講 0 軸）這次輸出是
   「MACD出現黃金交叉（0軸上方，訊號較明確；0軸是判斷多空力道強弱的分界線）」，完整通順。
   這是機率性緩解不是保證，之後仍可能偶發。
7. **【輕微】「📄全文摘要」標籤看起來像可點的按鈕——確認修好。** /news 捲動後找到 67 個實例，
   實際 DOM 是 `<span>`，`cursor: default`、`select-none`、`rounded-full` 圓角、11px 小字、
   `text-(--text-muted)` 低對比灰底徽章，還帶 `title="摘要根據文章全文內容，不只是標題"`。
   點下去 URL 不變、沒有任何反應。視覺上跟旁邊藍色的「查看原文 ↗」連結區隔明顯。
8. **骨架屏**：依原始結論不需複查，這次也沒觀察到異常。

**複查中新發現並修好的問題——圖表「載入中…」提示被價格軸刻度蓋住（`StockChart.tsx`）**：
把載入提示放大看的時候發現，在 /stock/1717 上「載入中…」四個字被價格軸的刻度數字「85.00」
直接蓋過去，變成一團看不清的疊字（換 /stock/6446 就正常，因為那檔的刻度剛好沒落在提示那一行，
所以這個瑕疵只在「刻度數字剛好對齊提示 y 座標」的股票上才看得到，很容易漏掉）。
根因：提示那個 pill 的 `z-index` 是 `auto`，而 lightweight-charts 的價格軸是畫在
兄弟節點 `absolute inset-0` 容器裡的 canvas，同層級下 painting order 贏過 pill。
在瀏覽器裡即時把 pill 的 `z-index` 設成 30 驗證，文字立刻完全清晰、刻度被正確遮住，確認就是這個原因。
**修法**：pill 的 className 加上 `z-20`（同一個框裡已經有一個 `z-10` 的十字線 tooltip，
用 z-20 可以同時蓋過它，不會互相打架），並在原始碼留註解記錄重現條件。
`npx tsc --noEmit` 通過；`npx eslint src/components/StockChart.tsx` 的 2 個
`react-hooks/set-state-in-effect` 錯誤在 `git stash` 回 HEAD 後同樣會出現，是既有問題、與這次改動無關。

### 2026-09-15（盤中）：獨立複查 `3508788` 上櫃(TPEx)報價修正——確認修好，6811 與 Yahoo 股市完全對上

使用者附 Yahoo 股市截圖回報「宏碁資訊(6811) 我們顯示 248.0 +9.98%，Yahoo 卻是 247.0 -0.40%」，
另一台裝置查出根因是 TPEx 官方 OpenAPI（`tpex_mainboard_quotes`）其實是盤後資料集、Date 欄位
整個交易日都停在前一天，改用 `mis.twse.com.tw` 的 `otc_` 前綴即時端點修好（`3508788`、`7e8a7f7`）。
這一輪用 Playwright 實際開瀏覽器＋WebFetch 抓 Yahoo 股市做外部交叉比對，獨立驗證。
**驗證時段：台北時間 2026-09-15 上午 9:58–10:05，台股盤中。**

**個股頁逐檔比對（我們 vs Yahoo 股市即時報價）**：

| 代號 | 我們顯示 | Yahoo 股市 | 昨收是否一致 | 判定 |
|---|---|---|---|---|
| 6811 宏碁資訊 | 247.0 / -0.40% / 量 738張 | 247.0 / 昨收248.0 / 量 739張 | ✅ 都是 248.0 | **成交價完全相同**，量差 1 張 |
| 3105 穩懋 | 468.0 / +5.41% / 量 21,769張 | 469.5 / +5.74% / 量 22,162張 | ✅ 都是 444.0 | 盤中時間差，量級一致 |
| 6488 環球晶 | 921.5 / +2.39% / 量 4,358張 | 918.0 / +2.00% / 量 4,494張 | ✅ 都是 900.0 | 盤中時間差，量級一致 |
| 8069 元太 | 147.3 / +1.20% / 量 1,022張 | 147.52 / +1.37% / 量 1,099張 | ✅ 都是 145.5 | 盤中時間差，量級一致 |
| 3293 鈊象 | 698.5 / -0.07% / 昨收 699.0 | （未另查） | — | 數字內部一致 |
| 2330 台積電（上市對照組） | 2,392.5 / +0.53% | （未另查） | — | 上市股不受影響，正常 |

其中 3293、8069、6488 是**協調者沒提過、我自己抽測的上櫃股**，一樣正常，證明不是只有 6811/3105
剛好修好。所有檔的 `價格 − 昨收 = 漲跌點數`、`漲跌點數 ÷ 昨收 = 漲跌幅` 都算得起來。

**最有力的證據**：6811 現在顯示「現價 247.0、**昨收 248.0**」——原本卡住的那個「248.0」正是
昨天的收盤價，完全印證「整天都在顯示昨天收盤」這個根因描述，而且現在已經正確前進到今天的即時價。
另外首頁「焦點排行」榜單整個換血，**6811 已經從漲幅榜上消失**（它今天實際是 -0.40%，本來就
不該出現在漲幅榜），驊宏資 6148 等其他原本卡住的上櫃股也一併不見了。

**這一輪新發現的觀察（不是這次修正造成的迴歸，暫未處理）——今天沒成交的股票會用委買賣中價
擠進漲跌幅排行榜**：
`tpex.ts` 的 `rowToOtcQuote()` 在 MIS 的 `z`（成交價）是 `-`（今天尚未成交）時，會改用最佳
買價/賣價的中價當現價——這段邏輯是**照抄 `twse.ts` 既有的 `rowToQuote()`**，上市股一直都是
這樣，不是這次改 TPEx 才引進的。副作用是：完全沒成交的冷門股也會算出一個漲跌幅並進入排行榜。
實測量化（盤中 10:05 抓 `/api/search?market=TW&limit=2000`）：
- 台股共 1385 檔，其中 **153 檔成交量為 0**，**145 檔成交量 0 但漲跌幅不為 0**（約 10.5%）。
- 例：4911 德英顯示 29.53 / +3.60% / 量 0 張，但 Yahoo 股市對同一檔顯示成交價「-」、成交量「-」
  （確認它今天真的一張都沒成交，所以**我們的「量 0 張」是正確的**，是那個 +3.60% 來自中價估算）。
- **對實際畫面的影響很小**：首頁焦點排行前 8 名 **0 檔**受影響，/search 漲幅榜前 30 名只有 **1 檔**
  （稍早一次抓是 4 檔），因為真正大漲的股票幾乎都有成交量、排序天然會把它們推到前面。
- 之所以沒有直接動手改：`volume: 0` 本身是對的、中價也是程式碼註解裡寫明的刻意設計，
  要不要讓「零成交股不進排行榜」或「漲跌幅顯示成 —」屬於產品語意決策，不適合在複查時單方面改掉。
  **如果之後使用者反映「這檔明明漲 3%、點進去卻是 0 張」，再來處理這一項。**

（這一輪沒有改任何程式碼，只有本文件的紀錄更新。）

### 2026-09-15（傍晚，續）：獨立複查 `9ee3760` 那兩個新問題——兩項都確認修好，不需再改程式碼

承接上一則（複查 Chrome Claude 那 8 項）之後，另一台裝置把使用者中途回報的兩個新問題
修好並 push（`9ee3760`，見下一則工作日誌）。這一輪一樣用 Playwright 實際驅動 Chromium
打開正式站驗證，只針對這兩項、沒有重新地毯式檢查。**驗證時段：台北時間 2026-09-15 上午
9:33-9:55，台股盤中、美股已收盤**——剛好是能同時檢驗「兩個市場狀態要分開判斷」的理想時段。

**1. 首頁「焦點排行」與「我的關注」的 20 秒輪詢——確認修好，有量測數據佐證。**

- **焦點排行**：攔截 network，`/api/search?market=TW&sortBy=changePercent&sortDir=desc&limit=8`
  在第 392.8 / 412.8 / 432.8 / 452.8 / 472.8 秒各打一次（另一輪重測是 33.9 / 53.8 / 73.9 /
  93.8 秒），**間隔精準 20.0 秒**，完全沒有手動重新整理。
- **我的關注**：`/api/quote/2330` 與 `/api/quote/2454` 在第 33.8 / 53.8 / 73.8 / 93.8 秒
  各打一輪（每輪兩檔），同樣精準 20 秒。
- **畫面數字真的有跟著更新（不是只有請求打出去）**，兩個獨立證據：
  (a) 我的關注裡台積電從 `2,387.5 / +0.32%` 變成 `2,390.0 / +0.42%`，跟 API 在第 74.7 秒
  回傳的 `price=2390 chg%=0.42` 完全對得上；大盤指數同時段也從 45,854.61 一路跳到 45,770.37。
  (b) 焦點排行的第一名當下是**漲停鎖死**的驊宏資（+10.00%，價格被鎖住本來就不會動，
  容易誤判成「畫面沒更新」），所以改用 `page.route` 攔截並竄改第 2 次以後的 `/api/search`
  回應（價格改 999.9、漲跌幅改 +7.77%、名稱前面加【測試標記】），等下一次輪詢後畫面上
  確實出現 `【測試標記】驊宏資 999.9 +7.77%`——證明 `setItems` 到畫面重新渲染這條路是通的。

**2. 今日市場快報的開收盤措辭——確認修好，台美兩市場措辭正確分離。**
讀首頁「今日市場快報」全文，台股（當下盤中）用的是「台股加權指數**目前盤中暫報** 45,860.23 點」、
「精材等個股在**盤中**出現顯著回檔」、「台股**盤中**個股表現分歧」；美股（當下已收盤）用的是
「美股**在日前已收盤**，道瓊工業指數下跌 0.29%…」、「**美股已收盤**，類股走勢受到…」。
全文搜尋「終場」「收在」**在台股段落一次都沒有出現**，收盤語氣只用在真的已收盤的美股上，
確認 `brief.ts` 餵進去的 `getMarketStatus()` 狀態有被 AI 正確遵守、而且兩個市場是分開判斷的。

（這一輪沒有改任何程式碼，只有本文件的紀錄更新。）

### 2026-09-15（下午）：修好首頁「還在看昨天資料」的問題——根因是移動排行/自選清單沒有即時輪詢，不是快取壞掉

使用者回報「現在資訊還在昨天的，但已經開盤了」以及「股票即時股價與漲跌幅呢?我看還是在昨天的」。
逐項排查：

1. **先排除快取本身的問題**：直接 curl 正式站 `/api/daily-brief`、`/api/quote/2330`、`/api/indices`，
   確認底層資料管線本身是新鮮的（`updatedAt`/`generatedAt` 都對得上當下時間，數字也跟著大盤即時
   跳動），`daily-brief` 的 5 分鐘 rolling TTL（`BRIEF_TTL_MS`，見 `lib/ai/brief.ts`）本身運作正常。
   也用 GitHub API 查了 `warm-cache.yml` 的執行紀錄，發現 GitHub Actions 的 `*/5 * * * *`
   schedule 常常被延遲數小時才真正觸發（GitHub 對高頻 schedule 本來就有已知的延遲/降頻），但這
   本身不是 bug 的根因——因為就算 cron 沒準時跑，只要有真人請求打到 `/api/daily-brief`，
   `cached()` 的 TTL 到期後一樣會自動重新計算，不需要靠 cron 才能刷新。
2. **真正根因（已修好）：首頁「焦點排行」跟「我的關注」清單只在頁面第一次載入時抓一次資料，
   之後就完全沒有任何輪詢機制**——跟同一個網站上其他即時元件（`LiveIndices`、`LiveQuoteHeader`）
   完全不同調。使用者只要開著分頁不重新整理，這兩個清單就會停在「開啟當下」的數字，隨著時間
   過去就會看起來像是「還在昨天的」。
   - 新增 `LiveMoversBoard.tsx`（仿照 `LiveIndices.tsx` 的寫法），在開盤時每 20 秒打
     `/api/search?market=...&sortBy=changePercent&sortDir=desc&limit=8` 刷新一次；
     `src/app/page.tsx` 首頁「焦點排行」改用這個元件取代原本純伺服器渲染、渲染完就不會再變的
     `MoversBoard`。
   - `WatchlistSection.tsx`（首頁「我的關注」）原本只在關注清單本身變動時抓一次個股報價，
     改成額外加上每 20 秒的 `setInterval` 輪詢（因為自選清單可能同時混合台股/美股、開收盤時段
     不同，簡化成不分市場狀態、固定每 20 秒都重打一次，對已收盤市場多打一次報價 API 成本很低）。
3. **順便修正一個關聯的措辭問題**：`lib/ai/brief.ts` 產生的「今日市場快報」不管台股當下是開盤還
   收盤，永遠寫「終場收在ＸＸ點」這種收盤語氣的字——即使是盤中即時重新產生的內容也一樣，讓使用者
   誤以為資料是舊的。修法：`getDailyBrief()` 新增呼叫 `getMarketStatus("TW"/"US")`
   （`lib/marketStatus.ts` 既有工具），把台股/美股各自的開盤狀態餵進 grounding 資料開頭
   `【市場狀態】`，並在 system prompt 加一條規則：只有該市場「已收盤」時才能用「收在/終場/收盤」，
   「盤中」時要改用「目前來到/截至目前/盤中來到」這種語氣，且提醒讀者這是撰稿當下、盤中仍會變動
   的即時數字；台美兩個市場的開收盤狀態要分開判斷，不能因為其中一邊收盤就假設另一邊也收盤。
   **這個措辭問題本身也是很合理的「使用者誤判成 bug」的來源**：就算數字真的是幾十秒前才重新
   產生的新鮮資料，只要寫成「終場收在」，使用者看了還是會覺得「這是收盤後的舊資料」。

`npx tsc --noEmit` 與 `npx next build` 皆過。待 push 後在正式站用 Playwright 實際驗證「焦點排行」
「我的關注」在開盤時段確實會自動更新（不需要手動重新整理），以及快報文字在盤中確實改用「目前來到」
而非「收在/終場」。

### 2026-09-15：修好 Chrome Claude 測試抓到的 8 個問題（`612bfe5`），已自行驗證，待 Opus 規則三複查
接手新對話後先讀 `股情雷達測試報告.md` 全文（不只 PROGRESS.md 摘要），確認沒有遺漏的問題，
逐一修正並在正式站用真實資料/真實 AI 驗證：

1. **【嚴重，已修好並驗證】AI 把「三大法人合計」講成「外資」**：`ask.ts` 系統提示詞新增明確
   規則，說明兩者是不同數字、不能互相替換引用。**正式站重問原本觸發問題的那句「台積電跟聯發科
   比較，誰比較好？」**，新回答正確寫成「三大法人合計賣超10,462,299股...其中外資...大舉砍倉」，
   不再把 10,462 這個數字講成外資單獨的數字。
2. **【中等，已修好並驗證】個股頁缺財報UI卡片**：新增 `EarningsCard.tsx`，`page.tsx` 加上
   `getEarnings()` 呼叫。正式站截圖確認顯示真實數字「2026年8月年增率 +53.32%」「115年Q2 EPS
   49.33元」，跟測試報告裡 AI 講的數字一致。
3. **【中等，已修好並驗證】搜尋頁產業別下拉選單首次開啟裁切**：`SectorMultiSelect` 從
   `absolute right-0` 改成 `absolute left-0`（它是 `justify-end` flex-wrap 列裡 DOM 順序
   最前的控制項，錨定右邊界時 192px 選單會往左超出視窗）。正式站量測下拉選單 boundingBox
   `x: 72.4`，不再是負值。`VolumeTrendMultiSelect` 沒被回報有問題，維持原樣不動。
4. **【中等，已修好並驗證】AI 認不得「Apple」只認「AAPL」**：`universe.ts` 的
   `findSymbolByName()`/`findAllSymbolsByName()` 改成大小寫不敏感、並額外嘗試去掉公司後綴
   （Inc./Corp./Ltd. 等）比對。正式站測試「apple」「Apple」「APPLE」三種大小寫寫法都能正確
   查到 AAPL 真實股價。同時把 3 處查無資料的錯誤說明文字補上美股涵蓋範圍（原本只提
   TWSE/TPEx）。
5. **【輕微，已修好並驗證】10年圖表按鈕無loading提示**：`StockChart.tsx` 新增 `isLoading`
   狀態，切換 range 期間右上角顯示「載入中…」（不清空舊圖表，避免畫面閃爍空白）。
   **驗證過程一度誤判為FAIL**：因為本機/正式站的圖表快取（Redis + 5分鐘TTL）被稍早的測試
   熱過，切換range常常在70毫秒內完成，快到抓不到loading瞬間；改成先等初始圖表真的載入完成、
   再套用網路節流模擬慢速連線點擊「10年」，才穩定重現並確認「載入中」提示會出現。**教訓：
   測試「切換狀態時的loading提示」這類短暫UI狀態，如果剛好命中快取會完全看不到，要嘛用未
   測過的資料組合、要嘛主動模擬慢網路，不能直接假設「沒看到=沒生效」。**
6. **【輕微，已修好並驗證（prompt層級緩解）】今日建議頁文字拼接錯亂「強隨著分界線」**：
   `actionBrief.ts`／`ask.ts` 原本要求「提到0軸要附帶解釋」，但原始技術訊號資料本身常常已經
   自帶「（0軸上方，訊號較明確）」括號說明，兩組說明擠在一起讓LLM生成語句斷裂。改成明確指示
   兩者要分開講。**正式站重問同一句「台積電跟聯發科比較」**，新回答寫成「MACD死亡交叉（0軸
   上方，訊號較明確；0軸是判斷多空力道強弱的分界線）」，語句通順、不再破碎。**這是機率性
   緩解不是保證100%根除**（LLM生成本來就有隨機性，之後如果又零星出現類似破碎文字，不代表
   這次修正無效，可能只是機率沒完全歸零）。
7. **【輕微，已修好，程式碼審查信心足夠，未能截圖到實例】新聞頁「全文摘要」標籤點擊無反應**：
   查證後這是刻意設計的非互動標示文字（見 `NewsFeedList.tsx` 既有註解，整張卡片刻意不做成
   可點擊卡片），不是漏掉onClick的bug。改成小圓角徽章樣式+`cursor-default`，視覺上更像被動
   標籤。**正式站測試當下這批新聞剛好沒有 fulltext 摘要項目可以截圖驗證**（文章全文擷取
   不是每則都會成功，這是正常現象），純 CSS class 修改、風險低，用程式碼審查確認已正確部署，
   之後有 fulltext 項目出現時可以再順手看一眼樣式對不對。
8. **【查證為誤報，無程式碼變更】頁面切換骨架屏無loading動畫**：逐一檢查
   `app/loading.tsx`、`highlights/loading.tsx`、`stock/[symbol]/loading.tsx`、
   `SearchClient.tsx`、`NewsFeedList.tsx`、`ActionBriefCard.tsx` 的骨架屏，全部都已經有
   `animate-pulse`，沒有找到缺漏的地方。使用者/Chrome Claude 感受到的「無明確loading動畫」
   研判是 `/search`、`/news`、`/action` 這幾頁「路由切換瞬間到client fetch骨架出現」之間
   極短暫的空窗，不是程式碼缺陷。

**下一步**：這 8 項都已經自己驗證過（規則二完成），接下來要派 1 個 Opus agent 做規則三獨立
複查，只需針對這 8 項複查、不用重新地毯式全站檢查，複查通過才能跟使用者說「更新完成」。

### 2026-09-15：使用者用 Chrome 版 Claude 對正式站做獨立地毯式測試，8 個問題待下次接手處理

使用者依照我提供的測試提示詞，另外用 Chrome 版 Claude（跟這個對話完全獨立、沒有共用上下文）對正式站做了一次地毯式功能測試，報告存在repo根目錄 `股情雷達測試報告.md`（已 commit，需要完整重現步驟/技術細節時請直接讀那份檔案，這裡只摘要重點）。**這些問題都還沒有人處理，是下一次對話要接手的待辦**：

**🔴 嚴重（1個）**
- **AI 問答把「三大法人合計」賣超數字誤植為「外資」單獨數字**：問「台積電跟聯發科比較」時，AI 說「外資大舉賣超10,462張」，但10,462張其實是「三大法人合計」賣超（-10,462,299股），真正的外資賣超是11,191張（-11,191,022股）。AI 把兩個不同籌碼欄位的數字張冠李戴、講錯法人歸屬，屬於資料正確性問題。

**🟠 中等（3個）**
- **個股頁完全沒有顯示「財報」（月營收/季度EPS）區塊**：報價→基本面→籌碼面→重大訊息→到價提醒→圖表，中間沒有財報區塊；但 AI 問答明明能講出「8月營收年增率53.32%、Q2 EPS 49.33元」，代表後端資料本來就有，只是沒有做對應的前端 UI 卡片顯示出來給使用者直接看。
- **搜尋頁「產業別」下拉選單第一次開啟時會裁切在畫面外**：`absolute right-0` 定位算出 `left: -61.5px`，選單本體有61.5px落在viewport左側之外，文字被切到只剩最後2-3個字看不清楚（例如「電子業」只看到「業」）。選過1-2個選項後重新渲染就會恢復正常，只有「首次開啟」會這樣。
- **AI 問答認不得英文公司名「Apple」，只認代號「AAPL」**：問「Apple現在股價多少」得到「查不到」，問「AAPL現在股價多少」才正確回答。首頁自己還有一個「試試看：Apple AAPL」的按鈕暗示可以用名稱查詢，實際上認不得。而且查無資料的錯誤訊息寫死只提到「本站台股涵蓋TWSE/TPEx」，完全沒提美股，容易讓人誤以為不支援美股名稱查詢。

**🟡 輕微（4個）**
- 個股頁點「10年」圖表按鈕後有5~8秒延遲，期間畫面停在舊的時間範圍圖案（按鈕已顯示選取狀態），沒有 loading 提示，容易誤以為沒反應。
- 今日建議頁出現文字拼接錯亂：「0軸上方，判斷多空力道強隨著分界線」（「強隨著」不是正常中文詞彙搭配，應該是「強弱的分界線」被拼接壞了）。
- 重大新聞頁的「📄全文摘要」標籤點擊沒有任何反應，不確定是純標示文字（設計上不需要互動）還是原本該是可互動按鈕但沒接好。
- 多個頁面切換後有2~3秒的骨架屏空白卡片、沒有明確 loading 動畫，容易誤以為卡住。

**⚪ 觀察（非bug，不用修）**：搜尋頁「價漲量增/價跌量增」目前篩選出0筆——這是均量歷史資料庫剛上線、需要約5個交易日累積資料的已知暖機期限制（頁面上已經有說明文字），不是新問題，之後資料累積夠了要記得回頭驗證正常。

**測試限制**：Chrome Claude 這次沒辦法真的把瀏覽器縮到手機寬度（工具回報成功但實際還是維持桌面寬度894px），所以「關注清單手機版橫向滑動清除成本」「AI對話框手機版是否完整顯示」這兩項這次沒有實際驗證到——這兩項在這個對話裡稍早已經被 Opus 用 Playwright 手動模擬390px寬度驗證過是正常的（見上面幾則工作日誌），並非新發現的疑慮，只是 Chrome Claude 這次沒有能力複驗而已。

### 2026-09-15：Opus 對字級版面/^SOX/結算日/台指期夜盤的獨立複查——夜盤功能抓到2個真實bug並修好

針對前一則工作日誌新增的四項（字級版面迴歸修正、費城半導體指數、台指期結算日、台指期夜盤）派 Opus 做規則三獨立複查，結果：

- **字級/版面迴歸、^SOX、結算日**：三項皆 FIXED，逐項附真實數字驗證（例如 ^SOX 本站 11215.78/-5.14% 對比直接打 Yahoo Finance 得到的 11214.18/-5.16%，差異僅在盤中逐秒跳動範圍內；AI 問答實測今天9/14確實在結算日9/16前2天觸發提醒）。
- **台指期夜盤**（`9e627ab`、`838ac33`）：抓到 2 個真實 bug 並修好：
  1. **跨午夜時間戳倒退一整天**：夜盤交易時段橫跨兩個日曆日（15:00~23:59 與隔天00:00~05:00），但交易所回傳的 `CDate` 是「session起始交易日」整段不換日，程式原本直接拼接日期+時間，導致每天凌晨0點到5點這5小時內，畫面跟AI回答都顯示成「昨天」的時間戳，看起來像資料一整天沒更新。修法：新增 `buildAsOf()`，利用「夜盤報價只會落在15:00~23:59或00:00~05:00，中間不存在」這個特性，時間<15:00就代表已經跨過午夜、補上一天，並處理跨月/跨年/閏年進位。
  2. **AI 把最新價誤講成漲跌點數**：問「台指期夜盤表現如何」，AI 答「下跌45562點」——45562其實是最新價（正確跌點應該是215），因為原本 grounding 字串只給「45562點（-0.47%）」沒有把漲跌點數本身放進去，AI 沒有數字可用就抓了價格填。修法：grounding 改成每個數字都附角色標籤（「最新價X點，較昨日結算價漲跌±Y點」），部署後重問4次數字全部正確且跟API吻合。
  3. **順手加固**：近月合約篩選原本用「排除 `-P` 結尾」的黑名單寫法，Opus 發現查日盤時現貨假列的代號其實是 `-S` 結尾會直接穿過篩選（本站固定查夜盤所以還沒踩到，但邏輯上是個真實缺口），改成正面表列 `/^TXF[A-Z]\d/`（TXF+月碼字母+年碼數字），驗證新舊條件在夜盤選出同一筆，行為零變動。

**Opus 額外發現、不在複查範圍內、記錄供之後處理**：`/api/indices` 偶爾會漏掉 TAIEX 這一筆（間歇性，上游資料其實有，原因待查）——行為本身安全（不會用假資料頂替，只是那次請求少一筆），但體驗不佳；`StockChart.tsx` 有一個既有的 `react-hooks/set-state-in-effect` lint 警告（`useEffect` 內同步呼叫 `setIndicators` 讀取 localStorage 設定，是常見且安全的「掛載後從 localStorage 初始化」寫法，只會多觸發一次重繪、不會造成連鎖重繪或無限迴圈，`npm run build` 不受影響），跟 `lib/watchlist.ts` 已經用 `useSyncExternalStore` 解決過同類問題，之後可以考慮比照改寫成同樣的寫法讓程式碼風格一致、順便消除這個 lint 警告。

### 2026-09-14：新增台指期夜盤（近月合約）——研究過程、找到的免費資料源、串接結果

使用者要求：「台股大盤要加一個夜盤」，指的是台灣期貨交易所（TAIFEX）台指期（TX，
大台指）的盤後交易時段（15:00~次日05:00），跟既有的台股加權指數（只在現貨盤中
09:00-13:30 更新）互補，補上收盤後到隔天開盤前這段「資訊真空期」的市場情緒指標。
比照這個 session 稍早 TPEx 那次「先研究免費公開資料源、再串接」的完整流程。

**研究過程（先查過的管道，含排除原因）**：
1. `openapi.taifex.com.tw`（TAIFEX 官方 OpenAPI）的 `/DailyMarketReportFut` 端點
   雖然有 `TradingSession` 欄位明確區分「一般」/「盤後」，但實測在晚上 23:30（當時
   夜盤已經開盤交易中超過 8 小時）呼叫，資料仍停留在 3 天前的最後一個交易日
   ——是「T+1 結算後才正式公布」等級的官方報表，不是近即時資料，排除。
2. `www.taifex.com.tw/cht/3/futDailyMarketReport`（官網歷史行情查詢頁）雖然頁面
   本身有「交易時段：一般/盤後」選單，但這是傳統 JSP 頁面，用 curl 直接 POST
   （含正確的 `queryType=3`、cookie、Referer）測試多次都被導向 404，且背後用來
   動態填契約選單的 `getFutcontract.do` ajax 端點怎麼測都回空清單——判斷這個頁面
   本身高度依賴瀏覽器環境（可能有額外的防爬蟲/session 機制），且就算打通了也只是
   同一份 T+1 報表，沒有必要再花時間排除，直接放棄這個管道。
3. 第三方看盤網站（BigGo財經、CMoney、Yahoo股市等）都只是網頁呈現，沒有公開、
   允許程式化查詢的端點，且條款上不確定是否允許這樣使用，排除。

**真正找到並採用的資料源**：TAIFEX 官方自己的免費看盤網站
`mis.taifex.com.tw/futures/`（不需登入、不需金鑰，網頁本身公開）——這是一個
Nuxt.js SPA，前端呼叫自己的 REST API：
`POST https://mis.taifex.com.tw/futures/api/getQuoteList`，body
`{"MarketType":"1","SymbolType":"F","KindID":"1","CID":"","ExpireMonth":""}`
（`MarketType`："0"＝一般交易時段/日盤，"1"＝盤後交易時段/夜盤）。這個端點跟這個
repo 既有的 `mis.twse.com.tw/stock/api/getStockInfo.jsp`（twse.ts 已經在用）是
同一種交易所「近即時看盤 MIS 系統」架構慣例——找到的方式是直接下載該網站實際載入
的 Nuxt 打包 JS（`_nuxt/*.js`），從裡面的 webpack 路由表找到
`/AfterHoursSession/EquityIndices/FuturesDomestic`（夜盤／國內指數期貨）這個頁面
對應的 lazy-load chunk，再從該 chunk 裡找到實際呼叫 `getQuoteList` 的程式碼跟
`pageAttr` 參數值，不是憑空亂猜端點名稱。

**驗證這是真正近即時、不是延遲資料**：晚上 23:38 呼叫時，回傳的 `CTime` 剛好等於
呼叫當下的時間（精確到秒），確認不是快取/延遲資料；間隔約 1 分鐘重打兩次，
價格、成交量、`CTime` 都正確跟著跳動（45613→45591→45593，22196→22904→22933張，
`CTime` 23:37→23:47→23:48）。另外用第三方頁面 BigGo財經（`finance.biggo.com.tw/
quote/TAIFEX_TX`）交叉核對：同一時間點附近價位落在 45591~45632 這個小範圍內，
方向（下跌）一致，價位量級吻合；漲跌金額不同（BigGo -555 vs 本站 -184）追查後
確認是「參考價基準點不同」造成的正常差異，不是資料錯誤——本站直接沿用交易所自己
回傳的 `CDiff`/`CDiffRate`（基準是當日日盤結算價 45777，這正是 TAIFEX 官方對夜盤
漲跌幅計算的正式基準），BigGo 疑似用前一整個交易日的基準（46187）換算，兩者對
「現在的價位」本身完全一致，只是「相對誰計算漲跌」的慣例不同。另外巧合驗證：測試
當下 `/api/ask` 抓到的真實新聞裡剛好有一則「快訊／台指期夜盤一度大跌近400點！」
（ETtoday財經雲），跟本站當時抓到的下跌走勢方向一致，是額外的真實性佐證。

**換月不用自己處理**：這個端點回傳的「國內指數期貨」清單已經由交易所自己依到期
月份排序好，近月合約永遠是清單裡第一筆 `SymbolID` 開頭 `TXF`、且不是現貨假列
`-P` 後綴的項目，結算日（每月第三個週三）隔天交易所自己就會把新的近月換到最前面，
`taifex.ts` 完全不用自己判斷換月時機。

**交易中／已收盤判斷不用自己猜時間表**：直接沿用交易所回傳的 `Status` 欄位
（`""`＝交易中，`"TC"`＝收盤，其餘 PT/NCP/TH/PO/PC/CO 等罕見狀態一律顯示成「特殊
狀態」，不強行歸類），不像 `marketStatus.ts` 那樣自己寫死時間區間去猜——這樣即使
遇到國定假日交易時段調整之類的特殊情況，也是交易所自己說了算。

**TLS 憑證**：`mis.taifex.com.tw` 的憑證是 Google Trust Services（WE1）簽發，
是全球通用的標準中繼憑證，跟 PROGRESS.md 記錄過的 `www.tpex.org.tw`（台灣本地
TWCA 簽發、境外連線會缺中繼憑證）是完全不同的憑證鏈，兩者不是同一類風險——但
仍照規則二要求，會在正式站部署後實際驗證一次，不能只憑本機測試斷定正式站也一定
正常。

**串接內容**：新增 `src/lib/data/taifex.ts`（`fetchTaifexNightFutures()` +
`describeTaifexNightFutures()`）、`types.ts` 新增 `TaifexFuturesQuote` 型別、
`index.ts` 新增 `getTaifexNightFutures()`（沿用跟其他即時報價一樣的
`QUOTE_TTL_MS`=20秒快取，抓不到或近月合約還沒開出成交價一律回傳 null，絕不用
參考價頂替）、新增 `/api/taifex-futures` route、新增首頁卡片
`components/TaifexFuturesCard.tsx`（放在大盤指數卡片下方、僅台股分頁，client
component 每 20 秒輪詢，狀態徽章跟資料時間完全依賴後端回傳的真實狀態，不用本站
自己猜的時間表）、AI 問答（`ask.ts`）與每日快報/今日建議（`brief.ts`/
`actionBrief.ts`）的「大盤概況」grounding 都加上這行資料（系統提示詞也更新，
明確告訴 AI 這是期貨、要照資料裡的交易中/已收盤狀態講、不要跟現貨加權指數混為
一談）。

**驗證（規則二自測）**：
1. 本機 `npm run build` 通過。
2. 本機 `npm run start` + 帶 unlock cookie 直接 curl `/api/taifex-futures`，
   拿到真實數字（見上方「驗證這是真正近即時」段落）。
3. `/api/ask` 問「台指期夜盤現在多少點？」，確認 grounding 文字正確組出
   「台指期（近月，9月合約）（夜盤交易中，資料時間 2026/09/14 23:48:45）：
   45593點（-0.4%）」這行，且跟大盤指數/新聞其他 grounding 區塊並存不衝突
   （本機沒有設定 AI API key，AI 生成本身沒測到，但 grounding 資料管線本身
   已確認正確）。
4. Push 後用 GitHub commit-status API 確認 Vercel 部署成功（`state: success`），
   接著在正式站（`https://stock-web-blond.vercel.app`，unlock cookie）實際驗證：
   - `GET /api/taifex-futures`：`{"quote":{"contractLabel":"台指期（近月，9月合約）",
     "price":45602,"change":-175,"changePercent":-0.38,"volume":23089,
     "status":"trading","asOf":"2026/09/14 23:52:34"}}`，跟本機測試同一時段的
     數字連續一致（本機45591→45593，正式站45602→45603，同一段時間內合理的
     微幅波動），確認正式站也能正常連到 `mis.taifex.com.tw`（沒有重演 TPEx 那次
     TLS 中繼憑證缺漏問題，跟研究階段判斷的一致：這裡是 Google Trust Services
     簽發的標準憑證）。
   - `POST /api/ask` 問「台指期夜盤現在多少點？」：正式站有真正的 AI API key，
     `usedAi:true`，AI 回覆「台指期（近月，9月合約）夜盤（夜盤交易中，資料時間
     2026/09/14 23:52:34）現在是45602點（-0.38%）」——完全根據真實 grounding
     資料作答，正確帶出交易中狀態跟資料時間，沒有編造或跟現貨指數混淆。
   - `GET /`（首頁）：SSR 輸出的 React payload 裡確認含有伺服器端抓到的真實
     `TaifexFuturesQuote`（`contractLabel`/`price`/`status:"trading"` 都對得上
     同一時間點的 API 回應），確認首頁卡片有拿到真實資料可以渲染。

**已完成規則二自我測試流程（含正式站驗證），還沒派 Opus 規則三獨立複查**——這是
使用者這次特別交代由他自己另外派 Opus，
這次對話完成規則二自我測試後就停下來，見下方「目前已知問題」。

### 2026-09-14：Opus 對整批新功能的獨立複查（規則三）＋字級迴歸修正＋新增費城半導體指數與台指期結算日考量

**Opus 獨立複查結果**（地毯式測試上一輪 9 大項：多股比較/主題股/5分鐘快取/關注清單手機版/圖表新功能/全站文字/新聞全文摘要/搜尋頁篩選/規則八）：8 項直接 FIXED，1 項（新聞摘要）抓到真正的編造等級問題並自行修好：

- **新聞摘要張冠李戴**（`e9187fc`）：正式站實測到一篇英文AI晶片報導被配上完全不相關的「國泰金上修台灣GDP」摘要——`summarizeBatch()` 原本完全信任 AI 自己回報的清單編號，但清單裡夾雜長篇【全文摘錄】時 AI 會數錯編號。修法：要求 AI 每筆回抄標題前12字，程式端用標題比對決定摘要真正屬於哪一則，比對不到就寧可不給摘要也不給錯的。同一個 commit 也修了「AI 回答個股時會自行簡稱股票名稱，導致跟另一家同簡稱公司搞混」（例如「台灣精材3467」被簡稱「精材」，跟另一家公司「精材3374」搞混），系統提示詞新增「一律附完整名稱與代號、不得自行簡稱」。
- **上面那個修正自己造成的新回歸**（`14c4675`，Opus 自己發現並修好）：新增的標題校驗欄位讓 AI 回應撐爆 `maxOutputTokens`，10則新聞只給600 tokens 導致從中間截斷、JSON.parse 直接丟例外、整批摘要變空白，比修之前還糟。修法：token 預算改成動態算（`max(1200, 則數*130)`），並新增容錯解析 `parseSummaryEntries()`，整段 parse 失敗時退回逐個掃描 `{...}` 物件，被截斷時至少保住已經寫完的項目。兩次修正都經過完整 build/push/正式站驗證（30則新聞樣本零錯配零空白）。這再次印證「本機/程式碼審查過關不代表正式站行為正確，一定要實際部署後測」這個 session 反覆出現的教訓。

**其餘 8 項全數 FIXED**，細節與具體驗證數字見 Opus 回報全文（已併入這則工作日誌的判斷依據）：多股比較兩檔資料互相核對無誤且不編造；主題股個股數字逐一核對真實；5分鐘快取數字跟 `/api/indices` 完全吻合；關注清單手機版清除成本並重新整理後確認真的移除；圖表9個時間選項資料筆數合理（5日=5筆...10年=2436筆，10年耗時5.7秒不會卡死）、技術線疊圖經截圖確認正確繪製且切股票後自動沿用設定；全站字級確認18px、灰字對比通過WCAG AA；搜尋頁篩選複選AND邏輯逐層驗證正確、價量關係「全部neutral」經確認是設計上的誠實暖機期行為（均量歷史剛上線，非bug）。

**接著使用者又回報一批新問題，同步處理**：

1. **字級放大造成版面迴歸**（`ea0d536`）：上一輪把根字級從100%拉到112.5%後，ChatWidget 對話框原本用 rem 定義尺寸（`h-[28rem] w-[22rem]`），字級變大後這個固定浮動視窗的物理尺寸也跟著變大（448px→504px），而它是 `bottom-4 right-4` 貼底部定位，變高後在較矮的瀏覽器視窗裡上緣會被裁到看不見，使用者體感就是「AI對話框顯示不完全」。同時使用者也反映「整體要滾動更多才能看到內容」（rem為單位的padding/gap也跟著等比放大，密度下降）、「電視等大螢幕上字體太細」（這是字重問題，不是字級大小問題）。修法：ChatWidget 尺寸改回固定 px（不受字級縮放影響）並加 `max-h-[calc(100vh-6rem)]` 保險；根字級從112.5%降到106.25%（16px→17px，仍比原本100%大，但退回一些密度）；新增 `body { font-weight: 500 }` + antialiased/optimizeLegibility 改善粗細觀感，不支援500字重的字體會自動退回最接近的粗細。
2. **新增費城半導體指數**（`ea0d536`）：使用者說「美股四大指數少一個」——台灣財經媒體慣用的「美股四大指數」在道瓊/S&P500/那斯達克之外，第四個通常是半導體指數（跟台股半導體供應鏈連動最直接）。新增 `^SOX`，已用真實 Yahoo Finance API 驗證代號有效，正式站 `/api/indices` 確認回傳真實數字（測試當天 -5.53%）。
3. **價量關係篩選說明文字補充暖機期提示**（`ea0d536`）：使用者反映這個功能「不太正常，全都分類在量能不明顯」——這其實是設計上的誠實行為（均量歷史資料庫剛上線是空的，需要約5個交易日才會累積出資料），但從使用者角度看像是壞掉，所以在篩選面板的說明文字裡加上「這項功能剛上線，均量資料要累積約5個交易日才會準確，這幾天顯示量能不明顯是正常現象」，避免誤會。
4. **AI 問答新增台指期結算日考量**（`a1d97fc`）：新增 `src/lib/marketCalendar.ts`，用純日曆計算（不需外部資料源，TAIFEX契約規則固定每月第三個星期三結算，不像假日行事曆每年會變）算出台指期結算日，只在接近（2天內）或剛好是結算日當天才會出現在 grounding 裡，提醒法人結算調節可能造成的量價波動跟基本面無關。正式站驗證：今天（9/14，結算日9/16前2天）問「最近大盤波動比較大是什麼原因」，AI 真的在回答裡自然帶出「台指期結算效應：本月台指期即將在2026-09-16結算」這個角度，跟其他真實因素（Fed利率決策、費半重挫等）並列，沒有生硬硬套。
5. **台股夜盤（台指期夜盤）**：使用者要求新增。這需要研究台灣期貨交易所（TAIFEX）有沒有可靠的免費公開資料源，工程性質跟這個 session 稍早的 TPEx 端點調查很類似，已派一個背景 agent 去研究＋視情況串接，結果待回報。

### 2026-09-14：AI 問答多股票比較＋主題概念股、全站快取改5分鐘、關注清單手機版修正、圖表新時間選項＋技術線疊圖、全站文字放大（我自己並行完成的部分，跟同時進行的新聞全文摘要/搜尋頁篩選兩個子 agent 任務分開記錄）

使用者一次提出好幾項要求，並明確要求「能並行處理就並行處理」（見 CLAUDE.md 規則七），這則記錄的是我自己完成的部分：

**AI 問答多股票比較＋主題概念股**（`8701118`）：原本 `guessSymbolFromText` 一次只鎖定一檔股票，「A跟B比較」這類問題只有其中一檔能拿到真實資料。改成 `guessSymbolsFromText`（新增 `universe.ts` 的 `findAllSymbolsByName`），一次最多偵測4檔股票、各自平行撈真實資料；部分股票查無資料時有專門的「部分查無」內部標記，讓 AI 只對真正查不到的那幾檔誠實說明。新增主題股問答：官方產業分類（半導體/航運/金融/生技/鋼鐵/通信網路/光電/資訊服務）直接用 TWSE/TPEx 真實產業分類撈股票；「AI供應鏈」是本站手動整理的小範圍知名個股清單，明確標示「非完整或官方分類清單」。開發時用 brute-force 參考值抓到 `findAllSymbolsByName` 的子字串誤判 bug（「聯發科」會誤連帶比對出不相關的「聯發」1459），已修正。

**全站快取統一改 5 分鐘更新**（`fee6927`）：使用者明確要求全站都要 5 分鐘更新一次。FUNDAMENTALS/EARNINGS/CHIPS/ANNOUNCEMENTS/MOMENTUM/BRIEF/ACTION_BRIEF/FEED/NEWS 這幾個 TTL 全部從 10分鐘~3小時不等統一改成 5 分鐘，warm-cache cron（本來就每5分鐘跑一次）同步新增預熱 daily brief/action brief/news feed 跟 fundamentals/chips/earnings/announcements。刻意不調整 TW_UNIVERSE_TTL_MS（公司清單，24小時——公司不會5分鐘內變更上市櫃或產業別）跟 ITEM_SUMMARY_TTL_MS（單則新聞摘要，12小時——同一則已發布新聞的標題不會變，重新摘要浪費 AI 呼叫又不會得到不同結果），兩者都在程式碼裡附上理由避免之後被誤改。

**關注清單手機版無法清除持有成本**（`4d17293`）：使用者回報「關注名單不能刪除持有股票平均成本」。實測後發現 `updateHolding` 清除邏輯完全正常，真正問題是手機版：表格寬度遠超過螢幕，雖然有 `overflow-x-auto` 可以滑動但沒有任何視覺提示，使用者根本不知道「持有股數/平均成本/損益」被裁切在畫面外。加上 `min-w-[560px]` 確保穩定觸發捲動，並加一行持續顯示的提示文字「← 可左右滑動查看持有股數／平均成本／損益 →」。

**圖表新增 5日/10日/2年/5年/10年時間選項＋技術線疊圖**（`447af2c`）：`ChartRange` 新增 5d/10d/2y/5y/10y；5日/10日用「抓最近2個月、依筆數裁切」處理（TWSE/TPEx 的歷史端點本來就是一次一個月），2年/5年/10年延伸月數迴圈；10年等於單一股票要打到120次月請求，改用 `mapWithConcurrency` 限制同時10個，避免重演這個 session 前面才修過的「批次請求量過大導致上游限流」問題。新增 `lib/indicators.ts` 把既有 `signals.ts` 的 MA/RSI/MACD/KD/布林通道公式包成「回傳整條序列」版本供畫線用，開發時用 brute-force 參考值比對抓到一個真實的 KD 序列位移 bug（%D被錯放在晚2天的位置），已修正並驗證吻合。新增 `lib/chartIndicatorSettings.ts` 用 localStorage 存全域技術線勾選設定，切換股票會自動沿用同一組設定；MACD/RSI/KD 各自用 lightweight-charts v5 的多子圖（pane）獨立顯示，設定改變時整個圖表重建（而非局部增減 pane，因為移除 pane 會讓後面的 pane index 位移，重建對任意勾選組合都更不容易出錯）。

**全站文字放大、對比修正，方便長輩閱讀**（`739952c`、`b6c70a5`）：html 根字級從 100% 拉高到 112.5%（16px→18px，Tailwind 的字級/間距都是 rem 為單位，等於全站等比放大不用逐一手動改元件）；body line-height 從 1.5 拉高到 1.6；用 WCAG 公式驗算發現 `--text-muted`（#898781）在淺色模式只有 3.4-3.5:1 對比、低於 4.5:1 門檻，改成 #6d6b65（同色調更深，5:1以上）；全站十幾個元件裡寫死的 `text-[10px]`/`text-[11px]`（不受根字級縮放影響的固定像素字級）逐一放大到 `text-[12px]`/`text-[13px]`。

**待辦（已排進 CLAUDE.md 規則七的累加清單，尚未處理）**：新聞頁面排版/其他頁面視覺再優化的空間、以及使用者提到的其他任何優化想法，會持續在後續對話追蹤。

### 2026-09-14：搜尋頁新增成交量篩選＋「價量關係」偏多/偏空篩選（跟全站字級調整、新聞全文摘要同時進行的子任務）
使用者在搜尋/篩選頁提出需求：除了既有的股價/漲跌幅/成交量之外，想知道一檔股票的
成交量「比較像買的還是賣的」（原話：「有些成交量大可能都是賣」），並且希望搜尋頁
新增成交量篩選＋這個買賣氣氛篩選、漲跌幅篩選改成可自訂數字（不是只有固定按鈕）、
且不衝突的篩選條件要能一起複選。

**最重要的前提限制（已跟使用者確認過）**：本站所有資料源（TWSE MIS/STOCK_DAY、
TPEx 對應端點、美股 Yahoo Finance）給的都是「收盤/週期性快照」，**沒有任何一個
提供真正逐筆成交的委買/委賣分類**（即真正的「內外盤」——這需要 Level-2 等級的
逐筆成交方向資料，台股/美股都沒有免費公開來源）。所以**沒有做、也不能做**真正的
「買賣單量能」統計，避免在完全沒有資料基礎的情況下編造一個看起來很像真的指標——
這比乾脆不做這個功能更糟糕，違反這個 codebase 一貫「沒有真資料就誠實說沒有，絕不
編造」的原則。

**改做的是有紮實根據、技術分析教科書等級的替代方案**：「價量關係」推論——比較
「今天成交量」跟「這檔股票自己近期的平均成交量」，加上「今天股價漲跌方向」：
- 股價上漲 + 成交量明顯高於自身均量 → 傳統上讀作「價漲量增」（籌碼偏多的常見判讀）
- 股價下跌 + 成交量明顯高於自身均量 → 「價跌量增」（偏空）
- 量能沒有明顯偏離均量 → 中性，不下判斷

「明顯高於均量」的門檻（≥2倍）**直接沿用 `lib/signals.ts` 既有的「爆量」訊號門檻**，
沒有另外發明一個不一致的數字——這代表個股頁顯示「爆量」的股票，在搜尋頁也會同時被
標成價漲量增/價跌量增，全站對這件事的認定是同一套。UI（`StockTable.tsx`）跟型別
（`types.ts` 的 `VolumeTrend`/`SearchItem.volumeTrend`/`volumeRatio`）的每一處註解跟
使用者可見文案都明確寫「這是價量關係推論，不是真實委買委賣單成交量統計」，包含
表格徽章的 `title` 提示文字跟篩選選單裡的說明文字，避免使用者誤以為是真正的買賣單
資料。

**效能設計（新增 `src/lib/data/volumeHistory.ts`）**：明確排除了「對搜尋清單每一檔
股票（TW 約 800~1300+ 檔、US 約 150 檔）各打一次歷史K線 API 來算均量」這個做法——
那樣做規模上會重現 PROGRESS.md 記錄過的 TPEx 炸開問題。查過 `twse.ts`/`tpex.ts`
既有的全市場批次端點，沒有現成的「均量」欄位可以白撿；改用**完全零額外上游請求**
的設計：每次 `fetchMarketQuoteMap()`（index.ts 既有的批次報價函式，search/
highlights/首頁都共用）重新計算時，piggyback 呼叫
`maybeRecordDailyVolumeSnapshot()`——**只在該市場已收盤、且是平日**的條件下，把
這次批次報價裡每檔股票的成交量，累加進一個存在 Redis/記憶體快取裡的「近20個交易日
成交量歷史」（`volume-history:{market}:v1`），用日期比對防止同一天被 warm-cache
cron（每5分鐘觸發一次）重複記錄。`searchStocks()`/`getMultiSignalStocks()` 讀取
這份歷史只需要一次 `peekCached`（純讀取，不觸發任何計算或上游請求），比對照的批次
報價 fetch 便宜得多。**已知的精度限制**（已誠實寫在程式註解跟這裡）：只過濾週末，
沒有完整的國定假日行事曆（TWSE/TPEx 公休日沒有免費公開行事曆可查，跟
`marketStatus.ts` 既有的限制一樣）——真的遇到平日國定假日時，上游資料本來就會維持
前一交易日不變，最多讓同一個真實數字在歷史裡被重複算一次，不是假資料，只是均量
精度上的已知小瑕疵。另外，**這個歷史需要實際部署後累積至少 5 個真實交易日**
（`MIN_HISTORY_DAYS_FOR_AVERAGE`，跟 signals.ts 爆量訊號要求的天數一致）才會開始
產生 volumeRatio/volumeTrend；剛部署的頭幾天，均量資料不足的股票會誠實顯示
`volumeTrend: "neutral"`（沒有假設值湊數），不是 bug。

**篩選邏輯（`SearchFilters`/`searchStocks()`，index.ts）**：新增 `minVolume`/
`maxVolume`（原始股數區間，跟既有 `minPrice`/`maxPrice`同一套慣例）、
`volumeTrends?: VolumeTrend[]`（多選，跟既有 `sectors: string[]` 同一套「不衝突
條件可複選」設計，用 Set 比對）。`/api/search` route 白名單解析
`volumeTrends`（逗號分隔，只接受 3 個合法值，不合法值直接丟棄，不會被當成
「永遠篩不到東西的條件」誤傳進去）。

**UI（`SearchClient.tsx`）**：既有的漲跌幅固定按鈕（`CHANGE_PRESETS`）改成
「自訂區間數字輸入框（可只填一邊）+ 快速套用按鈕」——按鈕只是把值填進輸入框的
捷徑，填完仍可自由修改，不再是唯一入口。新增「成交量」自訂區間輸入框、跟仿照既有
`SectorMultiSelect`（產業多選）同一套 pattern 新寫的 `VolumeTrendMultiSelect`
（價量關係多選 checkbox，選單裡也附上「這是推論不是真實買賣單」的文字提醒）。

**驗證（規則二自測，因為新協助均量資料需要多天真實交易日才會自然出現，本機/正式站
剛部署當下不可能馬上看到真實 buy/sell-leaning 分類，所以分兩層驗證）**：
1. 用本機真實抓到的當前股票資料（聯電2303當時+1.42%/量1.307億股、友達2409
   當時-4.97%/量3.10億股、年興1451當時+0.29%/量9.3萬股）搭配暫時寫入的合理歷史
   均量（透過臨時 debug route 直接寫進快取，驗證完立刻刪除、沒有進 commit），
   確認分類邏輯完全正確：2303→buy-leaning（ratio 2.62倍）、2409→sell-leaning
   （ratio 3.10倍）、1451→neutral（ratio僅1.03倍，未達2倍門檻，即使有均量資料
   也正確判斷為中性）。
2. 用同一份種好的資料驗證篩選：`volumeTrends=buy-leaning` 只回傳2303一檔；
   `volumeTrends=sell-leaning,neutral` 正確回傳其餘1363檔（複選聯集）；
   `minVolume=200000000` 只回傳2409（排除量較小的2303）；`maxVolume=1000000`
   回傳1064檔小量股（含1451）；漲跌幅區間+成交量+產業+價量關係四種條件疊加
   （例如「跌幅0~10% 且量≥5000萬股」「半導體業 且 buy-leaning」）都正確回傳
   AND 交集結果，複選/區間/多條件疊加全部如預期運作。
3. `npm run build` 通過（含中途修正 `WatchlistSection.tsx` 手動組出 SearchItem
   的地方少了新增的必要欄位 `volumeTrend`——那裡是逐檔查詢 `/api/quote/[symbol]`
   組出來的畫面，不是走 `searchStocks()` 的批次清單，沒有均量資料可用，誠實填
   `"neutral"`（沒有訊號），不是編造分類）。
4. **效能實測**（本機 `npm run start`，未開 Redis）：`/api/search?market=US`
   （不涉及 TPEx）冷啟動 1.38秒；`/api/search?market=TW`（TWSE+TPEx合併批次
   報價約1300+檔）冷啟動 45.9秒、**熱快取 0.048秒**（跟改動前的既有基準
   0.047秒幾乎完全一致，確認我的改動本身在快取熱的路徑上沒有增加任何有感延遲）。
   TW 冷啟動時間比 PROGRESS.md 更早記錄的 7.2秒基準高不少，但用單檔報價
   （`/api/quote/2330`，0.337秒，正常）交叉比對後確認**這個落差來自既有的
   TWSE+TPEx 批次報價/TPEx端點重試機制本身（本機環境的網路條件/當下 TPEx 端點
   穩定度），不是這次新增的程式碼造成**——新增的 `maybeRecordDailyVolumeSnapshot`
   跟 `getTrailingAverageVolumeMap` 都是非阻塞（fire-and-forget）或純讀取
   （peekCached），沒有引入任何新的上游請求。真實使用者幾乎不會碰到這個冷啟動
   數字：warm-cache cron 每 5 分鐘背景預熱，跟改動前完全相同。

**沒有改動**：`lib/signals.ts`（爆量門檻只被讀取/沿用，沒有修改原檔）、
`MomentumTable.tsx`（技術訊號共振榜沒有顯示成交量欄位，這次任務範圍明確在搜尋頁，
沒有動這個畫面）、`globals.css`（沒有新增或修改任何 CSS token，徽章顏色直接沿用
`SignalTags.tsx` 已經在用的 `--price-up`/`--price-down` 既有慣例）。

### 2026-09-14：新聞摘要改抓全文，不再只是換句話重複標題（子 agent 任務，跟圖表功能同時進行）
使用者回報：「新聞的部分摘要感覺都只是把標題講得更淺顯易懂而已，不是真正精華出內文的內容」。
追查後確認根因跟猜測完全一致：Google 新聞 RSS 的 `<description>` 欄位本身就不是內文摘要
（實測是一段 HTML，內容只是「標題本身再包一層連結」，不是文章內容），所以 `summarizeBatch()`
先前給 AI 的輸入從頭到尾只有標題，AI 當然只能換句話講標題，不是偷懶。

**新增 `src/lib/data/articleExtract.ts`**，解決兩層真正的工程難題：
1. **Google 新聞 RSS 的 `<link>` 是導轉頁，不是真正出版商網址**——實測發現舊版「直接
   base64 解碼文章 ID 就能還原網址」的技巧現在已經失效（解出來的是另一段不透明字串，不是
   URL，Google 顯然改過編碼方式）。改用目前仍然有效的機制：導轉頁本身的 HTML 內嵌
   `data-n-a-ts`（時間戳）跟 `data-n-a-sg`（簽章），把這兩個值連同文章 ID POST 給 Google
   內部的 `batchexecute`（`Fbv4je` RPC，導轉頁自己的前端 JS 就是呼叫這個端點）就能拿回真正
   的出版商網址——這是 Google 自己導轉頁 JS 實際在做的事，不是繞過任何存取控制，導轉頁本身
   是公開、不需登入的 HTML。
2. **從任意出版商 HTML 擷取內文**：沒有用 `@mozilla/readability`+`jsdom`，改用輕量 heuristic
   （去除 script/style/nav/footer/header/aside/form、抓所有 `<p>` 區塊、過濾少於15字或明顯
   樣板文字如「已將網址複製到剪貼簿」「延伸閱讀」「Subscribe」等、少於200字視為擷取失敗）。
   **先實測 22 則真實新聞（涵蓋 UDN、Yahoo新聞/股市、工商時報、自由財經、中央社、經濟日報、
   ETtoday、今周刊、CNBC、CNET 等台美真實會出現在本站新聞池的來源）比較這個輕量作法跟
   readability+jsdom 的差異，結果 18/22（82%）成功擷取到真實內文，失敗的 4 則有 2 則是真的
   403 擋爬蟲（旺得富理財網、CryptoRank）、2 則是清單/導購頁本來就沒有 `<p>` 內文
   （CMoney、Moomoo）——都是誠實 fallback 該接手的情況，換更重的 readability 也救不回來，
   所以沒有加這個額外依賴，符合「沒有明確理由不要加重依賴」的要求。**

**`src/lib/ai/newsfeed.ts` 改動**：`summarizeItems()` 對「還沒有快取摘要」的項目，先用
`mapWithConcurrency`（上限6，同時也是 Google 新聞導轉頁請求的隱性上限，避免對單一網域炸開
併發）平行呼叫 `fetchArticleFullText()`，成功擷取到全文的項目會在丟給 AI 的清單裡多帶一段
「【全文摘錄】」（最多1500字，在段落邊界截斷避免斷在句子中間），系統提示詞明確指示「有全文
摘錄就根據實際內容寫摘要（可以引用具體數字/原因/影響等細節），沒有全文摘錄的項目才依標題
本身合理描述大意」；擷取失敗的項目自動走原本的標題摘要路徑，不影響同一批次其他項目。新增
`NewsFeedItem.summaryKind`（`"fulltext"|"headline"`）誠實標記每則摘要實際依據哪種輸入，
UI（`NewsFeedList.tsx`）只在真正根據全文的摘要旁邊加一個小小的「📄全文摘要」標記（不是徽章式
大改版，維持這個 codebase 一貫的簡潔風格），headline-only 的維持原樣不做任何標記，不會假裝
兩者一樣可信。快取沿用既有的 `peekCached`/`writeCached`、12小時 TTL、以項目連結當 key 的架構
完全沒變，只是快取的值從純字串換成 `{summary, kind}` 物件。

**沒有改動**：`ITEM_SUMMARY_TTL_MS`、快取鍵值設計、pinned 項目的 `selectPinned()`（那是另一條
獨立的邏輯，這次任務範圍明確只在 `summarizeBatch()`/一般項目摘要，沒有動 pinned 摘要的生成
方式）。這次任務跟同時進行中的圖表功能（`StockChart.tsx`/`twse.ts`/`tpex.ts`/`us.ts`/
`signals.ts`/`types.ts`/chart route）完全沒有觸碰，過程中 build 一度因為圖表那邊還在改
（`StockChart.tsx` 缺 `computeRsiSeries` import）失敗，確認跟新聞檔案無關、之後圖表那邊修好
後重新 build 通過。

**Before/After 對照範例**（真實 fetch 的新聞，同一則的標題 vs 新版摘要邏輯實際會拿到的內文
開頭，示範差異有多大，不是憑空舉例）：
- 標題：「台股超級變數周！FOMC會前兵推 3劇本全解析 2情境『大盤恐破月線』」（工商時報）
  →舊版摘要只能是「台股本週面臨FOMC會議前的重大變數，法人分析出3種劇本」這類換句話重複標題
  的講法；新版實際抓到的全文開頭是「台股本週進入台指期結算、美FOMC利率決策超級變數周。儘管
  美國8月核心通膨高於預期，國際油價及美債殖利率雙雙走跌，仍激勵美股四大指數收紅，台指期夜盤
  同步上漲401點、收46,588點。法人指出，FOMC除利率決策外，聯準會獨立性、最新點陣圖及前瞻
  指引更是重中之重…」——AI 現在能根據這些具體數字（401點、46,588點、8月核心通膨數據）寫出
  真正反映內文重點的摘要，不是只把標題換句話說。
- 標題：「外資猛砍台積電！4天提款2.39萬張　今日買賣前10大曝光」（ETtoday財經雲）→新版能抓到
  「外資今日持續調節台股，單日賣超達369.64億元，其中台積電再遭賣超11,191張，已是連續第4個
  交易日站在賣方，4天累計賣超23,945張…外資持股比由69.3%降至69.23%」這類標題完全沒提到的
  具體數字（賣超金額、持股比變化），新版摘要可以引用這些真實細節。
- 反例（誠實 fallback）：「台積電『買回來了』！杜金龍自曝逆勢加碼…」（旺得富理財網）→ 這個
  來源會擋非瀏覽器請求（HTTP 403），新版程式碼正確偵測擷取失敗、自動退回舊版的標題摘要，
  `summaryKind` 標記為 `"headline"`，UI 不會顯示「全文摘要」標記，沒有假裝這則摘要比實際上
  更可信。

**驗證**：`npm run build` 通過（圖表功能修好之後）。上面的 22 則真實樣本擷取測試用獨立 Node
腳本執行兩次、結果穩定重現 18/22。

**正式站部署後實測又抓到兩個本機測試沒有暴露的真實 bug（跟這個 session 稍早 TPEx 那次「本機
過關、正式站三連敗」是同一類教訓，這次規模小很多但成因類似——本機單次測試環境沒有累積出
會觸發問題的條件）**：

1. **快取鍵沒有隨快取值格式改變升版，導致摘要全部變成 undefined**：部署後第一次
   `curl /api/news-feed?refresh=1` 測試，一般新聞項目的 `summary`/`summaryKind` 全部是
   `undefined`（不是掉回標題摘要，是完全空白）。加了一個暫時診斷路由 `/api/debug-news`
   （驗證後已移除）確認 Google 新聞導轉解析（RSS連結→真實文章網址→batchexecute）在正式站
   完全正常，問題其實出在 `summarizeItems()` 的 per-item 快取：`cacheKey` 沿用舊的
   `news-item-summary:${id}`沒有升版，但這次把快取值從純字串改成 `{summary, kind}` 物件，
   導致當天稍早（12小時TTL內）已經用舊版程式碼寫入的字串快取被誤判成新格式讀出，
   `stored.summary`/`stored.kind` 兩個屬性都是 `undefined`——跟這個檔案自己
   `getNewsFeed()` 已經踩過、也已經修過的同一類問題（`news-feed:v1`→`v2`）一模一樣。
   修法：`cacheKey` 升版為 `news-item-summary:v2:${id}`（commit `57c26b6`）。
2. **`?refresh=1` 強制重算時觸發正式站 504（60秒逾時）**：修好第1點後重測，`refresh=1`
   請求（會疊加：新聞池全部重抓+`selectPinned`AI呼叫+這次新增的全文擷取管線+
   `summarizeBatch`AI呼叫，四件事一次做完）實測跑滿 60 秒被 Vercel 砍斷。原本
   `fetchArticleFullText` 三個網路呼叫的 timeout（3.5s/3s/4.5s，合計最壞11秒/則）搭配
   `FULLTEXT_FETCH_CONCURRENCY=6`，在一頁最多約15-20則新聞、大多數是首次造訪（快取全未命中）
   的情況下，最壞情況會疊加到接近60秒上限。收緊為 2.5s/2s/3.5s（最壞8秒/則）並把並發數
   拉高到10（`commit 31a9605`）。過程中還發現一個更根本、屬於共用工具的潛在問題：
   `cache.ts` 的 `fetchWithTimeout()` 只在 `fetch()` 本身 resolve（也就是收到 response
   header 的當下）就在 `finally` 清掉 abort timer，完全沒有涵蓋後續 `res.text()` 讀取
   body 的時間——對這次抓的頁面（Google導轉頁實測約570-590KB）來說，慢速下載 body
   完全不受 timeout 保護。沒有直接改 `cache.ts`（全站共用、當下還有其他並行工作在動這個
   檔案附近，改動範圍刻意限縮），改成在 `articleExtract.ts` 自己新增
   `fetchTextWithDeadline()`，把 `fetch()`+`res.text()` 包在同一個 abort timer 底下。
   收緊 timeout 後重跑本機 22 則樣本測試，成功率維持 18/22（82%）不變，確認沒有因為時間
   變緊而犧牲成功率。

**兩次修正後的正式站最終驗證**（`https://stock-web-blond.vercel.app`，`POST /api/unlock`
取得 cookie 後測試）：
- `GET /api/news-feed?offset=0&limit=20&refresh=1`：HTTP 200，耗時 23.6 秒（在60秒預算內，
  且這是疊加四件事的最壞情境，一般使用者瀏覽不會觸發 `refresh=1`）。當頁 10 則一般新聞
  全部有摘要，5 則（50%）是根據全文（`summaryKind:"fulltext"`），5 則正確 fallback 回標題
  摘要（`summaryKind:"headline"`，包含明顯會擋爬蟲的 Investing.com、Reuters、cmoney.tw
  清單頁）。
- `GET /api/news-feed?offset=20&limit=20`（下一頁，全新項目，快取未命中）：HTTP 200，
  耗時 16.3 秒，20 則一般新聞裡 19 則有摘要、16 則（80%）是全文摘要——比本機 22 則抽測樣本
  的 82% 更高，因為這頁剛好台股來源（自由財經、ETtoday、UDN 等擷取成功率較高的來源）佔比
  較高。
- 重複請求同一頁（無 `refresh=1`，走快取）：HTTP 200，耗時 1.4 秒，證實 per-item 快取
  （12小時TTL）正確生效，不會每次都重新呼叫 AI 或重新擷取全文。
- **正式站真實摘要範例**（證明不是憑空舉例，是實際部署後真的拿到的內容）：
  「外資賣超369億！狠屠殺1檔8.8萬張 61萬名股東哭了」（TVBS新聞網）→實際產生的摘要是
  「台股跌破46000點，三大法人賣超497億元，外資大砍面板股友達逾8.8萬張，但加碼台企銀等
  金融股」——「三大法人賣超497億元」「友達」「台企銀」這些具體資訊完全沒有出現在標題裡，
  證實摘要真的是根據全文內容產生，不是換句話重複標題。

### 2026-09-14：TPEx 正式站三個獨立根因排查——本機 100% 通過但正式站三連敗的教訓
緊接著下一則（本機建置）工作日誌之後，`npm run build` 通過、本機用
`npm run start` 端對端測試全部正確之後，照規則二 push 上正式站，結果
**正式站上 `/api/quote/3293` 等 TPEx 端點 100% 失敗**，跟本機「幾乎沒問題」
完全相反。這次除錯過程本身值得完整記錄，因為連續踩到三個各自獨立、
且都是「只有在正式站的網路/規模條件下才會出現」的根因，本機測試環境
完全沒有機會暴露：

**根因一：TLS 憑證鏈驗證失敗（不是流量限制、不是資料截斷）**。正式站
`/api/quote/*` 一律回傳「目前無法取得即時報價」，看起來很像截斷問題的
延續，但新增臨時診斷路由直接在正式站呼叫原始 fetch 後，抓到真正的錯誤
是 `TypeError: fetch failed`／`unable to verify the first certificate`——
TLS 憑證鏈驗證失敗，100% 必定失敗、不是偶發。追查發現 `www.tpex.org.tw`
其實是依地理位置分流：從台灣連線（含這次建置時的本機開發環境）會連到
會送出完整憑證鏈（葉憑證+中繼憑證）的節點，但從境外連線（Vercel/AWS
一律算境外，不管設定哪個區域）會被導到只送出葉憑證、缺中繼憑證（TWCA
SSL Certification Authority）的 Cloudflare 節點。**過程中兩次誤判方向**
（先以為缺的是根憑證，補了根憑證完全沒用；後來才透過獨立研究 agent
直接連線 Vercel 對外 IP 重現問題，確認 OpenSSL 錯誤代碼
`UNABLE_TO_VERIFY_LEAF_SIGNATURE` 對應的其實是缺中繼憑證，Node 內建信任
清單其實早就有根憑證了）。修法：`tpex.ts` 改用 `node:https` 直接發送請求
（不用全域 `fetch`，undici 不支援自訂 `ca`），搭配專用的 `https.Agent`
把正確的中繼憑證（`TWCA_SSL_SUB_CA_PEM`）疊加在 Node 預設信任清單之上——
是「補齊缺的那一張」，不是關閉驗證。這個修正過程也曾誤加一張同名但實際
指紋對不上的舊版憑證，靠比對 SHA-256 指紋抓出來移除。

**根因二：TPEx 大檔案端點本身會偶發回應不完整**，獨立於憑證問題之外，
獨立研究 agent 直接對 `tpex_mainboard_quotes` 做多次 `curl -w` 量測後
確診：單次請求的失敗率約 40-60%（HTTP 200 但 JSON 內容中途被切斷，
一次診斷甚至量到同一分鐘內 curl 4 次失敗、Range 續傳 9/9 全部成功），
不是流量觸發、是伺服器（或前面某層 CDN/proxy）本身對完整單次下載不穩定。
修法：`fetchTpexJson()` 讀取回應的 `Content-Length`，不足時用
`Range: bytes=<已收到>-` 只續傳缺少的尾段（`If-Range`/ETag 確保續傳片段
來自同一版本），加上「整個請求失敗就整個重打」的高層重試（畢竟這類
全市場快照一天只需要成功一次）。**這個問題本身分成兩層**：報價快照
（~350KB）在續傳額度 6 次內就能穩定成功；但公司清單端點
（`mopsfin_t187ap03_O`，~1MB，體積約 3 倍大）在同樣額度下仍常失敗，
額度拉高到 25 次才穩定下來——這個「不同端點體積差異導致同一個重試額度
不夠用」的落差，本機測試完全沒測出來（本機測試當下這個端點剛好每次都
成功）。

**根因三：universe 的 24 小時快取沒有隨著「成功/降級」區分 TTL**，是
前兩個根因修好後才浮現的第三層問題。`getTwUniverse()` 合併 TWSE+TPEx
清單時，只要 TPEx 那次 fetch 剛好失敗（即使已經加了前述重試，仍有機率
用盡額度後失敗），程式會把「TPEx 清單是空的」這個降級結果，跟真正成功
一樣用滿 `TW_UNIVERSE_TTL_MS`（24小時）快取住——單一次運氣不好的
fetch，就能讓 TPEx 股票從搜尋/排行/技術訊號共振榜完全消失一整天。
這個問題還連帶造成快取 key 版本號（比照這個 repo 既有的
`news-feed:v1`→`v2` 慣例）被連續污染兩次：`tw-universe-full-raw-v2`
在根因一、二都還沒修好前就被寫入 TWSE-only 的結果鎖住；升版到
`tw-universe-full-raw-v3` 想繞過舊快取，結果那次重新計算剛好又踩到
根因二（公司清單 fetch 又失敗一次），一樣把降級結果鎖住 24 小時。
**修法**：改用 `peekCached`/`writeCached`（而不是 `cached()` 固定 TTL
的包裝）手動控制 TTL——TPEx 清單非空（真正成功）才用完整 24 小時，
TPEx 清單是空的（幾乎必然代表 fetch 失敗，真實公司清單不可能真的是
空的）只用 5 分鐘 TTL，讓下一次請求很快就有機會重新嘗試；同時補上簡易
single-flight 鎖避免拿掉 `cached()` 之後多個並發請求各自重打一次昂貴
的全市場 fetch。快取 key 升版到 `tw-universe-full-raw-v4`。

**驗證過程**：正式站對 3293/6274/6488 各自間隔 21 秒（跨過 20 秒報價
快取 TTL，確保每次都是真正重新 fetch）連續測 5 次，15/15 全部成功、
時間戳記正確更新；`/api/search?market=TW` 在 universe 快取自然過了
5 分鐘降級 TTL 後重新抓取，792 筆結果正確包含 3293/6274/6488/5347/8069；
`/api/ask` 對這幾檔股票分別用中文全名跟代號測共 6 次，全部正確回傳
真實資料（本益比、月營收年增率、季報EPS、三大法人買賣超、融資融券皆
逐項核對跟原始 TPEx API 數字一致），AI 也正確使用白話術語解釋
（例如「外資（外國機構投資人）」「融資（跟券商借錢買股票）」）。
除錯用的臨時診斷路由 `src/app/api/debug-tpex/route.ts` 已在確認修好後
移除。

**最大的教訓**：這次「本機 `npm run build` + `npm run start` 端對端測試
全部通過」跟「正式站三個獨立問題」的落差，不是同一類「本機單一 process
掩蓋跨 instance 記憶體快照」的舊教訓（那是本次會話更早之前修好的問題），
而是三種新的、只在正式站環境才會出現的落差：**地理位置決定的 TLS 憑證鏈
差異**（本機在台灣、Vercel 在境外，連到的是不同節點）、**上游端點本身
在跨網路環境下的不穩定性**（本機當下測試剛好幸運沒踩到，不代表這個
不穩定性不存在）、以及**快取 TTL 設計沒有考慮到「部分成功/降級結果」
該用不同的信任時長**（本機測試通常不會累積 24 小時份的快取狀態去暴露
這種問題）。之後任何跨網路服務整合，都不能只憑本機測試通過就判定
「正式站也會一樣」，尤其是憑證/CDN 分流、上游穩定性、快取 TTL 設計
這三類問題，本機環境結構性地測不出來，必須實際部署到正式站、用真實
多次重複測試才能發現。

### 2026-09-14：TPEx（上櫃）完整涵蓋——報價/K線/基本面/財報/籌碼面全數比照 TWSE 建置完成
延續前一則工作日誌記錄的「下一個主要項目」，這次把它做完：新增 `src/lib/data/tpex.ts`，
涵蓋 TPEx 上櫃股票的報價、歷史日K線、基本面（本益比/殖利率/股價淨值比）、月營收、
季報EPS、三大法人買賣超、融資融券、每日重大訊息、公司清單，全部整合進既有的
`universe.ts`/`index.ts` 架構，讓 `getQuote`/`getChart`/`getFundamentals`/`getEarnings`/
`getChips`/`getMaterialAnnouncements`/`searchStocks`/`getMultiSignalStocks` 這些既有函式
自動涵蓋 TPEx，`ask.ts`/UI 完全不用另外寫 TPEx 專用程式碼路徑。

**開發前先逐一實際 fetch 每個端點驗證真實欄位**（不是照抄先前調查筆記假設），過程中
確認/修正了幾個跟原先假設不同的地方：
- `mopsfin_t187ap03_O`（公司清單）：`SecuritiesIndustryCode` 產業代碼確認跟 TWSE 共用
  同一套分類表（3293鈊象="32"→文化創意業、6274台燿="28"→電子零組件業、6488環球晶
  ="24"→半導體業，皆跟真實產業別吻合），把 `twse.ts` 裡原本的 `TWSE_INDUSTRY_NAMES`
  改名並 export 為 `TW_INDUSTRY_NAMES` 供兩邊共用。
- `tpex_3insti_daily_trading`（三大法人）：欄位名真的如先前調查所述又長又不一致
  （例如 `" Foreign Investors...-Total Sell"` 開頭多一個空格、`"Dealers -TotalSell"`
  跟 `"Dealers-TotalSell"` 兩個不同間距但都存在、數值還不同——這是 TPEx 自己資料
  匯出的瑕疵）。改成用「小寫、去空白後比對子字串」的正規化比對法抓四個
  `-Difference` 淨額欄位，而不是硬寫死完整字串；程式邏輯有考慮到子字串比對順序
  （`foreigninvestorsincludemainlandareainvestors` 這個字串本身內含
  `foreigndealers`，必須先比對前者、否則會誤判成外資自營商欄位）。實測驗算
  `TotalDifference` = 外資(含大陸)+外資自營商+投信+自營商 的加總完全吻合
  （3293當日：-962746+0+35000+6571=-921175，跟欄位直接給的 TotalDifference 一致），
  單位也驗算確認是「股」（跟 TWSE 一樣，不是「張」）。
- `mopsfin_t187ap06_O_ci`（季報）：識別欄位是英文 `Year`/`Season`/
  `SecuritiesCompanyCode`（不是 TWSE 的中文 `年度`/`季別`/`公司代號`），但 EPS 欄位
  本身欄名 `"基本每股盈餘（元）"` 確認跟 TWSE 完全一樣；程式改成動態搜尋
  含「每股盈餘」的欄位名，不寫死。
- `mopsfin_t187ap04_O`（重大訊息）：確認欄位是 `主旨`（沒有尾端空格），跟 TWSE 對應
  端點 `"主旨 "`（有尾端空格）不同，這次事先注意到了沒有重蹈覆轍。
- **單一個股歷史日K線端點**（上一輪筆記說沒找到的那個）：這次確認找到並驗證能用，
  是 TPEx 網站自己的舊版查詢端點
  `www.tpex.org.tw/www/zh-tw/afterTrading/tradingStock/st43_result.php?date=YYYY/MM/DD&code=xxxx`
  （注意 `date` 要用西元年格式，不是 TWSE 慣用的民國年格式），實測 3293、6274 都能
  正確取回整月日K資料，欄位順序（日期/成交張數/成交仟元/開高低收/漲跌/筆數）跟筆記
  一致。
- **報價資料時效性跟預期不同**：原筆記假設 `tpex_mainboard_quotes` 是「近即時」報價，
  實測發現它其實是「每個交易日更新一次」的資料（測試當天盤中多次重新抓取，數值
  沒有隨盤中價格變動），不像 TWSE 的 `mis.twse.com.tw` 是真正逐筆即時。這是 TPEx
  這個免費公開端點本身的限制，已經在程式註解跟 PROGRESS.md 都誠實記錄，不是程式
  邏輯的問題，也不影響資料正確性（只是「當天最新成交」跟「前一個交易日收盤」的
  時效差異）。

**意外發現且已修好的真實 bug（TPEx 端點本身的不穩定性）**：建置測試過程中發現
TPEx 幾個大檔案端點（`tpex_mainboard_quotes`、`mopsfin_t187ap03_O`、
`tpex_3insti_daily_trading`、`tpex_mainboard_margin_balance`，體積約 350KB~1MB）
會有相當機率（實測約 40%）回傳不完整就中斷連線的回應（HTTP 200 但 JSON 內容
中途被切斷），用 curl 跟 Node 原生 fetch 都能重現、獨立於這個專案的程式碼之外，
排除是這次新寫的程式碼的 bug。**套用 CLAUDE.md 規則一**：同一個問題連續嘗試失敗
超過 2 次後，同步派了 Opus 跟 Haiku 兩個 agent 上網查證。Opus agent 直接對
TPEx 端點做了即時診斷（用 `curl -w`／HTTP Range 請求交叉驗證），確認**不是流量
觸發的限流**，而是 TPEx 伺服器（或前面的某層 proxy）本身對單次完整下載的傳輸
不穩定——用 HTTP Range 分段請求反而每次都成功。真正徹底的解法是改成 Range
分段下載，但考量到這個資料在正式站架構下本來就只會「每個快取 TTL 週期最多打一次」
（不是像測試時那樣短時間內打幾十次），改用折衷方案：在 `tpex.ts` 的
`fetchTpexJson()` 加上「最多重試 3 次、每次間隔拉長到秒級（1s/2s）」的重試機制，
用機率換算大約可以把單次呼叫的失敗率從約 40% 壓到約 6%，重試後仍失敗則沿用既有
「回傳 null/空 Map，不产生假資料」的容錯設計，不會讓整個網站掛掉。

**Universe 整合設計**：`UniverseEntry` 新增內部 `exchange?: "TWSE"|"TPEx"` 欄位
（只用於路由到對的資料源，對外的 `Market` 型別仍然只有 `"TW"|"US"`，沒有新增
公開的市場值）。`getTwUniverse()` 平行抓 TWSE+TPEx 官方清單並合併（各自獨立
try/catch，一邊掛掉不影響另一邊；TWSE 若整個抓不到才退回種子清單，TPEx 抓不到
純粹就是暫時沒有 TPEx 資料，不會連帶清空 TWSE）。批次查報價清單上限拆成兩個獨立
常數：`MAX_TWSE_UNIVERSE`（500，維持不變）+ `MAX_TPEX_UNIVERSE`（300，新增）——
分開設定是因為 TPEx 的批次報價端點本來就是「一次回傳全市場」，不像 TWSE 用
50檔一批的併發分批，所以放寬 TPEx 這邊的清單上限不會增加 TPEx 自己上游的併發
負擔。TPEx 公司清單刻意依「實收資本額」（真實回傳的官方欄位，不是憑空排序）
由大到小排序，確保知名大型上櫃股（環球晶排名第18、台燿第38、鈊象第40，滿分891檔）
在 300 檔上限內都排得進去，不會因為代碼數字順序被排擠掉（TPEx 代碼不像 TWSE
那樣「代碼小=老牌大公司」，排序方式不能直接套用）。`getQuote`/`getChart`
（單一資料源、需要先判斷走哪個交易所）新增 `resolveTwExchange()`：已知在
universe 裡的股票直接走對應交易所；真的查不到所屬交易所的代號（極新的IPO或
打錯代碼）才先試 TWSE、失敗再試 TPEx，不影響已知股票的查詢延遲。基本面/財報/
籌碼面/重大訊息這幾個「整個市場一次回傳」的類別，改成平行呼叫 TWSE+TPEx 兩份
資料各自的抓取函式並合併 Map（`mergeTwMaps()`），因為兩邊股票代碼是同一套國家
編碼、不會撞號，快取 key 維持不變（沒有新增快取項目，只是同一個 key 底下的內容
變廣）。

**效能**：批次報價（TPEx 全市場一次回傳、TWSE 50檔一批分批）、單股查詢
（`fetchTpexQuoteSnapshot()` 把整個 TPEx 全市場快照包在 `cachedMap()` 裡、
20秒 TTL，讓同一個 20 秒窗口內查詢任何 TPEx 個股都共用同一次上游 fetch，
不會每查一檔就重打一次全市場——這點跟 TWSE 不同：TWSE 的 MIS 端點原生支援
單檔查詢，TPEx 完全沒有這個能力，所以额外做了這層快照快取來達到等效的效率）、
產業/名稱清單合併（純記憶體比對，數量級對效能無感）都沿用或延伸既有 Redis
快取+背景 cron 預熱架構，`api/cron/warm-cache` 呼叫的 `searchStocks`/
`getMultiSignalStocks` 本身就是市場無關的抽象，不需要另外改 cron 程式碼就會
自動連帶預熱 TPEx 資料。本機測（未開 Redis，純記憶體快取，僅供參考，正式站數字
見下方）：冷啟動 `/api/quote/3293`（含 TPEx 上游重試）2.7秒，熱快取 0.042秒；
冷啟動 `/api/search?market=TW`（TWSE+TPEx 全部合併批次查價）7.2秒，熱快取
0.047秒——**正式站（有 Redis、有背景 cron 預熱）的實際數字見下一則工作日誌**，
本機數字因為沒有 Redis、也沒有 cron 預先跑過，冷啟動時間會比正式站真實情況更慢，
僅供本機測試階段的相對比較用，不代表使用者實際會遇到的體感速度。

**本機端對端驗證**（`npm run build` 通過；用 `npm run start` 起本機服務、走真實
上游資料）：3293鈊象、6274台燿、6488環球晶、5347世界（先進）、8069元太五檔，
`/api/search?market=TW`、`/api/quote/[symbol]`、`/api/chart/[symbol]`（1m/3m range）、
`/api/ask`（用中文全名跟代號分別測）全部正確回傳真實資料，數字跟直接呼叫 TPEx
原始 API 比對一致（本益比、殖利率、股價淨值比、月營收年增率、季報EPS、三大法人
買賣超、融資融券餘額皆逐項核對過）；技術訊號（均線排列、MACD含0軸判讀、RSI、
布林通道）也正確從 TPEx 的K線資料算出。`getMultiSignalStocks("TW")` 執行無錯誤
（沒有刻意檢查該次結果一定要出現 TPEx 個股，因為這個排行本來就是照當天漲跌幅
排序，TWSE/TPEx 誰上榜純粹看當天誰漲跌比較大）。**過程中一度以為
名稱查詢有嚴重 bug**（問「鈊象科技」/「鴻海」/隨便打一串中文，AI 回答都變成
美股某支股票如C花旗），追查後發現是自己下 curl 測試指令時，用 shell 直接內嵌
中文字串傳給 `-d` 參數在這台機器的環境下編碼會被打亂，改成先把 JSON 寫進
UTF-8 檔案再用 `--data-binary "@file"` 傳送就完全正常——**這是我自己的測試方法
問題，不是程式的 bug**，記錄下來避免下次又被同樣的假警報誤導。

### 2026-09-14：擴大股票查詢覆蓋率、新聞全面加摘要、深色模式配色修正（`1843959`、`a50790f`）
使用者回報三個問題，這次同時處理，過程中還意外挖出一個影響範圍更大的架構性 bug：

**1. 股票查詢覆蓋率太小，本益比/月營收等資料查不到**：追查後發現這些 TWSE OpenAPI
端點本來就涵蓋全市場，真正的瓶頸是「用中文名稱查詢」這條路只搜尋 `MAX_TW_UNIVERSE`
這個為了保護 TWSE 批次報價併發量設的 200 檔上限清單。拆成兩個獨立快照：
`twUniverseSnapshot`（200→500，給批次查報價的排行/搜尋頁用，上限本身合理保留）跟新增
的 `twFullCompanySnapshot`（完整未受限的官方全市場約 1000+ 檔清單，只給名稱/代號查詢
用）。

**架構性 bug（比原本設想的更根本）**：修好上面的擴大之後，正式站用「美利達」重測，
等了超過部署完成後 5 分鐘還是查不到，GitHub 也確認 Vercel 部署已成功——這不是部署
延遲，是本機測試環境本身掩蓋了真正的問題：`twFullCompanySnapshot`/`nameLookupPool`
是模組層級的記憶體變數，只有真的呼叫過 `getTwUniverse()` 的那個 process 才會被填入
完整清單；本機是單一長駐 process，前面測試剛好呼叫過 `/api/search` 而「順便」填好，
掩蓋了問題。但正式站 Vercel 是很多個各自獨立的 serverless instance，單純問個股問題
的 `/api/ask` 從來不會觸發 `getTwUniverse()`，一旦請求打到「沒跑過」的 instance，就會
靜默退回 44 檔種子清單，查不到清單外的任何股票——**這個問題其實原本 200 檔版本就存在，
只是這次擴大清單才讓它變得容易被注意到**。修法：`guessSymbolFromText` 呼叫
`findSymbolByName` 前先 `await getTwUniverse()`，底層資料是 Redis 快取、跨所有
instance 共用，所以除了真正冷啟動，其餘都是快速的快取命中。正式站重測「美利達」連續
8 次全部正確查到完整資料（報價/技術訊號/基本面/財報/籌碼面/新聞）。**教訓：本機用單一
長駐 process 測試「跨請求共用的記憶體快照」這類邏輯時，很容易因為前面剛好跑過其他會
填充該快照的路徑而看起來正常，掩蓋了在 Vercel 多 instance 環境下的真實問題；这類邏輯
要嘛確保呼叫端自己會觸發填充、要嘛測試時模擬「全新、什麼都沒跑過」的情境。**

**2. 重大新聞頁只有置頂項目有摘要**：一般項目多達數百則、每 20 分鐘重新整理一次，
若比照置頂項目在生成當下全部摘要，AI 用量會遠超免費額度。改成「使用者捲到哪一頁，
才對那一頁做摘要」：新增 `summarizeItems()`，每則新聞用自己的連結當 key 個別快取
12 小時。`cache.ts` 新增 `peekCached`/`writeCached` 兩個基礎函式，因為既有的
`cached()` 沒辦法做「檢查誰已經有快取、只對缺的批次呼叫 AI」這種模式。正式站驗證：
第 2 頁（第 21-25 則）5 則一般新聞全部正確產生白話摘要。

**3. 深色模式重大焦點卡片藍色不好看**：既有 `--accent-soft`（#184f95）用在小徽章/
按鈕沒問題，但同一個高飽和度顏色填滿整張卡片在深色主題裡很突兀。新增專用
`--highlight-bg` token，深色模式改成 #17253c（用 WCAG 公式驗算過，文字對比不減反增：
白字 15.37:1、次要文字 8.58:1，原本分別是 8.1:1、4.52:1，但這個顏色本身跟
`--surface-1` 只差 1.13:1，是很接近底色的低調色調）。深色模式兩個 CSS 區塊都同步
更新（這個檔案有多次「只改一處」的前科）。截圖確認修正後視覺明顯更協調。

**上一輪 Opus 規則三複查挖出 2 個真實問題（1 個高風險）+ 2 個低風險瑕疵，已經全部
修好並自己驗證過（`2ccd5be`、`185202a`），已經完成最終 Opus 複查確認**：

1. **【高風險】AI 對資料庫查無的股票會編造具體數字**：使用者問到上櫃/興櫃股票
   （例如鈊象科技3293、台燿6274，這兩者本站目前完全沒有涵蓋——台股資料只包含證交所
   上市公司）時，AI 會用自己訓練資料裡的舊知識編造本益比、成交量、法人買賣超、RSI
   等具體數字，講得煞有其事，直接違反本站「查不到就誠實顯示、絕不編造」的核心原則。
   修法：`guessSymbolFromText` 比對不到任何股票、且不是「推薦股票」這類泛問題時，
   grounding 新增明確的【查詢結果】區塊告知「沒有比對到任何股票」；系統提示詞最前面
   新增最高優先權規則，明確禁止用自己的知識填補任何具體數字。正式站驗證：針對鈊象
   科技、台燿分別重測 5 次跟 3 次，全部正確回答「目前查不到這檔股票/公司的資料」，
   完全沒有再出現任何編造的數字。
2. **【中風險、影響全站】globals.css 的 `* { border-color: var(--border); }` 沒有
   包在任何 `@layer` 裡——Tailwind v4 的 utility class 都在 `@layer utilities`，
   未分層的 CSS 一律贏過任何 `@layer` 包裹的規則，導致全站每一個 `border-(--accent)`
   /`border-(--gridline)` 這類顏色 utility 都被靜默蓋掉。改成包進 `@layer base`。
   Playwright 驗證搜尋頁篩選 chip 的 border-color 正確變回 `#2a78d6`。

**最終 Opus 複查結果（4 項全部確認修好，另外抓到 1 個由修正 1 附帶產生的新瑕疵，已修正）**：
用真實 chat widget、28 次全新對話（每次都重整頁面清空記憶體）測試上櫃股票（鈊象3293、
台燿6274、世界先進5347、環球晶6488、信驊5274 等 8 檔真正未涵蓋的股票，事先用
`/api/search` 回傳空陣列驗證這些確實是「真的查不到」不是誤判）：28/28 完全沒有編造
任何數字，也用數字掃描腳本自動確認過。CSS border 修正在正式站量到選中的篩選 chip
`border-color: rgb(42, 120, 214)`（= `#2a78d6`），三種 border utility 各自解析成
不同顏色，證實不再被覆蓋。429/timeout 錯誤訊息這次測試沒遇到任何 AI 呼叫失敗，程式碼
邏輯確認 `humanizeProviderError` 有掛在兩個 provider 的 catch block，但沒有真實案例
可驗證。關注清單按鈕對比度：淺色 14.87:1、深色 8.10:1，皆遠超 WCAG AA。

**Opus 額外抓到的新瑕疵（【查詢結果】內部標記洩漏到使用者看到的回答）**：約 11%
（3/28）比對不到股票的回答裡，AI 會把 grounding 裡那段「內部提示用」的
`【查詢結果】...` 標記文字整段照抄當成自己的回答輸出，其中 2 次甚至只回一句
`【查詢結果】` 6 個字、完全沒有任何說明，使用者體驗很差（雖然「不編造數字」這個核心
安全機制本身沒被破壞）。根因：這段標記是用一般文字塞進 grounding 給模型看，寫得像一句
完整的中文句子，模型偶爾會誤判成「這就是要講的答案」直接照抄。修法雙管齊下：
(1) 把 grounding 裡的措辭改寫成明確標示「【內部系統標記／非使用者可見文字，禁止原樣
照抄輸出】」+ 額外一句「請用你自己的話...不要複製這段標記文字本身」，讓它更不像可以
直接引用的答案內容；系統提示詞裡對應說明也同步更新，明講「絕對不要把參考資料裡的內部
標記文字（含中括號【】包住的內部提示語）直接照抄貼到回答裡」。(2) 加一層保險：新增
`sanitizeLeakedMarkers()`，在回傳給使用者前用正則清掉任何殘留的
`【內部系統標記...】`/`【查詢結果】`（含舊版洩漏出去的殘留格式）字樣，清完後若整段
變成空字串，才退回原本那句誠實的「目前查不到」訊息，確保就算模型還是誤判，使用者也
絕對不會看到裸露的中括號標記或空白回答。已用 4 組代表性輸入（純標記、純內部標記、
標記+後續說明、正常有數字的回答）跑過單元測試，行為皆符合預期；`npm run build`
通過。
3. **【低風險】Gemini 429 額度用完時，使用者會看到完整原始英文錯誤 JSON**。新增
   `humanizeProviderError()`，常見的額度/逾時錯誤換成中文白話說明。
4. **【低風險、非本輪造成】ChatWidget「分析我的關注清單」按鈕文字對比不足**
   （3.34:1 淺色/2.23:1 深色，低於 WCAG AA 4.5:1）。改用 `--text-primary`，
   對比度提升到 14.87:1/8.10:1。

**過程中意外發現一個值得記錄的擴充方向，使用者已決定列為下一個主要項目**：TPEx
（上櫃/興櫃）目前完全沒有涵蓋，且證實 TPEx 確實有等同 TWSE 的免費公開 OpenAPI
（`tpex.org.tw/openapi/v1/...`，不需金鑰），是可行的未來擴充，但完整涵蓋（即時
報價/歷史K線/基本面/財報/公司清單）需要另外建一整套等同 `twse.ts` 規模的新模組，
工程量不小。使用者確認：「要，列為下一個主要項目」；並進一步指出核心訴求不是
「讓 AI 對查不到的股票誠實回答查不到」就結束（那只是安全底線），而是要真的讓這些
股票查得到、給得出正確資料與分析——這正是 TPEx 擴充工程要解決的問題，詳見下方
新的工作日誌條目。使用者也問過「加了 TPEx 會不會拖慢網頁開啟速度」，答覆：不會，
理由跟既有 TWSE 架構一致——(1) 所有資料都會走 Redis 共用快取 + 背景 cron 預熱，
使用者實際造訪頁面時讀到的是已經算好的快取結果，不是即時打 TPEx API；(2) 批次
報價本來就用固定併發量的分批平行請求（TWSE 現在是每 50 檔一批），TPEx 只是多開
幾批平行請求，不是每股票多一條序列連線；(3) 名稱/代號查詢的清單合併只是記憶體裡
多幾百筆資料比對，數量級對效能無感。會在完整實作後實測冷啟動（快取全空）跟熱啟動
（快取命中）的實際回應時間，用數字驗證這個結論，而不是只用口頭保證。

### 2026-09-14：TPEx（上櫃）擴充——確定為下一個主要項目，API 端點調查進行中
使用者確認 TPEx 列為下一個主要項目，並釐清目標不只是「AI 誠實說查不到」，而是要
讓上櫃股票真的能查到正確資料。已確認 TPEx OpenAPI（`www.tpex.org.tw/openapi/v1/`，
免金鑰）以下端點可用、欄位結構跟 TWSE 對應端點高度相似：

- `tpex_mainboard_daily_close_quotes`／`tpex_mainboard_quotes`：全市場每日/即時報價
  快照（不支援 `?stockNo=` 單檔篩選，會忽略該參數回傳全市場，含債券 ETF 混雜在內，
  之後解析要過濾）。對應 TWSE 的 MIS 批次報價。
- `mopsfin_t187ap03_O`：公司基本資料清單，對應 TWSE `t187ap03_L`。
- `tpex_mainboard_peratio_analysis`：本益比/殖利率/股價淨值比，對應 TWSE `BWIBBU_ALL`。
- `mopsfin_t187ap05_O`：月營收，對應 TWSE `t187ap05_L`。
- `mopsfin_t187ap06_O_ci`：季度財報（含EPS），對應 TWSE `t187ap06_L_ci`。
- `tpex_3insti_daily_trading`：三大法人買賣超，對應 TWSE `T86`，但欄位是英文描述性
  長名稱（例如 "Foreign Investors ... -Total Buy/Sell/Difference"），跟 TWSE 短中文
  欄位命名風格不同，之後 parser 邏輯要分開寫，不能直接照搬。
- `tpex_mainboard_margin_balance`：融資融券餘額，對應 TWSE `MI_MARGN`，欄位確認為
  `Date`/`SecuritiesCompanyCode`/`CompanyName`/`MarginPurchaseBalance` 等，結構清楚。

**還沒找到的關鍵端點**：單一個股的歷史日K線序列（對應 TWSE 的 `STOCK_DAY`，是畫
K線圖必須的資料）。已嘗試 `tpex_mainboard_daily_close_quotes?stockNo=` （參數被
忽略）、TPEx 舊站 `afterTrading/tradingStock` 系列網址搭配多種日期/代號參數格式
都回傳 `{"stat":"參數輸入錯誤"}` 或 302 轉址，尚未成功。下一步會嘗試更多參數命名
變體、或改抓 TPEx 網站本身頁面實際發出的 API 請求格式來反推正確用法。這個端點沒
找到之前，TPEx 個股頁的 K 線圖功能無法實作，但報價/基本面/財報/籌碼面已足夠支援
AI 問答先行涵蓋這些股票——會視調查進度決定是否先讓 AI 問答/清單/搜尋涵蓋 TPEx（
沒有K線），K 線圖之後再補上，還是等端點齊全後一次做完。

**同一輪也修好 Opus 最終複查抓到的新瑕疵**（見上方 2026-09-14 第一則工作日誌的
「Opus 額外抓到的新瑕疵」段落）：`【查詢結果】` 內部標記偶爾洩漏到使用者可見的
回答內容，已改寫成明確標示「內部系統標記」的措辭 + 新增 `sanitizeLeakedMarkers()`
保險清除，`npm run build` 通過。

### 2026-09-13：使用者貼真實對話內容，補強技術指標判讀+跨市場關聯知識（`777f3ed`、`4e4c293`）
延續前一則的兩個分享連結（讀不到內容那個問題）——使用者這次直接把兩段 ChatGPT 對話全文
貼過來：一段是 MACD 的判讀教學（含真實股票截圖範例），一段是升息如何透過三個機制、三個
階段、產業別分化影響股市的完整拆解。使用者也澄清這次要求的範圍不是只有這兩段內容本身，
而是要廣泛加強「各種線怎麼看（不只MACD）」跟「各種直接、間接影響關係（不只升息）」，
AI 問答一定要整合進去。

**MACD 0 軸判讀（`lib/signals.ts`）**：對話裡提到業界慣用的判讀方式——黃金/死亡交叉如果
發生在 0 軸上方，訊號比較明確；在 0 軸下方（低檔黃金交叉、續跌死亡交叉）力道相對較弱。
原本 `computeMacdCross()` 只回傳 `"golden"/"death"`，沒有這層資訊，等於算出 MACD 線的
實際數值後又把它丟掉。改成順便回傳 MACD 線當下是否在 0 軸之上，標籤變成四種組合（例如
「MACD死亡交叉（0軸下方，屬續跌訊號，力道較弱）」）。

**新增均線多頭/空頭排列**：既有訊號只檢查「目前價格 vs 20日均線」，沒有檢查均線彼此之間
的排列——5日線在10日線之上、10日線在20日線之上（多頭排列）代表短中長期趨勢一致向上，
反過來（空頭排列）代表趨勢一致向下，這是技術分析裡另一個獨立的經典觀察角度。新增
`computeMaAlignment()`。**這跟既有的「站上/跌破20日均線」是兩個不同層次，可能同時出現、
也可能看似矛盾**：本機測試台積電時就真實出現「均線多頭排列」同時「跌破20日均線」的組合，
一開始懷疑是計算錯誤，實際手動核算真實K線資料（MA5=2451.0>MA10=2428.5>MA20=2411.0，
當日收盤價2410 剛好些微跌破 MA20）後確認兩個訊號都正確、完全合理——均線本身仍反映之前
的多頭趨勢，但股價剛发生短期回檔，這是正常的技術面現象，AI 也已經有「面向矛盾時要老實
點出」的既有規則可以妥善處理，不需要特別再處理。**教訓：新增的技術訊號如果跟既有訊號
同時出現、看起來矛盾，先手動核算真實數字確認是不是算錯，不要一看到「矛盾」就假設是
bug——技術分析裡本來就存在很多「短期訊號 vs 中期訊號不一致」的正常情況。**

**擴大直接/間接影響關係清單（`ask.ts`/`brief.ts`）**：在既有的美債殖利率/匯率/油價/
半導體庫存週期清單之外，新增兩個常見關係：CPI（消費者物價指數）或非農就業數據公布本身
就是市場短期波動的直接觸發點（因為會立刻改變市場對升息/降息的預期）；重要權值股或產業
龍頭（例如台積電、輝達）公布財報或財測展望，常直接牽動整個供應鏈/同族群類股，不是只
影響那一檔自己。同時把新出現的「0軸」術語加進籌碼術語解釋清單，用前一輪已經驗證有效的
「第一次出現就要附帶白話解釋」規則處理。

正式站已用真實 AI 驗證：問鴻海（2317）「MACD狀況是什麼？」，正確回答「死亡交叉（0軸是
判斷多空力道強弱的分界線）且在0軸下方，這屬於續跌訊號，代表目前空方力道較強」——「0軸」
術語正確解釋、強弱判讀也正確傳達。本機驗證 9 檔股票技術訊號抽測，均線多頭排列正確出現
在多檔股票上。

**Opus 規則三複查：3 項全部確認正確，沒有真的 bug**。這次複查特別紮實：Opus 自己
獨立重新實作了一份 MACD 計算邏輯（不是直接讀原始碼），拿正式站真實 K 線資料驗算，
確認 0 軸上下判斷跟部署的程式碼一致；也手動核算了 7 檔股票（含台積電、NVDA、META
等）的 MA5/10/20，逐一核對跟頁面實際顯示的均線排列訊號完全吻合，包含台積電「均線
多頭排列」同時「跌破20日均線」這個一開始容易誤判成 bug 的組合，Opus 也獨立確認是
真實資料算出來的正確結果。CPI/財報連動兩個新增的 macro 關聯也確認會在真的有對應
新聞資料時自然帶出、沒有強行套用。唯一記錄下來但不用修的小地方：AI 生成文字裡出現
一次「空手」應該是「空頭」的用詞筆誤，這是 LLM 生成內容的隨機瑕疵，不是 `signals.ts`
裡的標籤寫錯（標籤本身逐一確認都正確），沒有重複出現就不需要特別處理。

### 2026-09-13：新增「今日建議」新手頁面；技術訊號加布林通道/KD；AI 問答更好懂並整合更多面向（`ff37c9b`、`8104a0e`）
延續上一則「重大新聞」的三個後續要求：① 卡片不要整張點擊就開連結，要拆成摘要+連結兩塊，
而且不能只依賴新聞、要有真實數據；② 網站終極目標是讓完全沒有股市背景的人也能快速看懂
現況、知道該怎麼做，AI 問答要更好懂、更整合各方資訊，並新增一個「建議採取動作」頁面；
③ 技術面不要只看 MACD，也要納入其他常見指標，並學習更多「間接影響關係」不只升息一種
角度。使用者附了兩個對話分享連結（ChatGPT、Google AI Mode）供參考。

**誠實記錄一件事**：那兩個分享連結實際上讀不到內容——ChatGPT 分享頁是純前端渲染的 SPA，
`WebFetch` 只拿得到 `<title>`（「解釋MACD線」），Google AI Mode 連結轉址後是一個空的
搜尋錯誤頁。已經跟使用者說明這件事，沒有假裝讀過那兩個連結的實際內容，後續技術指標與
總經關聯性的擴充都是用既有的技術分析/總經常識自行設計，不是照抄那兩個連結裡的內容。

**重大新聞頁修正（`ff37c9b`）**：
- `NewsFeedRow` 不再整張卡片包一個 `<a>`：標題/來源/時間維持原樣，置頂項目多一段 AI
  白話摘要，最下面才是獨立的「查看原文 ↗」（外部新聞）或「查看個股頁面 →」（本站真實
  數據）連結，只有這個小連結才會真的跳走。
- 置頂分類的 AI 呼叫（`selectPinned`）現在同時要求對每則入選消息寫一句白話重點+可能
  影響（60字內），回傳格式改成 `{index, summary}` 物件陣列，一樣是引用清單編號、不是
  重寫標題，避免 AI 把原文改寫走樣。
- 新增 `buildDataCards()`：把已經在跑、已經驗證過準確的技術訊號資料（跟 `/highlights`
  同一套引擎）包成資料卡混進新聞池，`link` 指向本站 `/stock` 頁而不是外部新聞來源，
  卡片上明確標示「真實數據，非新聞」——直接回應「新聞可能不可靠，要有具體真實數據」，
  沒有另外接新的外部資料源，全部沿用站內已經驗證過的既有函式。8 大面向框架裡的法說會
  逐字稿/供應鏈(DIGITIMES)/總體經濟(FRED)/期權(TAIFEX) 這幾塊目前完全沒有資料管道，
  各自都是獨立的新工程，這次沒有動，留待之後評估是否要做。
- 同時修好上一輪 Opus 規則三挖出的 3 個問題：新聞池沒有時效過濾（新增 14 天過濾，Opus
  發現約 23% 項目超過 30 天甚至有 2022 年的內容）、置頂卡片來源/時間文字對比度不足
  （改用 `--text-secondary`，WCAG AA 4.5:1 都能過）、`<a>`/`<div>` 直接當 `<ul>` 子元素
  不符合 HTML 規範（改成每項包一層 `<li>`）；React key 也從池子索引改成用文章 `link`
  當 id，避免快取重新生成時理論上可能撞號。

**新增「今日建議」頁面（`8104a0e`，`/action`，並行由一個 subagent 在獨立 git worktree
建置，完成後合併回主分支，同時我自己並行處理 AI 問答與技術指標那兩塊）**：新的
`lib/ai/actionBrief.ts` + `api/action-brief/route.ts` + `ActionBriefCard.tsx` +
`app/action/page.tsx`，架構完全比照 daily-brief（20分鐘快取、`forceRefresh` 逃生艙、
client-side fetch 不擋頁面渲染）。內容鎖定「完全不懂股票的人」：任何專有名詞出現都要
順手白話解釋、字數控制在200-350字、三段式（一句話大盤氣氛／3-5點具體點名個股或事件／
一句風險提醒），資料來源全部沿用既有函式（大盤指數、技術訊號共振股、台股籌碼、重大
新聞頁已經篩過的置頂事件），沒有另外接新資料源。導覽列新增「今日建議」連結（刻意放在
首頁後面，因為使用者特別強調這是核心頁面）。

**技術訊號新增布林通道與 KD（`lib/signals.ts`）**：在既有的 `computeSignals()` 裡加兩個
新的客觀訊號——布林通道（20期均線±2倍標準差，股價觸及自己的上緣/下緣才觸發，刻意不做
「通道窄縮」訊號，因為「窄」只有跟股票自己的歷史通道寬度比較才有意義，固定寬度百分比
門檻在不同股票間會失真）；KD（隨機指標，9,3,3 業界慣用參數，跟 MACD 一樣只在真正
黃金/死亡交叉當天觸發，且只在超賣/超買區 20/80 才算數，避免中段區間雜訊交叉被當訊號）。
因為 `computeSignals()` 回傳的 `Signal[]` 全站通用消費（UI 標籤、三個 AI 功能都是直接
join label），加這兩個訊號不需要另外改任何顯示邏輯。本機驗證：恩德(1528)、中砂(1560)、
中華電(2412) 都正確觸發布林通道上緣訊號，跟該股當天實際大漲/創高走勢吻合。

**AI 問答改進（`ask.ts`）**：
1. **修正一個真實缺口**：個股問答原本完全沒有技術訊號資料——只有被技術訊號共振股篩選
   （至少2個訊號同時成立）選中的股票才會在「今日焦點數據」看到訊號，問任何其他個股
   （例如「台積電最近如何」）AI 完全看不到均線/RSI/MACD 狀態。現在 `buildStockGrounding`
   也會算一次 `computeSignals()`，任何個股問答都有自己的技術訊號可以引用。實測 7 檔不同
   股票，技術訊號行都正確顯示（台積電跌破均線+連跌、聯電死亡交叉、中華電站上均線+
   RSI超買+布林上緣等）。
2. 系統提示詞新增「用到任何專有名詞都要順手白話解釋」「結論要明確表態」等規則。
3. 新增「不要每次分析漲跌都只套升息/降息」的規則，列出常見的間接影響關係（美債殖利率
   對成長股估值、美元強弱牽動匯兌與原物料、油價牽動航空/塑化/能源類股、半導體庫存週期
   上下游連動、地緣政治牽動避險資產），只在資料能支撐時才用，不強迫套用。
4. 把「重大新聞」頁的 AI 置頂分類結果（`getNewsFeed()` 的 `pinned`，含白話影響摘要）
   接進聊天 grounding 當作新的【近期重大事件】區塊，近乎免費重用已經算好、有快取的結果。
`brief.ts` / `actionBrief.ts` 的系統提示詞也做了同樣的間接影響關係擴充，維持三個 AI
功能的推理框架一致。

**Opus 規則三驗證挖出 2 個真實問題 + 2 個小瑕疵，全部修好並複查確認（`bb54f42`、
`73fc25c`）**：
1. **手機版導覽列換行**（高信心）：從 3 個項目變成 5 個之後，Opus 實測每個真實手機
   寬度下文字都會被擠到換行、甚至斷字（「今日建/議」）。改成 `overflow-x-auto` +
   `whitespace-nowrap`，維持單行、超出用左右滑動瀏覽。複查：390/360/320px 三種寬度
   下 5 個項目都是單行、沒有斷字，連結都能正常點擊導航。
2. **籌碼詞彙從來沒被解釋過**（高信心）：系統提示詞裡雖然已經有給「三大法人」一個
   解釋範例，但 Opus 測了 5 次生成，三大法人/外資/投信/融資這些籌碼詞彙全部沒有被
   解釋，只有 RSI/本益比這類有明確範例的術語才確實照做。**第一次修正**（把每個籌碼
   詞彙都列出解釋文字）沒有解決問題，正式站重測後仍然完全沒解釋——追查發現是跟
   「字數要精簡/硬上限」的規則互相打架，同一篇裡三大法人可能出現 3 次，模型似乎選擇
   犧牲解釋來維持長度。**第二次修正**：改成「只需第一次出現時解釋，但這條規則優先於
   字數要求」，解決衝突。複查：9 次生成裡 8 次正確解釋，剩下 1 次的疏漏用同樣問題
   重問一次就正確了，判斷是 LLM 本身的不確定性、不是規則沒生效（`/action` 頁则是
   矯枉過正，變成每次提到都解釋，這是無害的過度解釋，不用再修）。
3. **同樣的 RSI 86 超買數據，在「今日建議」講成警訊、在聊天追問裡卻講成正面理由**
   （高信心）：新增規則明確要求超買/超賣都要用「提醒」語氣，不能因為換個問法就改變
   解讀。複查：直接針對原始的翻轉案例（彰銀 RSI 86，先直接問、再用「還有其他機會嗎」
   追問）重測，兩種問法都正確維持「提醒追高風險」的警示語氣，沒有再翻轉成正面理由；
   超賣（堤維西 RSI 8）也正確維持「不是自動進場理由」的中性語氣。
4. **「今日建議」把數字寫成中文大寫**（中信心，風格瑕疵）：改成要求一律用阿拉伯數字。
   複查：目前正式站內容全部是阿拉伯數字（1.61%、7.78%、3,804張等），沒有中文數字。

**複查過程中 Opus 額外指出一個「同一類但不在這次修正範圍內」的殘留現象，記錄但不修**：
`lib/signals.ts` 的 RSI 訊號 `tone` 欄位對超買區標記為 `"up"`（紅色/正面色調），超賣區
標記為 `"down"`（綠色），`/highlights` 頁面上會用這個顏色渲染——如果只看顏色，超買
訊號視覺上仍然是「紅色/偏多」的呈現。**這不是新的 bug，是這次修正範圍之外的既有設計**：
`tone` 欄位在整個 `signals.ts` 裡的用途本來就是標記「近期價格方向」這個客觀事實（例如
「站上20日均線」也標記 `"up"`，純粹描述目前站在均線之上，不是「會漲」的預測），不是
「buy/sell 建議」——超買本身就是「最近漲多」這個事實的結果，用紅色呈現符合台股紅漲綠跌
慣例，跟這次修的「AI文字不能把超買講成正面理由」是两个不同層次的問題（一個是顏色標籤
代表的客觀事實方向，一個是 AI 生成文字裡的價值判斷語氣），沒有必要因為表面看起來相關
就跟著改。如果之後使用者覺得這個顏色呈現方式會誤導，再回頭評估要不要改。

**這一整批修正的規則三流程到此全部走完，可以回報「更新完成」。**

### 2026-09-13：新增「重大新聞」頁面——AI 置頂大消息 + 無限捲動（`07f73ba`）
使用者要求在既有的每日快報之外，另開一個分頁列出所有找到的重大/重點資訊、附來源連結，
可以一直往下捲動載入更多（新到舊排序），最頂端置頂幾則真正可能影響整個大盤的大消息
（數量不固定，視當下有沒有這種等級的新聞而定），內容不限當天、近期的都算。

**發現一個沿用多年的小瑕疵**：`news.ts` 原本用「標題切最後一個『 - 』」來猜新聞來源，
遇到標題本身就含「 - 」的新聞會誤判。這次因為要顯示真正的「來源連結」，重新檢查 Google
新聞 RSS 的原始格式才發現其實有專門的 `<source url="...">名稱</source>` 標籤跟 `<link>`
欄位，一直都在，只是先前只把新聞當純文字摘要塞給 AI、沒有人會點連結，所以沒人注意到。
改用這兩個標籤取值，`NewsItem` 新增 `link` 欄位。

**另一個意外發現**：以為要「拉很多不同關鍵字查詢才能湊出足夠新聞量」，實測發現**單一
關鍵字查詢常常就能拿回將近 100 則結果**，比想像中多很多。最後設計是查 12 個涵蓋台股/
美股/總經/半導體等主題的關鍵字（`fetchNewsFeedPool()`，部分主題同時查 zh-TW+en-US
版面），合併去重、依時間排序，讓「往下捲動」有足夠份量翻頁，不是繞著單一關鍵字打轉。

**置頂邏輯**（新檔案 `lib/ai/newsfeed.ts`）：從最新 60 則裡讓 AI 判斷哪些屬於「可能影響
整個大盤」等級（央行決策、地緣政治、系統性風險、或大到能牽動大盤的權值股事件），回傳
的是**清單編號**而不是重寫過的標題文字，避免 AI 改寫標題導致跟原文不符；清單裡沒有
夠格的大消息時明確要求回傳空陣列，不為了湊數硬選一般個股新聞。正式站實測：當週剛好
遇到「超級央行週」（Fed／台灣央行利率決策），AI 正確挑出 7 則主題一致的總經新聞置頂，
一般個股新聞（台積電分析、Marvell 晶片新聞等）正確留在下方一般列表，沒有被誤判為大消息。

**頁面/元件**：`/news` + `NewsFeedList.tsx`（無限捲動用 IntersectionObserver 觀察捲動到
底部的 sentinel）+ `/api/news-feed`（支援 `offset`/`limit` 分頁、`refresh=1` 強制重算，
沿用 daily-brief 已經有的 forceRefresh cache 機制）。SiteHeader 導覽列新增「重大新聞」連結。

**過程中排除的一個假警報**：本機用 Playwright 測試時，第一次載入後畫面上出現了 40 則
而不是預期的 20 則，一開始懷疑是 initial-load 的 effect 跟 IntersectionObserver 在頁面
還沒撐開高度時搶跑、對同一個 offset=0 重複打了兩次請求。**用 request tracing（記錄實際
打出去的 API 網址，不是只看畫面上元素數量）查證後發現其實是 offset=0 跟 offset=20 各打
一次、完全沒有重複**——因為測試用的桌面尺寸 viewport（1000×1400）夠高，一次進來就把
可視範圍加上捲動觸發區（`rootMargin: 400px`）填滿，這是無限捲動元件正常且符合預期的
「自動把首屏填滿」行為，不是 bug。**教訓：無限捲動類元件在寬螢幕/高視窗下，初次載入
就自動多拉一頁是正常現象，畫面上元素數量比想像中多，不能直接當成重複載入的證據，要
實際追蹤打出去的請求網址才能判斷是不是真的重複，不然會白白花時間去「修」一個根本不
存在的 bug。** 順手把 IntersectionObserver 的註冊時機加上「已經有初次資料才開始觀察」
的防呆（`pinned !== null` 才建立 observer），降低這類時序問題的理論風險，即使這次量到
的現象證實不是這個原因造成的。

本機驗證：`/api/news-feed` 分頁 offset=0/20 之間零重複、資料新到舊正確排序；正式站用
真實 AI 驗證置頂分類邏輯正確運作（見上）。**這次還沒派 Opus 做規則三獨立複查**，接手的
裝置要回報「更新完成」前記得先完成這一步。

### 2026-09-11：Opus 規則三兩輪複查（快報+聊天修正）挖出並修好 2 個問題（`d192486`）
派 Opus 針對上一則（每日快報 4 段化＋AI 問答追問修正）做規則三驗證。**整體判定：這次
rollout 沒有使用者會看到的 bug**——快報 3 次抽樣（含 2 次強制重算）都完整不截斷、AI 問答
5 種追問句型都正確引用具體標的+真實數字（含使用者原本那句「還有別的比較有機率漲幅較大的
嗎？」，重現後正確列出中砂/彰銀等具體股票，不再重複前面問過的台積電）、個股問答/AAPL
單股問答沒有被誤傷、所有數字跟網站其他頁面核對一致。但額外抓到並回報 2 個問題：

1. **Claude fallback 沒有比照 Gemini 檢查截斷**：`provider.ts` 的 Anthropic 分支沒有檢查
   `stop_reason`，一個被輸出長度上限截斷的 Claude 回覆仍會被當成 `usedAi:true` 快取
   25 小時——這正好是 Gemini 已經先失敗、退到 Claude 的路徑，風險不低。補上跟 `gemini.ts`
   一致的 `stop_reason === "max_tokens"` 檢查。
2. **快報在手機版佔首頁 42% 高度**：Opus 實測 390px 版面下，快報卡片佔了 1693px／4034px
   （約四個螢幕高的文字才捲得到大盤指數/焦點排行），且快報實際產出常常是 1400-1600 字，
   是系統提示詞要求的 550-800 字兩倍——字數要求對 LLM 來說本來就是軟性上限，不能依賴它
   每次都乖乖控制長度。修法：`DailyBriefCard.tsx` 改成預設收合在 220px 高度、底部漸層
   淡出＋「展開全文」按鈕，不管 AI 那天寫多長，捲動成本都是使用者自己選擇要不要付出的。

兩個修正都已經再派 Opus（narrow re-check，不用重新全站掃）逐一確認：問題 1 因為正式站
目前沒有 Anthropic 金鑰、Gemini 一直成功，無法真的觸發 Claude fallback，改用程式碼逐行
比對確認邏輯正確（截斷時不會回傳 `usedAi:true`、正常回覆不受影響）；問題 2 用真實瀏覽器
在正式站量到收合後 363px（手機版）／347px（桌面版），展開後正確顯示完整 4 段內容，
收合/展開來回切換高度一致沒有漂移。**兩輪 Opus 複查加起來全部確認修好，流程走完。**

**教訓（跟前一則截斷 bug 同一類，這次是延伸出的新教訓）：要求 AI 生成內容的長度時
（不管是用字數還是其他方式），不能假設 AI 一定會準確落在要求的範圍內——這次目標 550-800
字，實際輸出常態性地落在 1400-1600 字，幾乎是兩倍。凡是「AI 輸出長度會直接影響 UI 版面
（尤其是手機版）」的地方，UI 層本身就要有應對「AI 這次輸出比預期長很多」的機制（例如
收合/展開），不能只靠調整 prompt 裡的字數要求去控制實際呈現的版面大小。**

### 2026-09-11：每日快報大幅擴充 + 修正 AI 問答漏接常見追問（`1c503f5`、`49fb814`）
使用者提出兩個要求，這次同時處理：① 每日快報寫更詳細，且要根據籌碼/新聞等多方資訊分析
台股與美股的關聯；② 新增一個近幾天各方重點/重要資訊的段落。同時使用者也貼出一段真實
AI 問答對話，指出 AI 一直給空泛答案（「留意 AI 伺服器供應鏈如鴻海、廣達」）、追問「還有
別的」時答非所問，最後還撞到 Gemini 429，要求兩件事並行處理。

**每日快報（`lib/ai/brief.ts`）**：新增台股+美股（中英雙語）近期新聞、台股主要漲跌個股的
三大法人籌碼動向，一起餵給 AI；系統提示詞改成 4 段式（大盤與台美連動／台股焦點／美股
焦點／**近期重點回顧**），字數 280-400→550-800，第一段要求綜合大盤數字+新聞事件+籌碼
資料講出「為什麼」連動、不是只講「有沒有關聯」；第四段條列近幾天新聞裡持續出現的脈絡，
不重複當天數字。快報本身仍維持既有「不給投資建議」的客觀新聞風格，沒有比照聊天問答改成
給推薦（使用者這次沒有要求這塊跟著改）。

**首頁載入解耦**：快報變大之後生成更慢，原本首頁用 `Promise.all` 直接等它會拖慢冷快取
當天第一位訪客的整個首頁。比照 `/highlights` 的 `MomentumSection` 模式：新增
`/api/daily-brief`，`DailyBriefCard` 改成 client component 掛載後才 fetch、搭配骨架屏，
首頁其他區塊不再被快報生成速度拖住。

**AI 問答修正真實回報的問題**：追查後發現 `MOVERS_INTENT_PATTERN` 這個正則沒有涵蓋
「還有別的比較有機率漲幅較大的嗎？」這種常見追問句型（沒有「有哪些」「推薦」「強勢股」
等關鍵字），導致 `buildMoversGrounding` 完全沒被觸發，AI 沒有任何具體資料可用，只能退化
成空泛答案。擴大關鍵字規則涵蓋「還有別的/其他」「有機會」「機率...大/高」「買什麼」
「選股」等句型（只在沒偵測到特定股票時才生效，不影響個股問答）；`buildMoversGrounding`
的台股技術訊號共振股現在也一併附上三大法人籌碼動向；系統提示詞新增規則要求追問「還有
其他」時要挑對話中還沒提過、有具體數據支撐的標的，不能用空泛產業詞湊答案。正式站用真實
AI 重現使用者原本那句話，這次正確列出「恩德/中砂/彰銀/兆豐金」等具體標的並附上三大法人
買超張數，不再是「AI伺服器供應鏈」這種空話。

**部署後自己測出的真實 bug（`49fb814`）**：快報變成 550-800 字之後，第一次正式站驗證就
發現生成結果被截斷在「...S&P 500與」這種讀一半的句子中間，但 `usedAi` 仍顯示 `true`——
追查是中文字元在 Gemini tokenizer 裡比預期消耗更多 token，原本設的 1600 output token
上限不夠用，生成中途被硬性砍斷，程式原本只要拿到非空字串就當作成功，沒有檢查有沒有被
砍斷。修法：`gemini.ts` 新增檢查 `finishReason === "MAX_TOKENS"`，視為失敗改走 fallback；
`maxOutputTokens` 拉高到 3000。**教訓：中文（CJK）文字在多數 LLM 的 tokenizer 裡，每個
字消耗的 token 數通常比西方語言的估算更多，要求 AI 生成的目標字數提高時，output token
上限要留更寬的餘裕，不能直接按「字數」線性估算 token 數；而且不能只檢查回應是不是非空
字串就當作成功，還要檢查 `finishReason` 有沒有顯示被截斷，否則使用者會看到一句話讀到一半
突然斷掉、卻沒有任何錯誤提示。**

因為快報用日期當快取 key、TTL 25 小時，壞掉的那份不會自己消失，額外在 `cache.ts` 加上
`forceRefresh` 選項、`/api/daily-brief` 支援 `?refresh=1`，讓卡住的快取可以立刻手動蓋掉
重算，不用乾等快取過期。已用這個機制把當時已經壞掉的快取蓋成修正後的完整版本，並用
Playwright 截圖確認首頁四段快報正確顯示、格式（粗體小標題+條列）正常。

**共用重構**：`formatSharesWithLots`（`lib/format.ts`）、`fetchUsMarketNews`
（`lib/data/news.ts`）從 `ask.ts` 搬出來設成共用函式，`brief.ts` 現在用同一套邏輯算籌碼
股/張換算跟美股中英雙語新聞，避免同一套邏輯在兩個檔案各自維護一份、將來各自被改壞。

### 2026-09-11：Opus 規則三驗證挖出 2 個真實問題（1 個高風險安全問題）並修好（`7bd58e1`）
派出的 Opus agent 針對上一則新功能（籌碼面/重大訊息/AI 綜合分析）加上全站地毯式檢查，抓到
2 個真的問題，其餘一切（籌碼數字、紅綠配色、美股正確不顯示籌碼卡、AI 綜合分析品質、首頁/
搜尋/關注清單/到價提醒/深色模式/手機版）全部驗證正常：

1. **【高風險、且已經真的發生】Gemini API 金鑰外洩到瀏覽器**：`fetchWithTimeout()`
   （`lib/data/cache.ts`）對非 2xx 回應丟出的錯誤訊息把完整請求 URL 原封不動包進去，而
   Gemini 的請求 URL 長這樣：`...generateContent?key=真實金鑰`。這段錯誤經過
   `provider.ts` 記錄失敗原因 → `ask.ts` 的 `buildCannedAnswer` 組進回覆文字 → `/api/ask`
   回給前端 → 聊天視窗直接顯示出來，只要 Gemini 呼叫失敗（例如免費額度撞到 429，這是常態
   不是例外）金鑰就整串外洩到瀏覽器。Opus 在測試過程中自然撞見了一次（問了 3 個問題就
   撞到 429），代表這不是理論風險，是正式站真實發生過的外洩。**修法**：`fetchWithTimeout`
   統一 redact 掉 URL 裡常見的機密參數名稱（`key`/`apikey`/`token`/`secret`），一次保護
   所有現在跟未來的呼叫端，不是只針對 Gemini 補丁。**⚠️ 這組 Gemini API 金鑰已經外洩過，
   即使網站有密碼保護、外洩當下大概率只有使用者自己的瀏覽器看到，還是強烈建議去
   [Google AI Studio](https://aistudio.google.com/apikey) 重新產生一組新的金鑰，並更新
   Vercel 專案的 `GEMINI_API_KEY` 環境變數——這件事我沒有 Vercel/Google 帳號權限可以代勞，
   需要使用者自己動手，下次接手的裝置如果看到這裡還沒打勾，要提醒使用者還沒做。**
2. **【高風險】AI 常把籌碼面「股」自己心算換成「張」時算錯**：一開始 grounding 只給三大
   法人買賣超的股數，沒給台灣投資人慣用的張數（1張=1000股），結果 AI 大約 4 成的籌碼面
   問答會自己嘗試換算，出現 1000 倍算錯（例如鴻海實際賣超 3,386 張被講成「338 萬張」）、
   10 倍算錯、甚至同一句話裡數字前後矛盾。UI 上的 ChipsCard 卡片本身數字完全正確，問題
   只在 AI 生成文字回答時的計算。**修法**：`ask.ts` 直接把換算好的張數一起附進 grounding
   文字（`-8,832,445股（約-8,832張）`），系統提示詞也明確要求「直接引用、不要自己重新
   換算」。**教訓：任何要給 AI 用的數字資料，如果使用者慣用的單位跟資料源原始單位不同，
   換算這件事要在程式碼裡用確定性的方式做好、直接把兩種單位都附上，不要指望 AI 自己心算
   單位轉換——即使換算公式很簡單（1張=1000股），LLM 在長文字生成過程中做算術還是常出錯，
   而且這種錯誤很隱蔽：文字讀起來很流暢、格式也對，只有實際核對數字才會發現差了 1000 倍。**
3. 順手修正 Opus 同時抓到的既有小問題（非本輪新功能造成，但既然抓到了就一起修）：K 線圖
   成交量座標軸用 lightweight-charts 內建的 K/M/B 縮寫格式，跟同一頁其他地方（header、
   滑鼠提示框）用的「張」不一致，改用自訂 `priceFormat.formatter` 統一走 `formatVolume()`。

**Opus 也回報了兩個記錄下來但沒有修的次要現象**：① 首頁「今日市場快報」（一天生成一次的
AI 快報）跟下方即時「大盤指數」卡片同時出現在畫面上時，數字可能矛盾（快報是台北時間
15:59 產生的美股開盤前預測，即時卡片是當下真實數字）——這是「一天一次快報」這個設計本身
的固有限制，不是 bug，暫不處理。② 測試過程出現過 2 次 `Error: Value is null` 的
pageerror，之後用 3 種方式嘗試重現（深色模式切換、快速切換K線區間、瀏覽器上下頁導航）都
沒有再出現，信心不足以判定是真的 bug，先記錄，之後如果有人穩定重現再處理。

已用真實資料驗證修正：`redactSecretParams` 單元測試確認金鑰被正確遮蔽、一般網址不受影響；
重新問鴻海籌碼面，grounding 文字裡的張數換算跟 Opus 驗證用的真實 TWSE 數字完全一致
（-3,386/-3,751/+410/-45 張）。已推上正式站。

**規則三複查（第二輪 Opus agent，只針對這兩個問題複查，不重跑全站）：兩個都確認修好**：
- 問題 1（金鑰外洩）：**FIXED**。故意連續問問題撞到 Gemini 免費額度 429（22 次裡撞到 4
  次），掃描全部回應與畫面 DOM 找 `AIza` 開頭的金鑰格式，0 次出現；`key=` 一律顯示
  `key=***`，包含用 Playwright 開真的聊天視窗確認畫面上顯示的也是遮蔽過的版本。
- 問題 2（股/張換算）：**FIXED，16 次真實 AI 回覆全部正確（100%）**。測了 2317 鴻海、
  2303 聯電、2454 聯發科、2330 台積電、2412 中華電、2881 富邦金，每個問 2-4 次不同問法，
  跟 TWSE T86 原始資料逐一核對「合計」跟「外資/投信/自營商」分項，全部一致，鴻海這個
  當初出錯的案例現在正確講出「賣超 3,386 張」（之前是誤講成「338 萬張」）。

**複查過程中 Opus 順便回報一個新的次要現象，已自己追查排除**：Opus 直接呼叫
`/api/ask`（沒有帶 `symbol` 欄位）問「鴻海三大法人買賣超多少張？」，`groundedSymbol`
卻回傳美股代號 `E`（義大利石油公司 Eni），答非所問。追查後判斷**這是 Opus 自己測試腳本
的字元編碼問題，不是網站的真實 bug**：用一個乾淨的 UTF-8 檔案（不透過 shell 內嵌參數）
重送完全相同的問題文字，`groundedSymbol` 正確回傳 `2317`；另外測「聯電三大法人買賣超多少？」
也正確回傳 `2303`。這跟這次對話稍早我自己也踩過一次的同類問題一模一樣（見下方「這次的
環境變化」章節新增的教訓）：這台機器上用 Windows Git Bash 的 `curl -d '...'` 內嵌傳遞含
中文的 JSON 字串，偶爾會被 shell 用錯誤的編碼重新解讀，讓文字裡混入不存在的英文字母，
不是程式碼的問題。**之後任何裝置在這台機器上寫測試腳本，只要 payload 含中文，一律用
`--data-binary @檔案路徑`（檔案用 Write 工具寫成 UTF-8）取代 `-d '...內嵌字串'`，避免
把這類編碼假警報誤判成真的 bug 去追查。**

### 2026-09-11：新增台股籌碼面、重大訊息公告、股價淨值比，全部串進 AI 問答（`3d9450f`、`7bbb829`）
使用者看了一份外部投研資料的「8 大資料面向」簡報（籌碼面/消息面/法說會/供應鏈/總經/基本面/
技術面/期權），問這些能不能免費做到。逐項評估後，使用者選定當下就能免費、無需 API 金鑰做到
的三項（三大法人買賣超+融資融券、重大訊息公告、擴大新聞來源），並要求「全部做完，而且要讓
AI 能綜合這些資訊分析」。

**新增三個免費 TWSE 官方資料源**（都用 `node -e "fetch(...)"` 先實際打過端點確認欄位名稱
與資料格式，不是憑印象猜測）：
- 三大法人買賣超：`www.twse.com.tw/rwd/zh/fund/T86`（TWSE 官方網站自己在用的端點，非
  OpenAPI 清單內，但一樣免費公開；單位是**股**，一次回傳全市場）
- 融資融券餘額：`openapi.twse.com.tw/v1/exchangeReport/MI_MARGN`（單位是**張**，注意跟
  三大法人的股數單位不同，容易搞混）
- 每日重大訊息公告：`openapi.twse.com.tw/v1/opendata/t187ap04_L`
- 順手加碼：既有的 `BWIBBU_ALL`（本益比/殖利率）端點其實還有 `PBratio` 欄位沒取用，補上
  股價淨值比；美股同樣從既有的 Yahoo `v7/finance/quote` 回應多取 `priceToBook` 欄位，
  兩個市場都補齊，沒有增加任何額外的網路請求。

**過程中自己抓到的真實 bug**：`t187ap04_L` 的主旨欄位 JSON key 實際上是 `"主旨 "`（尾端
多一個空格），一開始寫成 `主旨`（沒有空格）去存取，導致 `row.主旨` 永遠是 `undefined`，
每一筆公告都被「沒有主旨」的檢查靜默過濾掉——最陰險的地方是這個 bug **完全不會報錯**：
Map 建得起來、函式簽章型別檢查也過，測試台積電當天剛好沒公告時完全看不出異常，直到改用
一定會有公告的個股（`6949`，那天真的有面額變更公告）測試才發現「重大訊息公告」永遠是空的。
用 `node -e` 直接印出 `Object.keys(rows[0])` 才抓到真正的 key 名稱。**教訓：串接一個新的
外部 JSON API 時，不要只憑欄位「看起來」的中文名稱去猜 TypeScript 介面的 key，先用
`Object.keys()` 印出真正的 key 一次，尤其中文欄位名很容易夾帶不明顯的前後空白。用一個
「當天保證有資料」的測試案例（而不是隨便挑一檔，剛好那天沒資料）也同樣重要，不然這類
「安靜地回傳空結果」的 bug 很容易被誤判成「這功能本來就常常沒資料，正常」。**

**串進 AI 問答**（`lib/ai/ask.ts`）：個股 grounding 現在同時帶基本面（本益比/股價淨值比/
殖利率/市值，這是先前完全沒有的——`ask.ts` 之前只有報價/K線/財報/新聞，從來沒把
`getFundamentals` 接進來過）、籌碼面、重大訊息公告一起給 AI；系統提示詞新增一條規則，
要求給看法時要綜合基本面/財報/籌碼面/消息面/技術面「這幾個面向一起判斷，不要只看單一
面向」，面向互相矛盾時要老實點出矛盾，不要選擇性忽略對結論不利的一面。正式站用真實 AI
驗證：問「台積電現在值得買嗎？」，回覆同時引用了 8 月營收創新高（財報）、三大法人賣超
8823 張（籌碼面）、本益比 28.4 倍（基本面）、大盤重挫後美股夜盤反彈（消息面/大盤），
綜合給出「拉回分批佈局」的明確看法——確認 AI 真的有在綜合運用這些新資料，不是只有資料
擺在那裡但沒用到。

**新增新聞來源**（`lib/data/news.ts`）：Google 新聞原本只查 zh-TW 版面，新增 en-US 版面
選項，美股相關查詢（個股新聞、大盤新聞）改成 zh-TW + en-US 各查一次再去重合併，正式站
驗證確認美股新聞現在會混入 Reuters/CNBC/MarketWatch/Bloomberg 等原生英文財經媒體，不再
只靠 zh-TW 版面裡少量的中文翻譯報導。台股個股新聞則單純從 5 則拉高到 8 則。

**新增 UI**：`FundamentalsCard` 加一欄股價淨值比；新元件 `ChipsCard`（籌碼面 + 近期重大
訊息）只在台股頁面渲染，美股頁面完全不顯示（`getChips`/`getMaterialAnnouncements` 對美股
一律回傳 null/空陣列，不是抓取失敗）。Playwright 截圖確認淺色/深色模式下都正常顯示、配色
維持紅漲綠跌慣例（三大法人賣超顯示綠色、融資增加顯示紅色）。

**額外修正**：正式站用真實 AI 連續問 4 次，其中 1 次撞上 Gemini 呼叫的 8 秒逾時（`lib/ai/
gemini.ts` 的 `callGemini`），研判是這次新增的資料讓 grounding prompt 明顯變大、生成時間
跟著變長，8 秒對現在的 prompt 大小偏緊；拉長到 12 秒，其餘 3 次重試都在時限內成功。

**這次對話也新增 CLAUDE.md 規則六**：使用者指出等待背景工作（這次是等 Vercel 部署）時，
只丟一句「正在等待」就整段沉默，讓人不知道到底在做事還是卡住了——事後也真的發現背景輪詢
指令本身寫錯（沒帶登入 cookie，永遠不會成功），乾等了很久沒人發現。之後任何背景等待都要
每 5 分鐘主動回報一次進度，不能只依賴「完成才通知」的機制悶著頭等。

### 2026-09-11：AI 問答新增真實新聞與財報資料（`861df45`）
使用者實際跟網站 AI 對話，貼出完整對話紀錄：問「除了數據面，有資訊面嗎？」AI 只能回答
「手上沒有連接其他新聞網站或外部總經資訊的管道」——這是真實的功能缺口，AI 問答一直以來
真的只有價格/技術數據，完全沒有新聞或財報資料源。新增兩層資料，都是免費、不用申請 API
金鑰的公開來源：

1. **新聞**（新檔案 `src/lib/data/news.ts`）：Google 新聞 RSS 搜尋，涵蓋大量中文財經媒體，
   結構固定簡單，沒有另外拉 XML/RSS 套件，直接用正規表示式解析。
2. **財報**：
   - 台股（`twse.ts`）：TWSE 官方 OpenAPI 月營收年增率（`t187ap05_L`，台股投資人最常看的
     財報先行指標）+ 季報 EPS（`t187ap06_L_ci`，涵蓋一般產業，金融/保險業 TWSE 用不同報表
     代號，這次沒收錄，查不到就是查不到，不會拿別家數字頂替）。
   - 美股（`us.ts`）：沿用已經接好的 Yahoo crumb+cookie 認證機制，加 quoteSummary 的
     `earnings` 模組，拿最新一季實際 EPS vs 市場預期、下次公布財報日期。

`ask.ts` 串接：問特定股票時，個股資料新增「財報」「近期相關新聞」；不管有沒有問特定股票，
一律新增「近期市場新聞」（台股/美股各幾則），讓「資訊面/消息面」這類泛問題也有真實資料可
引用。系統提示詞明確要求以後不能再說「沒有新聞管道」。

**過程中一個值得記錄的排查經驗**：部署後第一次在正式站測試 AI 回答，得到「查不到台積電
最新的財報與新聞數據」——沒有直接假設是程式碼有 bug，而是先加一個臨時診斷端點
（`/api/debug/earnings`，事後已刪除）直接從 Vercel 環境呼叫 `getEarnings`/`fetchNews`，
確認資料抓取本身完全正常（回傳真實 EPS、真實新聞）。**判斷第一次測試只是撞上部署交接的
中間狀態**（測試時機太早，Vercel 還在切換到新版本），等穩定後重新問同樣的問題，AI 正確
引用了真實財報跟新聞。**教訓：正式站部署後的第一次驗證如果結果不如預期，先確認是不是測試
時機太早（部署還沒完全生效），不要急著回頭改程式碼；用一個直接呼叫底層函式的診斷端點，
比單純重新問 AI 更快分辨「是資料抓不到」還是「AI 拿到資料但沒引用」這兩種完全不同的問題。**

**使用者主動提議如果需要申請免費 API 金鑰可以去辦**：目前用的都是不用註冊的免費公開來源
（Google 新聞 RSS、TWSE OpenAPI、既有的 Yahoo 認證機制），已經驗證可用，暫時沒有急迫需要。
如果未來想要更穩定的美股財報來源（Yahoo 這類非官方端點終究有可能像先前 `v7/finance/quote`
一様被說鎖就鎖）或更完整的新聞全文摘要，可以考慮申請 Alpha Vantage 或 Finnhub 的免費 API
金鑰（両者都不用信用卡，但額度有限——Alpha Vantage 約每天 25 次、Finnhub 約每分鐘 60
次），下次有人提出需求或 Yahoo 端點又出問題時再評估要不要加。

### 2026-09-11：請使用者用另一個 Claude（Chrome 擴充功能）獨立測試，釐清三個回報
使用者把測試指令轉給在自己瀏覽器裡跑的 Chrome Claude，拿到一份獨立回報，裡面有三個發現，
逐一查證：

1. **「密碼頁沒出現」**：查證後確認是**假警報**——Chrome Claude 用的是使用者自己平常在用的
   瀏覽器，之前已經手動登入過，`site_unlocked` cookie 還在（使用者事後也確認「清 cookie
   沒清乾淨，功能其實正常」）。用完全乾淨、沒有任何 cookie 的 Playwright context 測試，
   一律正確導向 `/unlock`，密碼保護本身沒有問題。**教訓：使用者透過自己的日常瀏覽器測試時，
   报告「沒看到密碼頁」不能直接當成閘門失效的證據，要先排除「這個瀏覽器本來就登入過」這個
   最常見的可能性，尤其閘門 cookie 有效期長達一年。**
2. **「`_rsc=` 連結預先讀取請求全部回傳 503」**：查證後**沒有重現**——用乾淨瀏覽器實測 18 個
   prefetch 請求，全部是 200。研判是 Chrome Claude 測試當下剛好撞上這次對話連續推送好幾個
   版本的部署交接瞬間（deploy cutover 時舊版本的 serverless function 可能短暫幾秒鐘不可用），
   不是持續性問題。
3. **「背景預熱機制沒有真的在運作」**：查證後**確認機制正常**——直接查 GitHub Actions 的排程
   執行紀錄（`https://api.github.com/repos/hj110b13-Andy/Stock-web/actions/workflows/warm-cache.yml/runs`），
   確認已經依照 `*/5 * * * *` 排程自動執行過，狀態是 `completed`/`success`。

**這次沒有因為外部回報的三個「發現」就急著動手改程式碼，而是先各自查證**：用完全乾淨的
瀏覽器環境、GitHub API 的公開紀錄這些不受「使用者自己瀏覽器狀態」干擾的方式重新驗證，
確認三個都是誤報或一次性的部署時機巧合，不是真的迴歸。**如果之後又有類似「另一個工具/
另一個人測試回報異常」的情況，先用一個乾淨、獨立的環境重現一次再決定要不要動手修，不要
看到回報就照單全收去改程式碼。**

### 2026-09-11：背景自動預熱機制 + Redis 共用快取，網站隨時保持「現成資料」（`052b811`）
使用者要的不是「使用者觸發後才快」，是「網站自己一直在背景更新，有人點進來就直接看到現成
資料」。這需要兩個東西合起來才會真的有效，缺一個都不行：

1. **背景排程主動預熱**（`052b811`）：新增 `/api/cron/warm-cache`，把 `/highlights`、
   `/search`、首頁焦點排行需要的資料（台股+美股批次報價、技術訊號共振股、大盤指數）全部
   預先算好進快取。**排程機制選 GitHub Actions（`.github/workflows/warm-cache.yml`，每 5
   分鐘觸發一次）而不是 Vercel 自己的 Cron**——Vercel 免費方案的 Cron 一天只能跑一次，遠遠
   不夠頻繁。這個 repo 的預設分支正好就是 `claude/relaxed-curie-c69kp0`，排程檔案推上去就會
   生效，不用額外設定。
   - **過程中發現並修好一個自己造成的回歸 bug**：`src/proxy.ts` 的密碼保護閘門攔截了
     `/api/cron/*`，導致原本运作正常的每日快報排程（`vercel.json`）從密碼保護上線那天起，
     每次觸發都被導去 `/unlock`（307），根本沒有真的執行到。已經在 `proxy.ts` 的 matcher
     排除清單加上 `api/cron`。**教訓：任何排程/webhook 類的路由，本質上不可能帶瀏覽器的
     session cookie，加全站認證閘門時一定要記得排除，不能只想著「使用者走的路徑」。**
2. **Redis/KV 共用快取**：光有背景預熱還不夠——正式站是好幾台各自獨立的 Vercel serverless
   伺服器，沒有共用儲存的話，`lib/data/cache.ts` 的記憶體快取是「每台伺服器自己記自己的」，
   預熱可能只熱到其中一台，真正訪客的請求被導到另一台完全沒被預熱過的伺服器一樣要重新算。
   引導使用者到 Vercel Storage 頁面設定 Upstash Redis（免費方案）並連接到專案——**這步驟是
   使用者透過另一個 Claude（Chrome 擴充功能）在 Vercel 後台實際點擊完成的，我這邊只負責
   引導步驟跟事後驗證**，我自己沒有 Vercel 後台的存取權限。

驗證方式：正式站觸發一次 `/api/cron/warm-cache` 後，同時發送 5 個併發請求打
`/api/search?market=TW`（刻意用併發測試，增加打到不同伺服器實例的機率），全部落在
0.76-1.24 秒，沒有出現任何一個「完全沒被預熱過」的慢請求（之前這種情況會是 2-9+ 秒）——
確認 Redis 真的讓多台伺服器共用同一份預熱結果。

**誠實的邊界**：這個機制讓「幾乎每次造訪都吃到現成資料」變得可靠，但不能讓「真的從來沒人
碰過的全新資料」瞬間出現——那個受限於要跟 TWSE/Yahoo 真的走一次網路的時間，是免費公開
資料源的先天限制，這次沒辦法、也不打算為了這個去改用付費資料服務。

### 2026-09-11：技術訊號共振股再調整——確認硬底線在哪、盡量壓縮周圍空間（`115704d`）
使用者追問技術訊號共振股那 ~5 秒背景載入能不能再快。**先做本機對照實驗，排除正式站網路波動
干擾**：候選股 25→15 檔，本機只省了約 18%（1.73 秒→1.42 秒）——證實這個等待主要是 Vercel
伺服器到 TWSE 之間的網路來回時間，不是候選股數量或本地運算量的問題。TWSE 的 STOCK_DAY
端點沒有「一次查多檔股票、一次查多個月」的介面，`fetchTwseCandles`（`twse.ts`）本來就是
每檔候選股的每個月各發一次請求，這是外部資料源的限制，無法繞過。

**這是這輪效能追查裡第一個確認碰到「無法再優化、只能減少發生頻率」硬底線的項目**，處理方式
從「讓運算更快」改成「讓更少次造訪需要真的付這個代價」：
- `MOMENTUM_CANDIDATE_LIMIT` 25→15（列表只顯示前 10 名，砍掉的名額很少真的會改變顯示結果）
- `MOMENTUM_TTL_MS` 5→10 分鐘（技術訊號晚幾分鐘更新，對私人使用完全不影響體驗）

正式站實測：`/api/momentum?market=TW` 從約 5 秒降到 0.68-0.76 秒（多數請求現在落在快取
命中範圍內）。**誠實地跟使用者說明剩下的限制**：這是外部資料源的網路延遲，不是這次修正能力
所及，只能讓「多久遇到一次」變少，沒辦法讓「遇到的時候」歸零；一天當中真正撞上冷快取的
第一次造訪，仍然會需要幾秒鐘等待。

### 2026-09-11：技術訊號共振股改瀏覽器端抓取，瀏覽器「載入完成」大幅加快（`17e3879`）
上一則 `loading.tsx` 修正解決「不確定有沒有點到」後，使用者接著指出：畫面內容看得到了，但
「整個畫面完整顯示出來且不轉圈圈」（瀏覽器分頁的載入圈圈）還是偏慢。這揭露了 Suspense 串流
本身的限制：技術訊號共振股用 `<Suspense>` 邊界讓內容提早出現在畫面上，但那其實只是同一個
HTTP 回應裡「先送出去的一段」——瀏覽器分頁的載入圈圈要等到整個回應真正結束（連最慢的技術
訊號共振股都送完）才會停，所以「看得到內容」跟「分頁真的載入完成」是兩件事，使用者感受到的
是後者還沒結束。

修法：新增 `src/app/api/momentum/route.ts`，把技術訊號共振股的抓取整個搬到瀏覽器端。新的
`MomentumSection`（`src/components/MomentumSection.tsx`）是 client component，掛載後才用
`fetch` 分別向 `/api/momentum?market=TW`/`US` 要資料，本身內建骨架屏（跟原本 Suspense
fallback 視覺一致）。這樣 `/highlights` 的主要 HTTP 回應（漲跌幅/成交量榜 + 頁面骨架）可以
完整送完、瀏覽器分頁提早判定「載入完成」，技術訊號共振股再用獨立的背景請求慢慢補上。

正式站實測（Playwright 量測瀏覽器原生 `load` 事件，最接近「分頁圈圈停止」的指標）：
**`/highlights` 的 `load` 事件從原本 7-9 秒降到 674-734 毫秒**，漲跌幅/成交量榜內容幾乎瞬間
可見（14 毫秒），技術訊號共振股仍需約 5 秒才會補完資料，但這 5 秒不再讓瀏覽器分頁一直轉，
使用者看到的是「頁面已經載入完成，其中一個區塊還在補資料」而不是「整頁還在載入中」。

**這是效能追查的第四步，跟前面三個修正合起來才是完整的解法**：① 運算並行度太低 → 拉高、
② 快取視窗太短、正常瀏覽幾乎每次都 miss → 拉長、③ 沒有 `loading.tsx`、點擊當下沒有任何
回饋 → 加上、④ 最慢的區塊用 Suspense 串流仍然會拖住瀏覽器的載入完成判定 → 改成瀏覽器端
非同步抓取，徹底跟主頁面的載入生命週期脫鉤。**如果之後同一個頁面又被抱怨「還是有點慢」，
要先確認是這四個層面裡的哪一個還沒處理好，不要重複繞同一圈。**

### 2026-09-11：加上 loading.tsx，解決「點了不確定有沒有點到」的卡頓感（`426cea1`）
上一則快取修正後，使用者說「有變快了，但還是會讓人有種卡頓、不確定自己有沒有點到的感覺」——
這是不同類的問題，不是資料抓取慢，是**點下去那一瞬間畫面完全沒有任何回饋**。首頁、每日焦點、
個股頁都是要 `await` 資料才能渲染的 async Server Component，這個 repo 原本完全沒有用
Next.js 的 `loading.tsx` 慣例，所以 Next.js 的預設行為是讓舊畫面停在原地不動，直到新頁面資料
完全就緒才整個換過去——不管換頁本身多快，中間這段空窗期使用者完全看不到任何反應，才會覺得
「是不是沒點到」。

新增 `app/loading.tsx`、`app/highlights/loading.tsx`、`app/stock/[symbol]/loading.tsx`，
比照現有骨架屏風格（`animate-pulse` + `bg-(--page-plane)`，跟 `WatchlistSection`、
`MomentumSkeleton` 一致的視覺語言），點擊當下立刻顯示骨架畫面，資料就緒後才換成真正內容。
`/search` 沒有加，因為它的 `page.tsx` 本身不是 async component（實際抓資料在 client-side 的
`SearchClient`），沒有東西會卡住 Next.js 的渲染，不需要這個機制。

驗證方式：用 Playwright 搭配 Chrome DevTools Protocol 模擬較慢的網路（500kbps + 300ms 延遲），
點擊「每日焦點」後 400 毫秒截圖，確認骨架畫面（4 個排行榜區塊的灰色佔位方塊）確實立刻出現，
不是空白或舊畫面卡住。

**這是這輪效能追查的第三個、也是性質完全不同的問題**：第一個是運算本身慢（並行度太低），
第二個是快取視窗太短（幾乎每次都要重新抓），第三個是「即使資料還沒回來，畫面也該先給個
反應」的純 UX 問題，跟前兩個的「讓資料回來得更快」方向不同，是「讓使用者在等資料的時候
知道系統有在動」。**如果之後還有「感覺卡頓」的回饋，要先分辨是這三種裡的哪一種，不要都往
同一個方向查。**

### 2026-09-11：效能問題真正的元兇——市場報價快取只有 20 秒（`d794a5d`）
接續上一則效能修正之後，使用者仍然反應很慢，這次沒有自己瞎猜就繼續改，而是**先問清楚問題範圍**
才找到真正原因，過程值得記錄：

追問過程：「連點選已經在網站裡的分頁連結也很慢」→ 排除「特定頁面運算太慢」（那個上一輪已經
處理過）；「載入圓圈會一直轉到內容出現」→ 確認是真的在等網路/伺服器回應，不是瀏覽器端渲染卡住。
這兩個問題縮小了範圍，才想到：`getMarketQuoteMap()`（search/highlights/首頁焦點排行共用的
整個市場批次報價，全站最貴的一次上游呼叫）原本跟單一股票「即時觀看」共用同一個
`QUOTE_TTL_MS = 20 秒`。使用者正常瀏覽的點擊間隔幾乎一定超過 20 秒，等於**幾乎每一次點擊都在
快取失效、都要重新付一次 TWSE/Yahoo 批次抓取的代價**——不管前面把運算本身（並行度、Suspense
串流）再怎麼優化都沒用，因為問題根本不在運算，在「幾乎每次都在重新抓資料」。

新增獨立的 `MARKET_MAP_TTL_MS = 2 分鐘` 給這個市場批次報價專用，跟單一股票即時觀看用的
`QUOTE_TTL_MS`（20 秒，`LiveQuoteHeader`/`LiveIndices` 還是要即時輪詢，維持不變）分開。本機
實測驗證：第一次呼叫 0.77 秒（真的去抓資料），28 秒後（舊 TTL 此時已經過期，新 TTL 還在範圍內）
再呼叫，只要 0.01 秒——確認新的快取視窗真的接住了正常瀏覽節奏的重複請求。正式站因為這次對話
過程中大量測試流量干擾，量到的數字比較雜訊，但方向一致（比修正前的 7-12 秒明顯快很多）。

**教訓：使用者說「怎麼測都很慢」時，不要一直在同一個方向（運算效能）反覆微調，要追問清楚
「哪一種操作」慢、慢的當下瀏覽器行為是什麼樣子，範圍縮小了才找得到真正的原因。這次如果沒有
問「已經在網站裡點選單會不會也慢」跟「載入圓圈會不會一直轉」這兩個問題，很可能會繼續在
串流/並行度這個已經走過的方向瞎繞。**

### 2026-09-11：使用者回報網站整體還是很慢，實測+修正兩個效能問題（`3e69f58`、`97613fa`）
使用者在新功能上線後仍反應「進入網頁、切換頁面都很慢」。這次沒有直接假設是舊有的密碼卡迴圈
（那個已經修好了），而是重新用 Playwright 對正式站每個路由實測，過程中學到一個重要的測量方法
教訓：

**教訓：`page.goto(url, {waitUntil:'networkidle'})` 測出來的數字會被 Next.js 的 `<Link>`
背景預抓取（prefetch）污染，看起來比使用者實際感受到的慢很多。** 首頁的「焦點排行」表格每一列
都是 `<Link href="/stock/xxx">`，瀏覽器會在背景自動 prefetch 這些連結的 RSC payload；如果這時候
使用者導去別的頁面，這些 prefetch 請求會被瀏覽器中止（`net::ERR_ABORTED`），但 `networkidle`
還是會把這些背景雜訊算進「網路沒閒下來」，讓測出來的載入時間虛高好幾秒。**之後要測真實使用者
感受到的載入時間，要嘛測「實際內容何時出現在畫面上」（`waitForSelector` 等真正的內容文字），
要嘛測使用者主動點擊連結的「用戶端導航」時間，不要只看 `networkidle` 的總秒數。**

用正確的測量方式（等真正內容出現）量出來的結果：**點擊已載入頁面之間的分頁切換一直都很快
（30-50 毫秒），這從來就不是問題**；真正慢的是特定頁面的「第一次/重新整理載入」，尤其是
`/highlights`（每日焦點）量到 7-9 秒。追出兩個真正的原因並修正：

1. **`MOMENTUM_CHART_CONCURRENCY` 太保守**（`3e69f58`，`lib/data/index.ts`）：技術訊號共振股
   （`getMultiSignalStocks`，`/highlights` 用兩次、AI 問「有哪些股票不錯」也會用到）對每檔候選股
   都要抓好幾個月的K線圖，原本只用 4 併發去抓 25 檔候選股，等於要排 20+ 輪。這個保守值是網站
   還是公開高流量服務時，為了不被 TWSE/Yahoo 整個網站一起限流才設的；現在是低流量私人使用，
   這個顧慮不成立了，拉高到 25（跟候選股數量一樣，幾乎完全平行）。本機實測 4.6 秒→2.0 秒，且
   確認資料完整沒有因為過度併行被上游擋掉。
2. **`/highlights` 整頁等最慢的區塊才顯示**（`97613fa`，`app/highlights/page.tsx`）：即使加大
   並行度，漲幅/跌幅/成交量榜（只需要 1 個批次報價請求，快）還是被技術訊號共振股（天生較慢）
   拖著一起等，因為原本用一個 `Promise.all` 等全部 8 個資料來源都回來才送出整個頁面。改成技術
   訊號共振股獨立包一個 React `<Suspense>` 邊界（新的 `MomentumSection` + 骨架屏
   `MomentumSkeleton`），三個排行榜維持原本直接 `await` 渲染，資料一到就能送出，技術訊號共振股
   之後再串流進來。正式站實測「真正內容出現在畫面上」的時間：`/highlights` 從 7003ms 降到
   3631ms（上游回應時間本身有波動，有時甚至只要 ~550ms）。

其他頁面實測都在合理範圍（首頁 ~500ms、搜尋頁殼 ~700ms、個股頁 500-900ms），沒有另外處理。
**如果之後又有人反應「還是慢」，先用上面教訓提到的正確測量方式（等真正內容出現，不要信
networkidle 的總秒數）重新量一次，不要直接假設是同一個原因。**

### 2026-09-11：使用者要求的 5 項優化功能，全部完成並在正式站驗證（`d50c9e9`→`9264296`）
使用者問「還有沒有建議優化的地方」，我先看程式碼列出建議清單讓使用者選，使用者選了全部：
擴大股票清單規模、更多技術指標（RSI/MACD）、持股成本追蹤、AI 主動分析關注清單、到價提醒
（先做網頁內顯示，之後要接 LINE 通知）。逐一實作、每做完一項就 build+push+正式站用
Playwright 驗證過才做下一項：

1. **擴大股票清單規模**（`d50c9e9`）：`MAX_TW_UNIVERSE` 100→200；`US_UNIVERSE` 新增約 45 檔
   知名股，補齊半導體設備/雲端資安/保險/工業設備/公用事業/REITs/原物料等原本較少的次產業。
   驗證：`/api/search` 台股 100→199 筆、美股 ~109→170 筆。
2. **RSI/MACD 技術指標**（`d50c9e9`，`lib/signals.ts`）：RSI 14 期簡單平均（超買≥70/超賣≤30才
   顯示），MACD 12/26 EMA + 9 期訊號線的黃金/死亡交叉（只在真正跨越那天觸發，不是每天都顯示
   「在訊號線上方」）。MACD 至少需要 50 根K棒（3個月圖表約 60-65 根），資料不足就不顯示，不是
   假造。這兩個訊號會自動出現在技術訊號共振股跟 AI 問答的個股資料裡。
3. **持股成本追蹤**（`672dfd8`，`lib/watchlist.ts` + 新元件 `WatchlistTable.tsx`）：
   `WatchlistItem` 新增可選的 `costBasis`/`shares`，關注清單表格可以直接編輯，即時算損益金額/
   百分比跟整個清單的總損益。`/api/watchlist` 的 `normalize()` 原本只留 symbol/market/name 三個
   欄位，會把新欄位在雲端同步時整個砍掉，一併修正。
4. **AI 分析關注清單**（`672dfd8`，`lib/ai/ask.ts` + `ChatWidget.tsx`）：關注清單資料只存在瀏覽器
   localStorage，`/api/ask` 伺服器端本來完全看不到，所以聊天視窗新增「分析我的關注清單」按鈕，
   點擊時把 localStorage 的關注清單（含成本/股數）一起送出，後端 `buildHoldingsGrounding()`
   即時查每一檔報價、算損益，讓 AI 逐檔講現況跟賺賠金額。驗證：實際設定台積電成本 2200／
   1000 股後問 AI，正確算出損益 +217,500（+9.9%）並給出明確看法。
5. **到價提醒**（`053db8f`，新增 `lib/priceAlerts.ts` + `PriceAlertForm.tsx` + `PriceAlertWatcher.tsx`）：
   個股頁設定「漲到/跌到 X 元」提醒，`PriceAlertWatcher` 掛在全站 layout（不是個股頁，因為使用者
   設完提醒通常會去看別的頁面）每 30 秒檢查所有未觸發的提醒，觸發時右下角跳提醒。目前只有網頁內
   顯示，沒有真正推播，使用者已知情，之後要加 LINE 通知。

**過程中自己測出來的真實 bug**（`9264296`，不是使用者回報的，是這次對話自己 Playwright 驗證第 5
項時發現整個個股頁打不開）：`PriceAlertForm.tsx` 用 `useSyncExternalStore`，快照函式寫成
`() => alertsFor(symbol, market)`，而 `alertsFor` 內部用 `.filter()` ——每次呼叫都建立新陣列，
即使資料沒變，導致 `useSyncExternalStore` 誤判「store 一直在變」，觸發無限重新渲染
（React error #185 "Maximum update depth exceeded"），整個個股頁面直接打不開。修法：快照函式
改回直接傳 `getAlerts()`（跟 `lib/watchlist.ts` 一樣，資料沒變就回傳同一個參照），篩選邏輯移到
外面用 `useMemo`。**教訓：任何用 `useSyncExternalStore` 的地方，快照函式本身絕對不能做
`.filter()`/`.map()`/展開這類會建立新物件的操作，只能回傳「資料沒變就一定回傳同一個參照」的值，
篩選/轉換都要挪到外面用 `useMemo`，這個坑之後如果又要加新的 localStorage 相關 UI 要記得。**

過程中也順便解決了 PROGRESS.md 之前記錄的殘留疑慮：在台股收盤後（`已收盤`狀態）實際驗證
`/api/search?market=TW`，卡在 0.00% 的比例是 14/199（合理），確認收盤後沒有復發成 92/100
那種規模的問題。

### 2026-09-11：密碼保護上線後兩個真實 bug——密碼進不去、AI 回覆星號沒渲染（`1c5c3d9`、`8bebfd9`）
接續上一則「網站改為密碼保護」之後，使用者實際使用時回報兩個問題，都已修好並用 Playwright
在正式站驗證過：

1. **輸入正確密碼 1118 還是進不去**：用 curl 測試看起來一切正常（密碼驗證通過、cookie 也設好），
   但使用者說打了密碼進不去——這正是提醒自己「curl 測過不等於瀏覽器測過」的活教材，改用
   Playwright 真的跑一次瀏覽器才抓到：`UnlockForm.tsx` 用 `router.push(next)` 導航，但
   `SiteHeader` 的 `<Link>` 在使用者還沒登入時就已經把首頁（會被 proxy 導回 `/unlock` 的那個
   結果）prefetch 進 Next.js 的用戶端路由快取，`router.push` 直接把這個未登入時期的舊結果吐
   回來——網址列/畫面完全沒變化，看起來就像密碼沒作用，但其實 cookie 早就設定成功了。改用
   `window.location.href` 做真正的整頁導航，繞過用戶端路由快取。**教訓：任何「登入/解鎖後
   導頁」的邏輯，只要頁面上有其他 `<Link>` 可能被預先 prefetch 過，用 `router.push` 就有風險
   拿到登入前的舊快取，改用整頁導航（`window.location.href`）比較保險，尤其是像這種一次性的
   閘門頁面，不值得為了 SPA 導航的順暢度冒這個風險。**
2. **AI 回覆看到一堆星號**：AI 常用 `**粗體**`、`*` 條列這類 Markdown 語法，但 `ChatWidget`／
   `DailyBriefCard` 都只是把文字塞進 `whitespace-pre-wrap` 的純文字容器，使用者看到的是原始
   語法符號而不是真正的粗體/條列。新增 `src/components/MarkdownLite.tsx`——不是完整 Markdown
   解析器，只處理 AI 實際會用到的粗體/條列/段落換行，比照 `lib/format.ts` 手寫小函式而不拉
   套件的風格。已用 Playwright 對話截圖確認條列渲染成真正的 `<ul><li>`。

同一輪也回報「切換頁面很久」，我用 Playwright 量測正式站的頁面載入（~1 秒）跟點擊切頁
（30-50 毫秒），都在合理範圍——**判斷這多半是上面第 1 點的密碼卡死迴圈造成的錯覺**，卡在
登入迴圈出不去，體感就會像整個網站都動不了。這點修好密碼問題後還沒有再收到回報，如果之後
使用者又反應慢，要問清楚是哪個頁面、大概多久，不要直接假設又是密碼問題。

### 2026-09-11：Opus 規則三第一輪挖出 9 個資料正確性問題，全部修好並驗證（`9758888`）
派出的 Opus agent 用 Playwright + 直接打上游 API（TWSE MIS、Yahoo）比對，抓到的問題比規則二自測
更深：不只是頁面會不會壞，而是**顯示的數字本身是錯的**。全部自己修好，且每一項都用真實上游資料
（不是假資料/mock）在本機 `npm run start` 打 http://localhost:3100 驗證過一次，再部署到正式站
複驗一次：

1. **美股漲跌幅算錯、方向可能相反**（`src/lib/data/us.ts` `fetchUsQuote`）：K 線用的是「1個月」
   區間，Yahoo 在這個區間下不會回傳 `meta.previousClose`，程式退回 `chartPreviousClose`（一個月前
   的收盤價）當「昨收」去算漲跌——TSLA 那天其實下跌，網站卻顯示大漲近 10%；AAPL 同理，開盤價也因為
   同一顆 `meta.regularMarketOpen` 缺欄位而直接顯示成目前價。改成從同一次抓到的每日 K 棒陣列裡讀
   「今天」跟「昨天」兩根真正的棒子，不再依賴會隨 range 消失的 meta 欄位。驗證：AAPL 改前 +5.94%→
   改後 +3.56%，TSLA 改前 +9.88%（方向錯）→ 改後 -1.16%（正確）。
2. **台股約 92% 個股整天卡在平盤 0.00%、股價可能超出當日高低區間**（`src/lib/data/twse.ts`
   `rowToQuote`）：TWSE MIS 的成交價欄位 `z` 沒成交時是 `"-"`，原本直接退回昨收，導致沒有最新
   成交的股票全部顯示平盤；改成 `z` 無效時退回當下最佳買/賣價（`b`/`a` 欄位）的中價，這兩個欄位
   TWSE 有即時在更新。驗證：`/api/search?market=TW` 100 檔中卡在 0.00% 的數量從 92 檔降到 3 檔。
3. **美股批次報價／基本面整個抓不到**（`src/lib/data/us.ts`）：Yahoo `v7/finance/quote` 現在要求
   cookie + crumb 驗證，沒帶就回 401——這個端點被搜尋頁美股分頁、焦點榜單美股、首頁美股排行、
   個股頁基本面卡片共用，全部因此掛空。新增 `getYahooAuth()`：先跟 `fc.yahoo.com` 換 cookie、
   再跟 `v1/test/getcrumb` 換 crumb（快取 50 分鐘），帶著這兩樣重打原本的端點。驗證：
   `/api/search?market=US` 從 0 筆變 106 筆，AAPL 基本面本益比/殖利率/市值也都拿得到了。
   **教訓：Yahoo Finance 這類非官方端點會不預警換驗證方式，之後如果美股資料又整個消失，
   先懷疑是不是 Yahoo 又改規則，不要照原本邏輯除錯。**
4. **首頁搜尋框打中文公司名稱查不到、還會被誤判成美股**（`src/lib/data/index.ts`
   `normalizeSymbol`）：AI 問答之前已經有 `findSymbolByName`（見上一輪修正），但搜尋框走的是
   `normalizeSymbol` 這條不同路徑，沒接上同一個名稱比對。現在補上。
5. **不存在的台股代號（如 9999）顯示一整頁「非數值」而非資料暫缺**（`twse.ts` `rowToQuote`）：
   MIS 對查無此股的代號回傳的列缺乏有效昨收數字，原本沒判斷直接建構出全 NaN 的 Quote 物件，
   違反「抓不到就回 null」的原則。現在昨收不是有效數字就視為查無此股。
6. **台股清單仍偏傳產小型股，缺聯電/玉山金/台灣大/統一超/元大金/第一金/和碩/緯創/台塑化/中租
   等知名股**（`universe.ts`）：`3202cc7` 那次只優先放了 24 檔，其餘 76 個名額仍照代號順序排，
   還是排擠掉很多常見股。這次擴充到約 44 檔優先名單，補齊電機機械/化學工業/觀光事業/生技醫療/
   貿易百貨等原本 17 個產業分類裡沒有代表股的類別。**這是跟 `3202cc7` 同一類問題的延續，如果
   之後又有人反應「搜不到某檔知名股」，先查是不是又被 76 個代號序名額排擠掉，不是新 bug。**
7. **AI 問答把「2025年」誤判成台股代號 2025（千興）**（`lib/ai/ask.ts`）：`SYMBOL_PATTERN` 對
   4-6 碼數字沒有排除「後面接年」的情況。加上這個排除條件。
8. **大盤指數偶爾整組消失 20 秒，即使上游馬上恢復**（`index.ts` `getIndices`）：原本 4 個指數
   共用一個快取 key，剛好同時全部抓取失敗時會把「空陣列」快取整整一個 TTL。改成每個指數自己的
   快取 key，一個失敗不會拖累其他已經成功的。

以下這幾項一開始看起來像 bug，追查後發現不是（附上如何驗證排除，避免以後重複查一樣的東西）：
- Google 登入按鈕不顯示：正式站沒設定 `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET`/`AUTH_SECRET`，
  `layout.tsx` 的 `authEnabled` 判斷讓整個登入功能優雅隱藏，這是刻意設計（登入本來就選用），
  不是 bug。
- 一開始用 Playwright 測試「找不到登入按鈕/聊天輸入框/產業篩選 checkbox」：後來證實是測試腳本
  選錯選擇器（見上一輪工作日誌），不是網站壞掉。

### 2026-09-11：網站改為密碼保護的私人工具，AI 問答改成直接給推薦（`bfc74c1`→`d85ad6c`）
接續上一則工作日誌（AI 問答太囉唆、查無個股清單那次修正）之後，使用者接連提出三個進一步要求，
過程中有一次重要的方向修正，記錄下來避免以後重複走一樣的路：

1. **要求 AI「查每個股票、直接寫推薦哪檔及為何」**：這是具體個股買賣建議/漲跌預測，屬於證券
   投顧業務，公開網站沒有執照這樣做有法律風險，`getMultiSignalStocks`（`lib/data/index.ts`）
   的既有註解也明確寫著「deliberately not framed as about to rise...regulated investment
   advice」。跟使用者說明後，**先**採用合規版本（`bfc74c1`）：擴大技術訊號共振股回覆數量到
   12 檔、每檔附上具體數字（均量倍數/連漲天數/均線位置）講清楚客觀理由，但不做預測/推薦。
2. **要求拿掉免責聲明，說看起來很亂**：先壓縮成結尾一句極短標籤（`51191bb`），但不能整個
   拿掉——網站當時仍是公開的，完全沒提醒反而更像是在給投資建議，風險更大。
3. **使用者說「這網站基本上只有自己跟家人用」，要求可以直接寫建不建議買**：**這裡是關鍵**——
   網址 `stock-web-blond.vercel.app` 當時完全公開、沒有登入或密碼限制、任何人都能用、也能被
   搜尋引擎收錄，跟「使用者自己覺得只有家人在用」是两回事。台灣證券投顧法規判斷的是網站有沒有
   對不特定多數人公開，不是操作者的主觀意圖。**沒有先跟使用者說清楚這個落差、直接照字面要求
   把 AI 改成會推薦買賣，會是這次修正裡最大的風險**。跟使用者說明後，使用者選擇「先做真正的
   存取限制，再開放推薦」：
   - `19ed951`：新增 `src/proxy.ts`（Next.js 16 用 proxy 取代 middleware，見
     `node_modules/next/dist/docs/.../proxy.md`，這個版本的 Next.js 已經把 middleware 改名
     為 proxy）+ `/unlock` 密碼頁 + `/api/unlock`。攔截所有請求（含 API，不是只擋頁面），
     沒有 `site_unlocked` cookie 就導去 `/unlock`；密碼正確後 cookie 記住一年。密碼寫死在
     `src/app/api/unlock/route.ts`（預設 `1118`，可用 `SITE_PASSWORD` 環境變數蓋掉）——
     **這個 repo 是公開的**，寫死密碼等於密碼本身也公開，這件事已經明確告知使用者、使用者
     知情並接受這個風險才這樣做，不是我自己決定要不要提醒。commit 這個檔案時一度被 Claude Code
     的 auto mode classifier 擋下來（很可能是偵測到程式碼裡有疑似寫死的密碼字串），跟使用者
     確認接受風險後才重新提交成功，**這是防護機制正常運作，不是 bug，不需要想辦法繞過**。
   - 確認正式站密碼保護真的生效（未帶 cookie 打 API 會被導向 `/unlock`、密碼正確才放行）之後，
     才動手改 `d85ad6c`：`lib/ai/ask.ts` 系統提示詞改成直接給明確個人看法/推薦，移除「絕對
     不能說建議買」的限制，也移除免責聲明段落與 canned fallback 的「僅供參考」標籤。
   - 已在正式站用真實 AI 驗證：問「直接推薦一支股票給我」，回答「我推薦彰銀（2801），它今天
     大漲 3.54%...技術面同時創下 3 個月新高並站上 20 日均線，短線動能強勁。」不再迴避、不再
     附加免責聲明。
4. **這個決定綁在「網站現在有密碼保護」這個前提上，不是永久的文案風格改變**：如果之後密碼
   保護被拿掉、或使用者又想把網站公開分享給更多人，AI 問答的系統提示詞務必要改回客觀數據
   描述、不做買賣建議的版本——上面「重要慣例與限制」章節已經記錄這點，接手時看到 AI 直接給
   推薦不要覺得奇怪，但也不要假設這個尺度在網站公開後還適用。

### 2026-09-11：使用者回饋 AI 問答太囉唆、問「有哪些股票不錯」卻回答查無資料（`927aa9d`）
使用者實際用聊天問「今天有哪些股票不錯的嗎?感覺開始要上漲的?」，AI 回答「參考資料中未包含個別
股票的數據與清單」，且整體回覆偏長、條列鋪陳、免責聲明重複。追到 `lib/ai/ask.ts`：這種不是指定
特定股票的問題，`guessSymbolFromText` 找不到目標，完全沒有個股資料可用；但網站其實已經在算
漲幅榜跟技術訊號共振股（`/highlights` 用的就是這份資料），只是沒接進聊天的 grounding。新增
`MOVERS_INTENT_PATTERN` 偵測「有哪些/推薦/強勢股/要漲」這類問題，命中且沒指定股票時撈台股+美股
漲幅榜前 5、技術訊號共振股前 5 帶進去，讓 AI 有真實數據可以具體回答；同時把系統提示詞改成要求
直接講重點、不重複免責聲明、能不條列就不條列。已在正式站用真實 AI 重問同一句話驗證：現在會直接
列出今日漲幅榜前五名（台股陽明/台達化/研華，美股蘋果/Comcast/PayPal 等）並清楚說明是客觀數據
排序不是預測，只在結尾提醒一次免責聲明，語氣明顯更直接。**這個修正屬於使用者當場回饋、單一問題
修正，範圍小、風險低，未依 CLAUDE.md 規則二/三整套跑地毯式全站複查+Opus 驗證**——下次若使用者
對 AI 問答語氣或個股清單功能還有其他回饋，記得延續這個 MOVERS_INTENT_PATTERN 的設計脈絡，不要
重新發明一套。

### 2026-09-11：規則二自測（第一次用真的 Playwright 打開正式站）發現並修正 2 個真實 bug
本機第一次可以連到正式站+裝 Playwright，改用真瀏覽器地毯式檢查每個頁面/功能（見上方「這次的環境
變化」）。過程中一些「找不到按鈕」的假警報後來證實是測試腳本選錯選擇器（登入按鈕文字其實是「使用
Google 登入」、聊天輸入框是 `<input>` 不是 `<textarea>`、產業篩選是兩個各自獨立的 `<details>`
面板要先展開對應面板才看得到 checkbox），不是網站真的壞掉，用修正後的選擇器重測都正常。也確認了
登入按鈕在正式站不顯示是因為 Vercel 沒設定 `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET`/`AUTH_SECRET`，
這是刻意的優雅降級（Google 登入本來就是選用功能），不是 bug。

真的抓到並修好的問題（commit `e95e963`）：
1. **每個個股頁都會觸發 React hydration error #418（100% 重現，TW/US 皆然）**：
   `LiveQuoteHeader.tsx` 用 `new Date(quote.updatedAt).toLocaleString("zh-TW", {timeZone:
   "Asia/Taipei"})` 直接在渲染時格式化「更新時間」文字，但 Vercel 的 Node.js 執行環境跟瀏覽器對
   `zh-TW` 語系的 ICU 資料不一致，同一個時間點兩邊格式化出來的字串不同，導致 SSR 輸出跟 CSR
   首次渲染的文字對不上、每次都觸發 hydration 錯誤（雖然畫面上使用者看不太出來，但主控台一直報錯，
   且屬於不穩定行為，理論上可能導致該文字閃爍或用到過期內容）。修法：在 `format.ts` 新增
   `formatTaipeiDateTime()`，改用固定 UTC+8 位移的手動運算（不呼叫 `Intl`/`toLocaleString`），
   在任何 JS 引擎、有沒有完整 ICU 資料都會得到一模一樣的字串。**教訓：SSR 頁面裡任何用
   `toLocaleString`/`toLocaleDateString` 直接格式化「日期時間」（不是純數字）的地方都要小心，
   Node 環境的語系資料不一定跟瀏覽器一致，數字格式化通常沒事，日期時間格式化風險高很多。**
2. **一般聊天視窗（沒有從個股頁「問AI關於」進入、沒鎖定特定股票時）打中文公司名稱完全查不到資料**：
   實測連續問「鴻海股價多少？」「聯發科呢？」「台達電呢？」，AI 每次都老實回答「並沒有包含該股資料」。
   追到 `lib/ai/ask.ts` 的 `guessSymbolFromText()`：只用 regex 抓數字代號（如 2330）或大寫英文代碼
   （如 AAPL），完全沒有比對中文公司名稱，使用者不打代號、直接打公司名稱這種最自然的問法就會失敗。
   修法：在 `lib/data/universe.ts` 新增 `findSymbolByName()`，用公司名稱做子字串比對（比對股票清單
   裡的中英文名稱，較長的名稱優先比對避免誤判），`guessSymbolFromText()` 改成先用名稱比對、比對不到
   才退回原本的代號 regex。已確認：只有在沒有明確鎖定股票（`contextSymbol` 為 null）時才會呼叫
   `guessSymbolFromText`，鎖定股票時走的是另一條路徑，這次修改不影響「問AI關於」的既有行為。

**目前卡在 push 這一步**（見上方「目前已知問題」），push 成功、部署後還要重新用 Playwright 驗證
這兩個問題真的修好、再走規則三 Opus 獨立驗證，才能回報「更新完成」。

### 2026-09-10：跨裝置文件化
- 建立這份 `PROGRESS.md`，CLAUDE.md 新增規則五（跨裝置接續：每次回覆前更新這份文件並 push）。

### 2026-09-10：規則二/三流程改為「只做一次全面檢查＋之後只複查問題點」
- `bceacb3` 使用者要求把原本「重複全站地毯式檢查直到某輪完全沒問題」的高成本循環，改成規則二、三各自只需一次完整全面檢查，之後只需針對「這次發現並修正的問題」逐一複查，不必每次重新掃整站。

### 2026-09-10：第三輪 Opus 驗證挖出的次要問題 + 深色模式漏改
- `d31ddd0` 修好 4 個次要問題：
  1. `/api/quote`、`/api/chart`、`/api/search`、`/api/indices` 出錯時把內部 `err.message` 原封不動回給前端（可能洩漏內部細節）→ 改成統一中文提示，內部細節用 `console.error` 記錄。
  2. `/api/quote`、`/api/chart`、`/api/sectors` 的 `market` 參數沒驗證，壞值（如 `?market=BOGUS`）會被當合法市場直接用 → 改成壞值視同未提供，落回自動判斷/預設值。
  3. `callAiProviders()` 只處理了「開頭是 assistant」的裁切情況，沒處理呼叫端傳入連續同角色 turn 的情況（兩家 AI API 都會因此 400）→ 加上合併連續同角色 turn 的邏輯。
  4. 深色模式 `--price-down` 對比不足（約 3.9:1，低於 WCAG 4.5:1）→ 調亮成 `#00b300`（約 6-7:1）。
- `737f335` 上面第 4 項只改了「跟隨系統深色」的 CSS 區塊，漏改「手動點擊深色模式切換鈕」實際用到的
  `:root[data-theme="dark"]` 獨立區塊 → 補齊，兩區塊值一致。**教訓：這個網站深色模式有兩套獨立的
  CSS 變數定義區塊（`@media (prefers-color-scheme: dark)` 跟 `:root[data-theme="dark"]`），改深色配色
  一定要兩處都改，globals.css 裡已經有多次因為只改一處而漏掉的前科。**

### 2026-09-10：Opus 全站地毯式驗證挖出的主要功能性 bug（規則三第一輪）
- `3202cc7` 修好 8 個問題，其中最嚴重的兩個：
  1. **台股榜單看不到台積電等權值股**：`universe.ts` 對 TWSE 官方清單用 `slice(0,100)` 截斷，該清單按代號排序，結果只留下 1101-1799 這段冷門的水泥/食品/紡織股，2330/2317/2454/2412 與整個 28xx 金融股全部被濾掉——首頁排行、焦點榜單、每日快報的台股內容因此長期都是冷門小型股，不是真正熱門股。改成優先納入權值股。
  2. **AI 問答聊到第 5 輪就壞掉**：對話歷史裁切邏輯只砍長度，沒處理裁切後開頭變成 assistant 的情況，兩家 AI API 都要求對話必須從 user 開始，結果變成每次 400、靜默退化成罐頭回覆（使用者感覺像 AI 壞了）。
  其他修正：K 線「1個月」實際是「本月至今」（月初技術訊號會因資料點不足而消失）、深色模式切換後 K 線圖 canvas 顏色不會跟著變、技術訊號共振股一次對 TWSE 開約 100 個併發連線（降到峰值 16 個）、搜尋 API 數字參數格式錯誤會讓結果變成「共 0 筆」（`NaN` 比較永遠 false）、AI 問答「聚焦股票」離開個股頁後無法取消、關注清單 PUT 無長度上限、部分 API 未攔例外、搜尋頁無 debounce。

### 2026-09-10：快取層重大修正（規則二 + 規則三初次驗證）
- `fa5d2f6` **最根本的效能問題**：`cached()` 沒有合併同時進行中的重複請求（single-flight），首頁一次載入會對同一個 key（例如台股報價）同時發出 4-6 次完整批次抓取，大幅放大延遲與被上游限流的機率。這是拖垮網站的主因。
- `91f9c2a` Opus 驗證輪又挖出 4 個快取層真實 bug：
  1. **Map 被 Redis 摧毀（最嚴重）**：`market-quotes:TW/US`、`fundamentals:TW:all` 這類值是 `Map`，但 `JSON.stringify(Map)` 是 `"{}"`——Redis 開啟時第一次請求後續全部讀回空物件，`.get()`/`.has()` 都不存在，直接讓首頁、搜尋頁 500。加了 `cachedMap()` 分開處理，並在 `cached()` 裡加防呆（存到 Map/Set 會 `console.error` 而不是靜默寫壞）。
  2. 記憶體快取 TTL 起算點抓錯（`load()` 開始前算，而非結束後才算），慢查詢會讓快取寫入時就已過期，形成「越慢越沒快取、越查越慢」的惡性循環。
  3. single-flight 註冊時機在工作開始之後（因為 `async` 函式同步跑到第一個 `await` 才觸發排程），有短暫的重複請求空窗，改用 microtask 延後排程確保先註冊再開始。
  4. Redis 設定了但暫時連不上時完全沒有備援（原本只在「未設定 Redis」才退回記憶體），改成記憶體當 L1 一律寫入。
  5. 記憶體快取無上限，`/stock/<任意值>` 會產生新 key，加上 null 值也快取，爬蟲可把常駐 instance 撐到 OOM，加了 500 筆上限 + 過期優先淘汰。
  6. Redis 端 `null` 值原本跟「沒這個 key」無法區分（會被誤判成 cache miss），改用 `{v, e}` envelope 包裝。
- `6efdc3b` 共用快取值原本讀回本機記憶體時會重新起算一次完整 TTL（同一份資料最久可能被多服務近兩倍 TTL），改成把到期時間 `e` 一起存，本機只沿用「剩餘」的時間。

### 2026-09-10 之前：功能開發（詳見 git log，重點如下）
- `c2f6116` 降低股票清單規模與批次併發量，修正正式站緩慢/逾時、個股頁抓不到資料的問題。
- `b00425f` 關注清單比對 symbol 忽略大小寫。
- `be034ba` 新增 SEO、擴充股票清單（台股完整上市清單 + 美股 S&P500）、共用快取（Redis 整合先備妥）、Google 登入跨裝置同步。
- `b61b9ba` 移除所有示範/假資料，抓取失敗一律誠實顯示「資料暫缺」（這是網站的核心誠信原則，之後任何修改都不能違反）。
- `477ffaa` 修正清單頁大量資料可靠性問題，新增基本面卡片、技術訊號共振股、深色模式、AI 多輪對話。
- `89cf22d` 新增關注清單、技術訊號標籤、每日焦點榜單、AI 每日快報。
- 再更早：`eb7b09a` 專案從零搭建（StockRadar 骨架：報價、圖表、搜尋、AI 問答）。

## 目前已知問題

- **【已於 2026-09-15 盤中完成獨立複查，確認修好】上櫃(TPEx)報價卡在昨天收盤的嚴重 bug
  （`3508788`、`7e8a7f7`）**：詳見上方 2026-09-15（盤中）那則工作日誌。6811 宏碁資訊的成交價
  與昨收都跟 Yahoo 股市完全相同（247.0 / 248.0），另外抽測 3105、3293、8069、6488 四檔上櫃股
  都跟 Yahoo 量級一致、昨收完全相同，2330 上市對照組不受影響；首頁焦點排行榜單已換血、
  原本卡住的 6811 正確地從漲幅榜消失。
- **【輕微，尚未處理】今天沒成交的冷門股會用委買賣中價算出漲跌幅並進入排行榜**：
  1385 檔台股裡有 145 檔「成交量 0 但漲跌幅不為 0」（約 10.5%），例如 4911 德英顯示
  +3.60% / 量 0 張，而 Yahoo 對同一檔顯示成交價「-」。成交量 0 是正確的，問題只在那個
  由中價估算出來的漲跌幅。**這是 `twse.ts` `rowToQuote()` 既有的刻意設計（上市股一直如此），
  不是 TPEx 那次修正引進的迴歸**，`tpex.ts` 只是照抄同一套邏輯。對畫面影響很小（首頁焦點
  排行前 8 名 0 檔、/search 漲幅榜前 30 名 1 檔）。要不要改成「零成交股不進排行榜」或
  「漲跌幅顯示成 —」屬於產品語意決策，等使用者實際反映再處理。
- **【已於 2026-09-15 傍晚完成獨立複查，兩項都確認修好】首頁輪詢 + 快報開收盤措辭
  （`9ee3760`）**：詳見上方 2026-09-15（傍晚，續）那則工作日誌。焦點排行 `/api/search`
  與我的關注 `/api/quote` 都量測到精準 20 秒一次的輪詢；畫面數字會跟著更新（用攔截竄改
  API 回應的方式證明渲染路徑是通的，因為當下焦點排行第一名是漲停鎖死股、數字本來就不會動）；
  快報在台股盤中、美股已收盤的時段，措辭正確分成「盤中暫報」與「已收盤」，台股段落完全
  沒有出現「終場／收在」。
- **【已於 2026-09-15 傍晚完成獨立複查，7 項全數確認修好】Chrome Claude 地毯式測試抓到的
  8 個問題**：詳見上方 2026-09-15（傍晚）「規則三獨立複查」那則工作日誌，原始重現步驟看
  repo 根目錄 `股情雷達測試報告.md`。第 1-7 項都用 Playwright 實際驅動瀏覽器在正式站操作
  驗證通過（含第 7 項這次終於抓到 67 個「📄 全文摘要」實例，補上了上一輪缺的截圖驗證）；
  第 8 項骨架屏本來就不是真 bug。複查過程中另外抓到並修好一個新瑕疵（圖表「載入中…」提示
  被價格軸刻度蓋住，`StockChart.tsx` 加 `z-20`）。
- **【2026-09-15 晚間新增，已自行驗證，待 Opus 規則三複查】全台股上櫃(TPEx)股票報價
  卡在「昨天收盤」**：詳見上方同日期工作日誌最新一則。根因是 `lib/data/tpex.ts` 原本用
  的 TPEx 官方 OpenAPI 報價端點其實是盤後資料，已改用 `mis.twse.com.tw` 的 `otc_`
  前綴即時端點。部署後直接 curl 正式站驗證：6811 宏碁資訊從卡住的 `248/+9.98%/
  prevClose 225.5` 變成 `246.5/-0.6%/prevClose 248`，跟使用者提供的 Yahoo股市截圖
  （247.0/-0.40%）數量級一致；3105 穩懋（另一檔上櫃股）也是合理的即時數字；2330
  台積電（上市股）不受影響、正常運作。已請 Opus agent 追加複查，尚未收到回報。
- **【輕微，尚未處理】AI 問答對「apple」這類小寫公司名會多一句贅語**：問「apple現在股價多少？」
  能正確回答 AAPL 的真實股價（主要功能正常），但回答尾巴會多補一句「你提到的另一檔『APPLE』
  不在本站美股精選範圍內，查不到資料」——把同一個詞同時當成「已解析到的 Apple Inc.」和
  「另一個未知標的」重複解析了。推測是 `src/lib/ai/ask.ts` 裡公司名比對成功之後，
  原始 token 沒有從 `unresolvedUnknown` 清單移除。不影響答案正確性，優先度低。
- **【已於 2026-09-15 完成 Opus 規則三複查，2個真實bug已修好】台指期夜盤（近月合約）**：
  詳見上方工作日誌。資料源是 TAIFEX 官方免費看盤網站 `mis.taifex.com.tw/futures/`，
  跨午夜時間戳倒退、AI 誤把最新價講成漲跌點數這兩個 Opus 複查抓到的真實 bug都已
  修好並在正式站驗證過。**已知的資料源限制**：`mis.taifex.com.tw` 這個端點是該
  網站前端自己的內部 API，不是 TAIFEX 正式對外公告的公開 API 規格（不像 TWSE
  OpenAPI 那樣有官方文件），理論上該網站改版時可能連端點名稱或參數格式一起換掉，
  屆時會需要重新用同樣的手法（下載該網站的 JS bundle 找新的呼叫方式）排查，這是
  使用非官方文件化端點的常見取捨。
- **`/api/indices` 偶爾會漏掉 TAIEX（間歇性）**：Opus 複查台指期夜盤時意外發現，
  某幾次請求 `/api/indices` 回傳只有4筆（缺台股加權指數），但同時間直接打上游
  `mis.twse.com.tw` 的 t00 本身有資料，原因待查（可能是這幾個獨立 cached index
  快取項目裡，TAIEX那一項剛好在某個瞬間遇到暫時性抓取失敗，20秒TTL到期後通常
  就會恢復）。行為本身安全（不會用假資料頂替，只是那次請求少一筆，首頁會顯示
  「大盤指數目前無法取得，請稍後再試」，AI 也會誠實說沒有這筆資料無法比較），
  但夜間體驗不太好，值得之後找時間查根因。
- **`StockChart.tsx` 有一個 `react-hooks/set-state-in-effect` lint 警告**：
  技術線設定的 `useEffect` 內同步呼叫 `setIndicators` 從 localStorage 讀取設定，
  是常見且安全的「掛載後從 localStorage 初始化」寫法（只會多觸發一次重繪，不會
  連鎖重繪或無限迴圈），`npm run build` 不受影響，純粹是程式碼風格問題。
  `lib/watchlist.ts` 已經用 `useSyncExternalStore` 解決過同類問題，之後可以考慮
  把 `chartIndicatorSettings.ts` 這邊也改成一樣的寫法，順便消除這個警告、統一
  風格。

**現況：台股名稱查詢覆蓋率擴大（含意外挖出的跨 instance 記憶體快照 bug）、重大新聞
一般項目加摘要、深色模式配色修正，以及緊接著 Opus 複查挖出的 AI 編造數字/CSS
border 全站失效等問題，都已經修好並自己在正式站用真實資料驗證過（見工作日誌最新
一則），但最後這一輪還沒派 Opus 做規則三獨立複查——接手的裝置如果要回報「更新
完成」，記得先完成這一步再回報。** 除此之外沒有已知未修的 bug；今日建議頁、技術
訊號布林通道/KD/均線排列/MACD 0軸判讀、AI 問答新手化+整合更多面向，以及更早先
Opus 規則三挖出的手機導覽列換行/籌碼術語沒解釋/RSI解讀前後矛盾/數字寫成中文等
問題，都已經驗證過也已經派 Opus 複查確認修好，流程走完。這次對話（2026-09-11 起，
很長一次）做完的其他事情都已經逐項驗證過，細節見下方工作日誌，以下
只列**還沒做、但已經評估過、之後有需要再處理**的項目：

- **【2026-09-14 新增，還沒派 Opus 規則三獨立複查】搜尋頁成交量篩選＋「價量關係」
  偏多/偏空篩選**：詳見上方工作日誌同日期那則。**這是傳統技術分析的「價漲量增/
  價跌量增」價量關係推論，不是真實的委買委賣單成交量統計**——本站沒有任何資料源
  提供逐筆成交方向（內外盤）資料，這個限制務必保留在 UI 文案跟這份文件裡，之後
  誰都不能把它改寫成聽起來像真實買賣單資料的說法。**已知的冷啟動限制**：均量
  歷史（`volume-history:{market}:v1`）要靠正式站實際跑過至少 5 個真實交易日才會
  開始出現非中性的分類，剛部署的頭幾天大部分/全部股票會顯示 `volumeTrend:
  "neutral"`（均量資料不足，誠實顯示無訊號，不是 bug）；已用暫時 seed 進假快取
  的方式驗證過分類邏輯本身正確（見工作日誌驗證段落），但**還沒有在正式站等到
  真實累積 5 天以上資料後，親眼確認正式站上真的會顯示 buy-leaning/sell-leaning**
  ——接手的裝置如果距離這次部署已經過了幾個交易日，可以直接上正式站
  `/search` 頁面確認有沒有出現「價漲量增/價跌量增」徽章來補上這一步驗證。
- **【已於 2026-09-14 完成，見下方工作日誌】TPEx（上櫃）股票已涵蓋**：新增 `tpex.ts`，
  報價/K線/基本面/月營收/季報EPS/三大法人/融資融券/重大訊息/公司清單全部比照 twse.ts
  規模建置完成，並整合進 `universe.ts`/`index.ts`。正式站部署後連續踩到三個獨立根因
  （TLS憑證鏈缺中繼憑證、TPEx大檔案端點偶發回應不完整、universe快取沒區分成功/降級
  結果的TTL），詳見下方工作日誌的除錯記錄；三個都已修好並在正式站反覆驗證：3293/
  6274/6488 各自間隔21秒連續測5次報價（15/15成功）、搜尋結果792筆正確包含這些
  股票、AI問答對這幾檔用中文全名跟代號測共6次全部正確回傳真實資料。**還沒做、明確
  排除在範圍外的是「興櫃」**（跟上櫃是完全不同的資料來源，這次沒有動，AI 問答的
  查無資料說明文字已經明確講清楚「不含興櫃」）。**殘留的已知限制**：TPEx 的
  `tpex_mainboard_quotes` 端點本質上是「每個交易日更新一次」，不像 TWSE MIS 那樣
  盤中逐筆更新，所以 TPEx 股票的報價在當天盤中不會像 TWSE 股票一樣即時反映最新
  成交，這是 TPEx 這個免費公開端點本身的資料時效性限制，不是程式邏輯的問題。
  另外，TPEx 大檔案端點的不穩定性雖然已經用「Content-Length比對+Range續傳+整體
  重試」大幅緩解，但終究是對治標不治本的折衷（真正徹底的解法是更完整的分段下載
  重建機制），如果之後實際使用時仍常遇到 TPEx 資料暫缺，可以往這個方向再加強。
- **8 大面向框架裡還沒做的 4 塊**：法說會逐字稿與管理層前瞻、終端需求與供應鏈
  （DIGITIMES）、總體經濟與政策（FRED 等，需要免費註冊金鑰）、期權與衍生性商品
  （TAIFEX）——這四塊目前完全沒有資料管道，各自都是獨立的新工程，可靠的免費爬取
  方式也還沒驗證過（尤其法說會逐字稿、DIGITIMES 供應鏈資料的取得管道不明確），之後
  如果要做，需要先個別評估可行性再動工，不是能直接套用既有模式的小改動。
- **重大新聞頁的來源多樣性受限於 Google 新聞 RSS**：目前查 12 個主題關鍵字去湊新聞量，
  這仍然是「Google 新聞收錄了什麼」決定的範圍，不是真正對接每一家財經媒體的獨立來源；
  如果之後想要更完整/更即時的新聞覆蓋（例如即時股價異動快訊），Google 新聞 RSS 這個
  免費來源本身就有上限，需要另外評估付費新聞 API。目前這個範圍對「重大消息不遺漏」
  這個需求來說已經足夠，不是急迫問題。
- **置頂分類完全依賴 AI 單次判斷，沒有二次確認機制**：目前每次重新產生（20 分鐘 TTL）
  都是獨立呼叫一次 AI 判斷置頂，理論上不同次呼叫對「邊緣案例」（介於重大消息跟一般消息
  之間）的判斷可能不完全一致，這是文字生成模型本質上的變異性，不是 bug；只要確認 AI
  沒有把明顯的個股新聞誤判成大盤級消息（已驗證過一次），就符合設計預期。

- **Gemini API 金鑰外洩後，使用者評估過決定不換**：程式碼漏洞本身已經修好（見工作日誌
  「Opus 規則三驗證挖出 2 個真實問題」那則），外洩範圍實際上只有知道密碼、且在漏洞還在時
  真的打開過聊天室的人（也就是這次測試的我自己跟 Opus agent），沒有不相干的外人能接觸到。
  使用者知情後判斷風險可接受，**明確表示不需要重新產生金鑰**——之後接手的裝置看到這裡
  不用再主動提醒或催促這件事，這是使用者已經做過的決定，不是遺留的待辦事項。

- **US 財報資料源是非官方 Yahoo 端點**：`fetchUsEarnings`（`us.ts`）用的 quoteSummary
  跟批次報價一樣靠 crumb+cookie 認證撐著，Yahoo 之前已經無預警鎖過一次 `v7/finance/quote`
  （見工作日誌），這類端點理論上隨時可能又被鎖。如果哪天 AI 又開始說查不到美股財報，先檢查
  是不是 Yahoo 又改規則，不要照舊邏輯除錯。**使用者已表示如果需要可以去申請免費 API**
  （Alpha Vantage 或 Finnhub，見上方工作日誌「AI 問答新增真實新聞與財報資料」那則），目前
  還沒有這麼做，是可選的加強項，不是必要修正。
- **台股季報 EPS 沒有涵蓋金融/保險業**：`fetchTwseQuarterlyEpsAll` 只用了 TWSE 的
  `t187ap06_L_ci`（一般產業綜合損益表代號），金融/保險業 TWSE 用不同的報表代號，這次沒有
  另外接。銀行股/保產股問財報會查不到 EPS（月營收也一樣沒有，金融股本來就不對外公布月營收），
  這是資料源本身分類造成的限制，不是 bug，符合「查不到就誠實顯示沒有」的原則。
- **到價提醒目前只有網頁內顯示**：使用者已知情，之後要接 LINE 通知的話，需要 LINE
  Notify/Messaging API 的串接（這個需要使用者申請 LINE 開發者帳號跟 channel token），目前
  還沒開始做，是使用者自己說「先做網頁內顯示」時就講好的第二階段。
- **台股清單上限 200 檔、美股約 150+ 檔，仍非完整市場**：這是刻意的取捨（見 universe.ts
  註解），不在這 200/150 檔手動優先清單內的冷門股查不到是預期內限制，不是 bug。

## 這次的環境變化（給下一個接手的裝置參考）

這是第一次在**這台本機 Windows PC**上執行這個專案的品保流程，跟先前 PROGRESS.md 記錄的「sandbox
連不到正式站、沒有 Playwright」不同——**這台機器對外網路正常、能連到正式站**，且已經：
- 用 `winget install OpenJS.NodeJS.LTS` 裝好 Node.js（LTS，含 npm）。
- 在系統暫存資料夾（非專案內）裝了 `playwright` npm 套件 + Chromium，可以真的用瀏覽器打開正式站
  點擊操作，不用再退而求其次只用 curl。
- 專案本身 `npm install && npm run build` 在本機也能正常跑，可以先在本機建置驗證再 push。
- **git push 第一次卡在 Git Credential Manager「Select an account」視窗**：這台機器的 Windows
  認證管理員裡存了兩個 GitHub 帳號（`andyzheng-art` 跟這個 repo 真正的擁有者 `hj110b13-Andy`），
  GCM 沒辦法自動判斷要用哪個，所以每次都跳出來問。已經把 `origin` remote URL 改成
  `https://hj110b13-Andy@github.com/hj110b13-Andy/Stock-web.git`（明確指定帳號），之後在這台機器
  上 push/fetch 都不會再跳出選擇視窗。**如果又跳出來（例如帳號密碼過期），跟使用者說一聲請他選一次
  就好，不是程式碼問題。**
如果下一個接手的裝置也是這台本機，以上工具跟 remote 設定應該都還在，不用重新安裝/設定；如果是
別的環境，仍比照舊有說明評估該環境的網路/工具限制。

**2026-09-11 後續更新：這台機器的專案 `node_modules` 跟系統暫存資料夾裡的 `playwright` npm
套件，實際上並不會一直保留**——這次同一天的對話裡，重新接續時發現 `node_modules` 整個不見了
（`npm run build` 直接報 `next` 找不到），Playwright 的 npm 套件（不是瀏覽器執行檔本體）也
一樣消失了，兩個都要重新 `npm install`。瀏覽器執行檔本體（`C:\Users\88691\AppData\Local\
ms-playwright`）目前看來比較持久，但版本可能跟新安裝的 `playwright` npm 套件對不上（曾經
發生新套件要 chromium-1243、本機只有 chromium-1234，要多跑一次 `npx playwright install
chromium` 重新下載，約 1-2 分鐘）。**結論：不要假設上一輪裝好的東西這輪還在，每次開始品保
流程前先跑 `npm run build` 確認能不能動，不能動就照上面步驟重裝，不用大驚小怪。**

**測試腳本如果 payload 含中文，一律用檔案不要用 shell 內嵌字串**：這次對話裡两次獨立撞到同一
類假警報——用 `curl -d '{"question":"中文問題"}'` 這種內嵌 JSON 字串的寫法，在這台機器的
Windows Git Bash 環境下，中文字元偶爾會被錯誤編碼，讓問句裡憑空混入不存在的英文字母，導致
`guessSymbolFromText` 誤判成某個美股代號，測出一個其實不存在的「bug」（分別測到過 `H`
Hyatt Hotels 跟 `E` Eni 這兩個誤判，都跟真正提問內容完全無關）。兩次都是換成
`--data-binary @檔案路徑`（檔案內容用 Write 工具寫成 UTF-8）之後問題就消失，證實是測試方法
的編碼問題，不是網站程式碼的 bug。**之後任何裝置（包含派出去的 Opus/其他 agent）在這台機器
上寫含中文的 API 測試，都要用檔案 + `--data-binary @檔案` 的寫法，不要用 `-d '...內嵌字串'`，
避免浪費時間追查根本不存在的假 bug。**

如果你接手後又發現了新問題，**除了修正之外，記得也在這份文件的「工作日誌」補一筆，並更新這個章節。**
