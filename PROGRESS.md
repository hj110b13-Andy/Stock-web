# PROGRESS.md — 專案現況與工作日誌

> **給接手這個專案的 Claude Code：** 這份文件是為了讓你在完全沒有先前對話紀錄的情況下，
> 也能看懂這個 repo 每個資料夾/檔案在幹嘛、目前做到哪、之前修過什麼問題。
> 每次修改完程式碼準備回覆使用者之前，**先讀這份文件掌握現況，回覆前再照 CLAUDE.md
> 的規定更新這份文件並 push**（見下方「跨裝置接續規則」）。

## 這是什麼專案

**股情雷達 StockRadar**——台股／美股股票研究網站。即時查詢報價、互動 K 線圖、篩選排行、
AI 問答。Next.js (App Router) + TypeScript + Tailwind CSS，圖表用
[lightweight-charts](https://github.com/tradingview/lightweight-charts)。

**2026-09-11 起改為私人／家人使用**（見下方工作日誌）：網站現在有進入密碼保護
（`src/proxy.ts` + `/unlock`），不再是對外公開服務；AI 問答也因此改成會直接給明確的
個人看法/推薦，不再強制客觀中立、不再附加免責聲明——**這個決定的前提是「網站已經真的做了
存取限制」，如果之後又要把網站改回公開，AI 問答的系統提示詞（`lib/ai/ask.ts`）務必要
改回客觀描述、不做買賣建議的版本，順序不能顛倒。**

- **正式站**：https://stock-web-blond.vercel.app（進入密碼預設寫在 `src/app/api/unlock/route.ts`，或看有沒有設定 `SITE_PASSWORD` 環境變數蓋掉它）
- **GitHub**：hj110b13-Andy/Stock-web
- **開發分支**：`claude/relaxed-curie-c69kp0`（所有工作都在這個分支上，push 上去 Vercel 會自動部署）
- **功能完整說明、環境變數設定、資料來源限制**：見 `README.md`，這份文件不重複列，只補「檔案功能地圖」跟「工作日誌」。

## 資料夾與檔案功能地圖

```
src/
├─ app/                              Next.js App Router 頁面與 API
│  ├─ page.tsx                       首頁：大盤指數、AI 每日快報、焦點排行、我的關注清單
│  ├─ layout.tsx                     全站 layout；deferred 深色模式偵測 inline script（避免 FOUC）
│  ├─ not-found.tsx                  404 頁
│  ├─ robots.ts / sitemap.ts         SEO：/robots.txt、/sitemap.xml
│  ├─ globals.css                    全站設計 token（CSS 變數）：淺色/深色配色、紅漲綠跌色票
│  ├─ stock/[symbol]/page.tsx        個股頁：報價 + K線 + 技術訊號 + 基本面 + AI問答 + 關注按鈕
│  ├─ search/page.tsx                搜尋/篩選頁（外殼，實際邏輯在 components/SearchClient.tsx）
│  ├─ highlights/page.tsx            每日焦點榜單：漲幅/跌幅/成交量榜 + 技術訊號共振股
│  └─ api/
│     ├─ quote/[symbol]/route.ts     GET 單檔即時報價
│     ├─ chart/[symbol]/route.ts     GET 歷史 K 線（range: 1m/3m/6m/1y）
│     ├─ search/route.ts             GET 篩選/排序股票清單（市場、產業、價格區間、漲跌幅）
│     ├─ sectors/route.ts            GET 指定市場的產業分類選單
│     ├─ indices/route.ts            GET 大盤指數
│     ├─ ask/route.ts                POST AI 問答（支援多輪對話歷史）
│     ├─ watchlist/route.ts          GET/PUT 登入後自選股跨裝置同步
│     ├─ auth/[...nextauth]/route.ts NextAuth（Google 登入）
│     ├─ cron/daily-brief/route.ts   Vercel Cron 排程觸發：預先生成當天 AI 快報
│     ├─ cron/warm-cache/route.ts    背景預熱：由 .github/workflows/warm-cache.yml 每 5 分鐘
│     │                              呼叫一次，預先算好 search/highlights/momentum/indices，
│     │                              讓真正訪客幾乎都吃到現成資料
│     └─ momentum/route.ts           GET 技術訊號共振股（給 highlights 頁的 client-side fetch用）
│
├─ components/                       UI 元件（React, 'use client' 除非明確是 server component）
│  ├─ StockChart.tsx                 K 線圖 + 成交量（lightweight-charts），會讀 CSS 變數換色
│  ├─ ChatWidget.tsx                 右下角 AI 問答浮動視窗，多輪對話
│  ├─ AskAboutButton.tsx             個股頁「問AI關於這檔股票」按鈕，會設定聊天視窗的 grounded symbol
│  ├─ MarketTabs.tsx                 台股/美股分頁切換共用元件（全站涉及雙市場的地方都用它，不並排）
│  ├─ StockTable.tsx                 股票清單表格（排行/搜尋結果共用）
│  ├─ MomentumTable.tsx              技術訊號共振股清單
│  ├─ SearchClient.tsx               搜尋頁的篩選邏輯（產業多選、價格區間、debounce）
│  ├─ SignalTags.tsx                 個股頁技術訊號標籤（爆量/創新高低/均線/連漲跌）
│  ├─ FundamentalsCard.tsx           基本面卡片（本益比/股價淨值比/殖利率/市值）
│  ├─ ChipsCard.tsx                  籌碼面卡片（三大法人買賣超/融資融券）+ 近期重大訊息公告，
│  │                                 僅台股頁面渲染（`quote.market === "TW"` 才顯示），美股
│  │                                 沒有對應的公開資料源
│  ├─ LiveQuoteHeader.tsx            個股頁報價 header，盤中每 20 秒自動刷新
│  ├─ LiveIndices.tsx / IndexCard.tsx 首頁大盤指數卡片，同樣有自動刷新
│  ├─ MarketStatusBadge.tsx          「盤中／已收盤」狀態徽章
│  ├─ DailyBriefCard.tsx             首頁 AI 每日快報卡片
│  ├─ WatchlistButton.tsx            加入/移除關注清單的星號按鈕
│  ├─ WatchlistSection.tsx           首頁「我的關注」清單區塊（排序、匯出 CSV）
│  ├─ WatchlistTable.tsx             關注清單表格（取代 StockTable）：可編輯持有股數/平均成本，
│  │                                 即時算損益，寫入用 useSyncExternalStore 要注意快照函式不能
│  │                                 用 .filter()/.map()（見下方工作日誌無限迴圈那次教訓）
│  ├─ WatchlistSync.tsx              登入後把本機 localStorage 清單與雲端清單合併同步
│  ├─ PriceAlertForm.tsx             個股頁「到價提醒」表單：設定/列出/刪除該股的到價提醒
│  ├─ PriceAlertWatcher.tsx          掛在全站 layout，每 30 秒檢查所有未觸發的到價提醒，
│  │                                 觸發時右下角跳提醒（目前只有網頁內顯示，還沒接 LINE 通知）
│  ├─ MarkdownLite.tsx               輕量 Markdown 渲染（粗體/條列），給 AI 回覆/每日快報用，
│  │                                 不是完整解析器，只處理 AI 實際會用到的語法
│  ├─ ThemeToggle.tsx                深色模式手動切換開關，寫 data-theme + localStorage
│  ├─ SiteHeader.tsx                 全站頂部導覽列（含 ThemeToggle、AuthButton）
│  ├─ AuthButton.tsx                 Google 登入/登出按鈕
│  └─ AuthProvider.tsx               NextAuth SessionProvider 包裝
│
└─ lib/
   ├─ data/                          資料層——所有股票資料的唯一進出口
   │  ├─ index.ts                    對外統一介面：getQuote/getChart/getIndices/searchStocks/
   │  │                              getFundamentals/getEarnings/getMultiSignalStocks/detectMarket/normalizeSymbol
   │  │                              等，全部「抓不到資料就回 null，絕不產生假資料」
   │  ├─ twse.ts                     台股資料抓取：TWSE 即時報價/K線/OpenAPI 基本面(含P/B)/月營收/
   │  │                              季報EPS/三大法人買賣超(legacy T86)/融資融券(MI_MARGN)/
   │  │                              每日重大訊息(t187ap04_L，注意「主旨 」欄位名尾端有個空格)
   │  ├─ us.ts                       美股資料抓取：Yahoo Finance 報價/K線/基本面(含P/B)/季度財報（皆需 crumb+cookie 認證）
   │  ├─ news.ts                     新聞資料：Google 新聞 RSS 搜尋（zh-TW + en-US 雙版面，美股
   │  │                              相關查詢會混合中英文來源），給 AI 問答「資訊面」用
   │  ├─ universe.ts                 股票清單：台股動態抓 TWSE 官方上市清單（取權值股優先的 200 檔子集，
   │  │                              見下方工作日誌 3202cc7 與 2026-09-11 擴大清單那次），美股約 150+ 檔
   │  │                              精選跨產業大型股種子清單
   │  ├─ cache.ts                    共用 TTL 快取：cached()/cachedMap()，Redis 優先、記憶體備援、
   │  │                              single-flight 去重複、null 值也會被正確快取（見工作日誌，這層踩過最多坑）
   │  ├─ kv.ts                       Redis 連線設定（Vercel KV / Upstash）——**2026-09-11 起正式站
   │  │                              已經設定好並連接（Upstash 免費方案），不再是「選用但沒開」
   │  │                              的狀態；程式碼邏輯仍然保留「沒設定就退回記憶體快取」的容錯，
   │  │                              本機開發沒設這兩個環境變數一樣能跑，只是不會有跨伺服器共用
   │  └─ types.ts                    Market / Quote / Chart 等共用型別
   │
   ├─ ai/                            AI 問答與每日快報
   │  ├─ provider.ts                 共用的 Gemini→Claude fallback 呼叫邏輯（callAiProviders），
   │  │                              處理對話歷史裁切、開頭必須是 user、合併連續同角色 turn
   │  ├─ ask.ts                      /api/ask 的問答邏輯：組 system prompt、抓相關股票資料當 RAG context
   │  ├─ brief.ts                    每日 AI 快報生成邏輯，以台北時間日期當快取 key
   │  ├─ gemini.ts                   Gemini API 呼叫封裝（含自動探測可用模型名稱）
   │  └─ types.ts                    ChatTurn 等共用型別
   │
   ├─ auth.ts                        NextAuth 設定（Google Provider）
   ├─ watchlist.ts                   自選股清單邏輯（localStorage），含可選的 costBasis/shares
   │                                 持股成本欄位跟 updateHolding()
   ├─ watchlistStore.ts              登入後自選股的伺服器端 Redis 儲存
   ├─ priceAlerts.ts                 到價提醒的 localStorage CRUD，架構跟 watchlist.ts 一致
   ├─ signals.ts                     客觀技術訊號計算（不是預測，只描述當下數據狀態），含
   │                                 均線/爆量/創新高低/連漲跌，以及 RSI/MACD 黃金死亡交叉
   ├─ marketStatus.ts                判斷台股/美股目前是否為交易時段
   ├─ format.ts                      數字/價格格式化（含台股紅漲綠跌慣例）
   ├─ theme.ts                       深色模式相關輔助（讀 CSS 變數、fallback 色票）
   ├─ chatEvents.ts                  跨元件溝通用的事件（例如 AskAboutButton 通知 ChatWidget 换聚焦股票）
   └─ site.ts                        網站名稱/網址常數（SEO metadata 用）

├─ proxy.ts                          Next.js 16 的 proxy（原 middleware）：全站密碼保護閘門，
│                                     沒有 site_unlocked cookie 一律導去 /unlock，含 API 路由
└─ app/
   ├─ unlock/page.tsx + UnlockForm.tsx  密碼輸入頁
   └─ api/unlock/route.ts               驗證密碼、設定 cookie（預設密碼寫死在這個檔案）

根目錄:
├─ README.md          完整功能說明、環境變數設定、資料來源與限制、專案結構、設計慣例
├─ CLAUDE.md           使用者要求的品保流程規則（每次對話都要照做，見下方摘要）
├─ AGENTS.md            Next.js 版本提醒（這個版本跟訓練資料的 Next.js 可能有差異，寫程式前看 node_modules/next/dist/docs/）
├─ vercel.json          Vercel Cron 排程設定（每天觸發每日快報預生成，注意：這個一天一次的頻率
│                       不夠用來預熱 search/highlights，那個改用下面的 GitHub Actions）
├─ .github/workflows/warm-cache.yml  每 5 分鐘呼叫 /api/cron/warm-cache 預熱快取（見工作日誌）
└─ .env.example         需要的環境變數範例（AI API key、Redis、Google OAuth、SITE_PASSWORD，全部選用；
                        Redis 這幾個雖然程式碼邏輯上是選用，但正式站現在已經設定好了，見下方
                        「重要慣例與限制」）
```

## 目前所有功能（快速索引，細節見 README.md）

首頁大盤指數/AI快報/關注清單、個股頁報價+K線+技術訊號（含 RSI/MACD）+基本面+AI問答+到價提醒、
搜尋/篩選頁（產業多選+價格區間）、每日焦點榜單（漲跌幅/成交量/技術訊號共振）、AI 多輪問答（可分析
整個關注清單、依持股成本算損益、可引用真實新聞與財報——台股月營收年增率/季報EPS、美股季度EPS，
新聞來源 Google 新聞 RSS）、每日 AI 快報（Vercel Cron 排程）、關注清單＋持股成本追蹤
（localStorage，登入後跨裝置同步）、到價提醒（目前僅網頁內顯示）、深色模式、共用 Redis 快取
（**2026-09-11 起正式站已設定，Upstash 免費方案**）、背景自動預熱（GitHub Actions 每 5 分鐘）、
Google 登入（選用）、全站密碼保護（`SITE_PASSWORD`）、全站 SEO metadata。**全站不使用
任何示範/假資料**——抓不到就誠實顯示「資料暫缺」。**網站現在是密碼保護的私人工具，不是對外公開
服務**，AI 問答會直接給明確推薦/看法，這點跟一般公開股票網站的「不做投資建議」慣例不同，見
「重要慣例與限制」。

## 重要慣例與限制

- **紅漲綠跌**：台灣/中國市場慣例，跟美股常見的反過來，全站配色與 `format.ts` 都照這個做。
- **AI 問答的建議尺度跟網站是否公開綁在一起，不是單純的文案風格選擇**：網站現在有密碼保護
  （見下方工作日誌 2026-09-11、`src/proxy.ts`），只有使用者跟家人用，所以 `lib/ai/ask.ts`
  的系統提示詞允許直接給明確個人看法/推薦。**這個前提很重要**：台灣證券投顧法規管的是「對不
  特定多數人」提供投資推介，判斷基準是網站有沒有做存取限制，不是使用者自己覺得是不是私人
  用途。如果之後把密碼保護拿掉、或網站又變成任何人都能進來，AI 問答務必要改回客觀數據描述、
  不做買賣建議/漲跌預測的版本（`getMultiSignalStocks` 在 `lib/data/index.ts` 的註解本來就是
  照這個原則設計的）——**不要因為看到這裡的舊版指示殘留就假設現在還是客觀中立的版本，先看
  `lib/ai/ask.ts` 目前實際的系統提示詞內容再判斷。**
- **雙市場一律用分頁，不並排**：`MarketTabs` 元件，全站慣例。
- **這台本機開發環境（Windows PC）對外網路正常、能連到正式站**：跟這份文件更早版本記錄的
  「sandbox 連不到外部」不同（那是另一台/另一個環境的限制，不是這台）。這台機器上已經裝好
  Node.js（`winget install OpenJS.NodeJS.LTS`）跟 Playwright（裝在系統暫存資料夾，不在專案
  `node_modules` 裡），可以真的開瀏覽器測試正式站，也能 `npm install && npm run build &&
  npm run start` 在本機起服務打真實上游資料驗證修改。如果換了一台新環境，還是要重新確認一次
  網路/工具限制，不要照抄這段。

## 品保流程（詳細規則見 CLAUDE.md，這裡只摘要）

使用者要求每次對話回報「更新完成」前要走完：
1. **規則一**：同一類問題卡關連續失敗 2 次，第 3 次要同步派 2 個不同模型 agent 上網查解法，交疊進行不間斷。
2. **規則二**：自己先做一次完整的實測（build+start+curl，模擬瀏覽器操作）地毯式檢查，抓到的問題全部修好；之後只要針對這次修的問題複查即可，不必每次重新全站掃。
3. **規則三**：規則二做完後，派 1 個 Opus agent（額度不夠才臨時換模型頂替，之後仍改回 Opus）做一次完整地毯式檢查；抓到的問題自己修，修不好就讓 Opus 直接動手；之後只需針對這次修的問題再複查，直到 Opus 確認「沒有發現問題」才能回報「更新完成」。
4. **規則四**：每次回覆使用者都要附上網站網址 https://stock-web-blond.vercel.app 。**網址跟密碼絕對不能寫在同一行/緊接在一起**（會被通訊軟體自動連結辨識吞掉變成壞連結，這件事已經真實發生過不只一次），務必分開兩行。
5. **規則五（跨裝置接續）**：每次回覆使用者之前，都要更新這份 PROGRESS.md 並 push，讓其他裝置的 Claude Code 接得上。
6. **規則六**：只要在等待背景工作完成（部署、下載、agent 執行等）導致一段時間沒有新回應，每最多 5 分鐘要在對話視窗主動回報一次目前狀態，不能整段沉默、也不能只依賴「完成才通知」的機制悶著頭等。

## 工作日誌（新到舊，只列有意義的變更；commit hash 對應 `git log`）

### 2026-09-11：每日快報大幅擴充 + 修正 AI 問答漏接常見追問（`1c503f5`、`49fb814`）
使用者提出兩個要求，這次同時處理：① 每日快報寫更詳細，且要根據籌碼/新聞等多方資訊分析
台股與美股的關聯；② 新增一個近幾天各方重點/重要資訊的段落。同時使用者也貼出一段真實
AI 問答對話，指出 AI 一直給空泛答案（「留意 AI 伺服器供應鏈如鴻海、廣達」）、追問「還有
別的」時答非所問，最後還撞到 Gemini 429，要求兩件事並行處理。

**每日快報（`lib/ai/brief.ts`）**：新增台股+美股（中英雙語）近期新聞、台股主要漲跌個股的
三大法人籌碼動向，一起餵給 AI；系統提示詞改成 4 段式（大盤與台美連動／台股焦點／美股
焦點／**近期重點回顧**），字數 280-400→550-800，第一段要求綜合大盤數字+新聞事件+籌碼
資料講出「為什麼」連動、不是只講「有沒有關聯」；第四段條列近幾天新聞裡持續出現的脈絡，
不重複當天數字。快報本身仍維持既有「不給投資建議」的客觀新聞風格，沒有比照聊天問答改成
給推薦（使用者這次沒有要求這塊跟著改）。

**首頁載入解耦**：快報變大之後生成更慢，原本首頁用 `Promise.all` 直接等它會拖慢冷快取
當天第一位訪客的整個首頁。比照 `/highlights` 的 `MomentumSection` 模式：新增
`/api/daily-brief`，`DailyBriefCard` 改成 client component 掛載後才 fetch、搭配骨架屏，
首頁其他區塊不再被快報生成速度拖住。

**AI 問答修正真實回報的問題**：追查後發現 `MOVERS_INTENT_PATTERN` 這個正則沒有涵蓋
「還有別的比較有機率漲幅較大的嗎？」這種常見追問句型（沒有「有哪些」「推薦」「強勢股」
等關鍵字），導致 `buildMoversGrounding` 完全沒被觸發，AI 沒有任何具體資料可用，只能退化
成空泛答案。擴大關鍵字規則涵蓋「還有別的/其他」「有機會」「機率...大/高」「買什麼」
「選股」等句型（只在沒偵測到特定股票時才生效，不影響個股問答）；`buildMoversGrounding`
的台股技術訊號共振股現在也一併附上三大法人籌碼動向；系統提示詞新增規則要求追問「還有
其他」時要挑對話中還沒提過、有具體數據支撐的標的，不能用空泛產業詞湊答案。正式站用真實
AI 重現使用者原本那句話，這次正確列出「恩德/中砂/彰銀/兆豐金」等具體標的並附上三大法人
買超張數，不再是「AI伺服器供應鏈」這種空話。

**部署後自己測出的真實 bug（`49fb814`）**：快報變成 550-800 字之後，第一次正式站驗證就
發現生成結果被截斷在「...S&P 500與」這種讀一半的句子中間，但 `usedAi` 仍顯示 `true`——
追查是中文字元在 Gemini tokenizer 裡比預期消耗更多 token，原本設的 1600 output token
上限不夠用，生成中途被硬性砍斷，程式原本只要拿到非空字串就當作成功，沒有檢查有沒有被
砍斷。修法：`gemini.ts` 新增檢查 `finishReason === "MAX_TOKENS"`，視為失敗改走 fallback；
`maxOutputTokens` 拉高到 3000。**教訓：中文（CJK）文字在多數 LLM 的 tokenizer 裡，每個
字消耗的 token 數通常比西方語言的估算更多，要求 AI 生成的目標字數提高時，output token
上限要留更寬的餘裕，不能直接按「字數」線性估算 token 數；而且不能只檢查回應是不是非空
字串就當作成功，還要檢查 `finishReason` 有沒有顯示被截斷，否則使用者會看到一句話讀到一半
突然斷掉、卻沒有任何錯誤提示。**

因為快報用日期當快取 key、TTL 25 小時，壞掉的那份不會自己消失，額外在 `cache.ts` 加上
`forceRefresh` 選項、`/api/daily-brief` 支援 `?refresh=1`，讓卡住的快取可以立刻手動蓋掉
重算，不用乾等快取過期。已用這個機制把當時已經壞掉的快取蓋成修正後的完整版本，並用
Playwright 截圖確認首頁四段快報正確顯示、格式（粗體小標題+條列）正常。

**共用重構**：`formatSharesWithLots`（`lib/format.ts`）、`fetchUsMarketNews`
（`lib/data/news.ts`）從 `ask.ts` 搬出來設成共用函式，`brief.ts` 現在用同一套邏輯算籌碼
股/張換算跟美股中英雙語新聞，避免同一套邏輯在兩個檔案各自維護一份、將來各自被改壞。

### 2026-09-11：Opus 規則三驗證挖出 2 個真實問題（1 個高風險安全問題）並修好（`7bd58e1`）
派出的 Opus agent 針對上一則新功能（籌碼面/重大訊息/AI 綜合分析）加上全站地毯式檢查，抓到
2 個真的問題，其餘一切（籌碼數字、紅綠配色、美股正確不顯示籌碼卡、AI 綜合分析品質、首頁/
搜尋/關注清單/到價提醒/深色模式/手機版）全部驗證正常：

1. **【高風險、且已經真的發生】Gemini API 金鑰外洩到瀏覽器**：`fetchWithTimeout()`
   （`lib/data/cache.ts`）對非 2xx 回應丟出的錯誤訊息把完整請求 URL 原封不動包進去，而
   Gemini 的請求 URL 長這樣：`...generateContent?key=真實金鑰`。這段錯誤經過
   `provider.ts` 記錄失敗原因 → `ask.ts` 的 `buildCannedAnswer` 組進回覆文字 → `/api/ask`
   回給前端 → 聊天視窗直接顯示出來，只要 Gemini 呼叫失敗（例如免費額度撞到 429，這是常態
   不是例外）金鑰就整串外洩到瀏覽器。Opus 在測試過程中自然撞見了一次（問了 3 個問題就
   撞到 429），代表這不是理論風險，是正式站真實發生過的外洩。**修法**：`fetchWithTimeout`
   統一 redact 掉 URL 裡常見的機密參數名稱（`key`/`apikey`/`token`/`secret`），一次保護
   所有現在跟未來的呼叫端，不是只針對 Gemini 補丁。**⚠️ 這組 Gemini API 金鑰已經外洩過，
   即使網站有密碼保護、外洩當下大概率只有使用者自己的瀏覽器看到，還是強烈建議去
   [Google AI Studio](https://aistudio.google.com/apikey) 重新產生一組新的金鑰，並更新
   Vercel 專案的 `GEMINI_API_KEY` 環境變數——這件事我沒有 Vercel/Google 帳號權限可以代勞，
   需要使用者自己動手，下次接手的裝置如果看到這裡還沒打勾，要提醒使用者還沒做。**
2. **【高風險】AI 常把籌碼面「股」自己心算換成「張」時算錯**：一開始 grounding 只給三大
   法人買賣超的股數，沒給台灣投資人慣用的張數（1張=1000股），結果 AI 大約 4 成的籌碼面
   問答會自己嘗試換算，出現 1000 倍算錯（例如鴻海實際賣超 3,386 張被講成「338 萬張」）、
   10 倍算錯、甚至同一句話裡數字前後矛盾。UI 上的 ChipsCard 卡片本身數字完全正確，問題
   只在 AI 生成文字回答時的計算。**修法**：`ask.ts` 直接把換算好的張數一起附進 grounding
   文字（`-8,832,445股（約-8,832張）`），系統提示詞也明確要求「直接引用、不要自己重新
   換算」。**教訓：任何要給 AI 用的數字資料，如果使用者慣用的單位跟資料源原始單位不同，
   換算這件事要在程式碼裡用確定性的方式做好、直接把兩種單位都附上，不要指望 AI 自己心算
   單位轉換——即使換算公式很簡單（1張=1000股），LLM 在長文字生成過程中做算術還是常出錯，
   而且這種錯誤很隱蔽：文字讀起來很流暢、格式也對，只有實際核對數字才會發現差了 1000 倍。**
3. 順手修正 Opus 同時抓到的既有小問題（非本輪新功能造成，但既然抓到了就一起修）：K 線圖
   成交量座標軸用 lightweight-charts 內建的 K/M/B 縮寫格式，跟同一頁其他地方（header、
   滑鼠提示框）用的「張」不一致，改用自訂 `priceFormat.formatter` 統一走 `formatVolume()`。

**Opus 也回報了兩個記錄下來但沒有修的次要現象**：① 首頁「今日市場快報」（一天生成一次的
AI 快報）跟下方即時「大盤指數」卡片同時出現在畫面上時，數字可能矛盾（快報是台北時間
15:59 產生的美股開盤前預測，即時卡片是當下真實數字）——這是「一天一次快報」這個設計本身
的固有限制，不是 bug，暫不處理。② 測試過程出現過 2 次 `Error: Value is null` 的
pageerror，之後用 3 種方式嘗試重現（深色模式切換、快速切換K線區間、瀏覽器上下頁導航）都
沒有再出現，信心不足以判定是真的 bug，先記錄，之後如果有人穩定重現再處理。

已用真實資料驗證修正：`redactSecretParams` 單元測試確認金鑰被正確遮蔽、一般網址不受影響；
重新問鴻海籌碼面，grounding 文字裡的張數換算跟 Opus 驗證用的真實 TWSE 數字完全一致
（-3,386/-3,751/+410/-45 張）。已推上正式站。

**規則三複查（第二輪 Opus agent，只針對這兩個問題複查，不重跑全站）：兩個都確認修好**：
- 問題 1（金鑰外洩）：**FIXED**。故意連續問問題撞到 Gemini 免費額度 429（22 次裡撞到 4
  次），掃描全部回應與畫面 DOM 找 `AIza` 開頭的金鑰格式，0 次出現；`key=` 一律顯示
  `key=***`，包含用 Playwright 開真的聊天視窗確認畫面上顯示的也是遮蔽過的版本。
- 問題 2（股/張換算）：**FIXED，16 次真實 AI 回覆全部正確（100%）**。測了 2317 鴻海、
  2303 聯電、2454 聯發科、2330 台積電、2412 中華電、2881 富邦金，每個問 2-4 次不同問法，
  跟 TWSE T86 原始資料逐一核對「合計」跟「外資/投信/自營商」分項，全部一致，鴻海這個
  當初出錯的案例現在正確講出「賣超 3,386 張」（之前是誤講成「338 萬張」）。

**複查過程中 Opus 順便回報一個新的次要現象，已自己追查排除**：Opus 直接呼叫
`/api/ask`（沒有帶 `symbol` 欄位）問「鴻海三大法人買賣超多少張？」，`groundedSymbol`
卻回傳美股代號 `E`（義大利石油公司 Eni），答非所問。追查後判斷**這是 Opus 自己測試腳本
的字元編碼問題，不是網站的真實 bug**：用一個乾淨的 UTF-8 檔案（不透過 shell 內嵌參數）
重送完全相同的問題文字，`groundedSymbol` 正確回傳 `2317`；另外測「聯電三大法人買賣超多少？」
也正確回傳 `2303`。這跟這次對話稍早我自己也踩過一次的同類問題一模一樣（見下方「這次的
環境變化」章節新增的教訓）：這台機器上用 Windows Git Bash 的 `curl -d '...'` 內嵌傳遞含
中文的 JSON 字串，偶爾會被 shell 用錯誤的編碼重新解讀，讓文字裡混入不存在的英文字母，
不是程式碼的問題。**之後任何裝置在這台機器上寫測試腳本，只要 payload 含中文，一律用
`--data-binary @檔案路徑`（檔案用 Write 工具寫成 UTF-8）取代 `-d '...內嵌字串'`，避免
把這類編碼假警報誤判成真的 bug 去追查。**

### 2026-09-11：新增台股籌碼面、重大訊息公告、股價淨值比，全部串進 AI 問答（`3d9450f`、`7bbb829`）
使用者看了一份外部投研資料的「8 大資料面向」簡報（籌碼面/消息面/法說會/供應鏈/總經/基本面/
技術面/期權），問這些能不能免費做到。逐項評估後，使用者選定當下就能免費、無需 API 金鑰做到
的三項（三大法人買賣超+融資融券、重大訊息公告、擴大新聞來源），並要求「全部做完，而且要讓
AI 能綜合這些資訊分析」。

**新增三個免費 TWSE 官方資料源**（都用 `node -e "fetch(...)"` 先實際打過端點確認欄位名稱
與資料格式，不是憑印象猜測）：
- 三大法人買賣超：`www.twse.com.tw/rwd/zh/fund/T86`（TWSE 官方網站自己在用的端點，非
  OpenAPI 清單內，但一樣免費公開；單位是**股**，一次回傳全市場）
- 融資融券餘額：`openapi.twse.com.tw/v1/exchangeReport/MI_MARGN`（單位是**張**，注意跟
  三大法人的股數單位不同，容易搞混）
- 每日重大訊息公告：`openapi.twse.com.tw/v1/opendata/t187ap04_L`
- 順手加碼：既有的 `BWIBBU_ALL`（本益比/殖利率）端點其實還有 `PBratio` 欄位沒取用，補上
  股價淨值比；美股同樣從既有的 Yahoo `v7/finance/quote` 回應多取 `priceToBook` 欄位，
  兩個市場都補齊，沒有增加任何額外的網路請求。

**過程中自己抓到的真實 bug**：`t187ap04_L` 的主旨欄位 JSON key 實際上是 `"主旨 "`（尾端
多一個空格），一開始寫成 `主旨`（沒有空格）去存取，導致 `row.主旨` 永遠是 `undefined`，
每一筆公告都被「沒有主旨」的檢查靜默過濾掉——最陰險的地方是這個 bug **完全不會報錯**：
Map 建得起來、函式簽章型別檢查也過，測試台積電當天剛好沒公告時完全看不出異常，直到改用
一定會有公告的個股（`6949`，那天真的有面額變更公告）測試才發現「重大訊息公告」永遠是空的。
用 `node -e` 直接印出 `Object.keys(rows[0])` 才抓到真正的 key 名稱。**教訓：串接一個新的
外部 JSON API 時，不要只憑欄位「看起來」的中文名稱去猜 TypeScript 介面的 key，先用
`Object.keys()` 印出真正的 key 一次，尤其中文欄位名很容易夾帶不明顯的前後空白。用一個
「當天保證有資料」的測試案例（而不是隨便挑一檔，剛好那天沒資料）也同樣重要，不然這類
「安靜地回傳空結果」的 bug 很容易被誤判成「這功能本來就常常沒資料，正常」。**

**串進 AI 問答**（`lib/ai/ask.ts`）：個股 grounding 現在同時帶基本面（本益比/股價淨值比/
殖利率/市值，這是先前完全沒有的——`ask.ts` 之前只有報價/K線/財報/新聞，從來沒把
`getFundamentals` 接進來過）、籌碼面、重大訊息公告一起給 AI；系統提示詞新增一條規則，
要求給看法時要綜合基本面/財報/籌碼面/消息面/技術面「這幾個面向一起判斷，不要只看單一
面向」，面向互相矛盾時要老實點出矛盾，不要選擇性忽略對結論不利的一面。正式站用真實 AI
驗證：問「台積電現在值得買嗎？」，回覆同時引用了 8 月營收創新高（財報）、三大法人賣超
8823 張（籌碼面）、本益比 28.4 倍（基本面）、大盤重挫後美股夜盤反彈（消息面/大盤），
綜合給出「拉回分批佈局」的明確看法——確認 AI 真的有在綜合運用這些新資料，不是只有資料
擺在那裡但沒用到。

**新增新聞來源**（`lib/data/news.ts`）：Google 新聞原本只查 zh-TW 版面，新增 en-US 版面
選項，美股相關查詢（個股新聞、大盤新聞）改成 zh-TW + en-US 各查一次再去重合併，正式站
驗證確認美股新聞現在會混入 Reuters/CNBC/MarketWatch/Bloomberg 等原生英文財經媒體，不再
只靠 zh-TW 版面裡少量的中文翻譯報導。台股個股新聞則單純從 5 則拉高到 8 則。

**新增 UI**：`FundamentalsCard` 加一欄股價淨值比；新元件 `ChipsCard`（籌碼面 + 近期重大
訊息）只在台股頁面渲染，美股頁面完全不顯示（`getChips`/`getMaterialAnnouncements` 對美股
一律回傳 null/空陣列，不是抓取失敗）。Playwright 截圖確認淺色/深色模式下都正常顯示、配色
維持紅漲綠跌慣例（三大法人賣超顯示綠色、融資增加顯示紅色）。

**額外修正**：正式站用真實 AI 連續問 4 次，其中 1 次撞上 Gemini 呼叫的 8 秒逾時（`lib/ai/
gemini.ts` 的 `callGemini`），研判是這次新增的資料讓 grounding prompt 明顯變大、生成時間
跟著變長，8 秒對現在的 prompt 大小偏緊；拉長到 12 秒，其餘 3 次重試都在時限內成功。

**這次對話也新增 CLAUDE.md 規則六**：使用者指出等待背景工作（這次是等 Vercel 部署）時，
只丟一句「正在等待」就整段沉默，讓人不知道到底在做事還是卡住了——事後也真的發現背景輪詢
指令本身寫錯（沒帶登入 cookie，永遠不會成功），乾等了很久沒人發現。之後任何背景等待都要
每 5 分鐘主動回報一次進度，不能只依賴「完成才通知」的機制悶著頭等。

### 2026-09-11：AI 問答新增真實新聞與財報資料（`861df45`）
使用者實際跟網站 AI 對話，貼出完整對話紀錄：問「除了數據面，有資訊面嗎？」AI 只能回答
「手上沒有連接其他新聞網站或外部總經資訊的管道」——這是真實的功能缺口，AI 問答一直以來
真的只有價格/技術數據，完全沒有新聞或財報資料源。新增兩層資料，都是免費、不用申請 API
金鑰的公開來源：

1. **新聞**（新檔案 `src/lib/data/news.ts`）：Google 新聞 RSS 搜尋，涵蓋大量中文財經媒體，
   結構固定簡單，沒有另外拉 XML/RSS 套件，直接用正規表示式解析。
2. **財報**：
   - 台股（`twse.ts`）：TWSE 官方 OpenAPI 月營收年增率（`t187ap05_L`，台股投資人最常看的
     財報先行指標）+ 季報 EPS（`t187ap06_L_ci`，涵蓋一般產業，金融/保險業 TWSE 用不同報表
     代號，這次沒收錄，查不到就是查不到，不會拿別家數字頂替）。
   - 美股（`us.ts`）：沿用已經接好的 Yahoo crumb+cookie 認證機制，加 quoteSummary 的
     `earnings` 模組，拿最新一季實際 EPS vs 市場預期、下次公布財報日期。

`ask.ts` 串接：問特定股票時，個股資料新增「財報」「近期相關新聞」；不管有沒有問特定股票，
一律新增「近期市場新聞」（台股/美股各幾則），讓「資訊面/消息面」這類泛問題也有真實資料可
引用。系統提示詞明確要求以後不能再說「沒有新聞管道」。

**過程中一個值得記錄的排查經驗**：部署後第一次在正式站測試 AI 回答，得到「查不到台積電
最新的財報與新聞數據」——沒有直接假設是程式碼有 bug，而是先加一個臨時診斷端點
（`/api/debug/earnings`，事後已刪除）直接從 Vercel 環境呼叫 `getEarnings`/`fetchNews`，
確認資料抓取本身完全正常（回傳真實 EPS、真實新聞）。**判斷第一次測試只是撞上部署交接的
中間狀態**（測試時機太早，Vercel 還在切換到新版本），等穩定後重新問同樣的問題，AI 正確
引用了真實財報跟新聞。**教訓：正式站部署後的第一次驗證如果結果不如預期，先確認是不是測試
時機太早（部署還沒完全生效），不要急著回頭改程式碼；用一個直接呼叫底層函式的診斷端點，
比單純重新問 AI 更快分辨「是資料抓不到」還是「AI 拿到資料但沒引用」這兩種完全不同的問題。**

**使用者主動提議如果需要申請免費 API 金鑰可以去辦**：目前用的都是不用註冊的免費公開來源
（Google 新聞 RSS、TWSE OpenAPI、既有的 Yahoo 認證機制），已經驗證可用，暫時沒有急迫需要。
如果未來想要更穩定的美股財報來源（Yahoo 這類非官方端點終究有可能像先前 `v7/finance/quote`
一様被說鎖就鎖）或更完整的新聞全文摘要，可以考慮申請 Alpha Vantage 或 Finnhub 的免費 API
金鑰（両者都不用信用卡，但額度有限——Alpha Vantage 約每天 25 次、Finnhub 約每分鐘 60
次），下次有人提出需求或 Yahoo 端點又出問題時再評估要不要加。

### 2026-09-11：請使用者用另一個 Claude（Chrome 擴充功能）獨立測試，釐清三個回報
使用者把測試指令轉給在自己瀏覽器裡跑的 Chrome Claude，拿到一份獨立回報，裡面有三個發現，
逐一查證：

1. **「密碼頁沒出現」**：查證後確認是**假警報**——Chrome Claude 用的是使用者自己平常在用的
   瀏覽器，之前已經手動登入過，`site_unlocked` cookie 還在（使用者事後也確認「清 cookie
   沒清乾淨，功能其實正常」）。用完全乾淨、沒有任何 cookie 的 Playwright context 測試，
   一律正確導向 `/unlock`，密碼保護本身沒有問題。**教訓：使用者透過自己的日常瀏覽器測試時，
   报告「沒看到密碼頁」不能直接當成閘門失效的證據，要先排除「這個瀏覽器本來就登入過」這個
   最常見的可能性，尤其閘門 cookie 有效期長達一年。**
2. **「`_rsc=` 連結預先讀取請求全部回傳 503」**：查證後**沒有重現**——用乾淨瀏覽器實測 18 個
   prefetch 請求，全部是 200。研判是 Chrome Claude 測試當下剛好撞上這次對話連續推送好幾個
   版本的部署交接瞬間（deploy cutover 時舊版本的 serverless function 可能短暫幾秒鐘不可用），
   不是持續性問題。
3. **「背景預熱機制沒有真的在運作」**：查證後**確認機制正常**——直接查 GitHub Actions 的排程
   執行紀錄（`https://api.github.com/repos/hj110b13-Andy/Stock-web/actions/workflows/warm-cache.yml/runs`），
   確認已經依照 `*/5 * * * *` 排程自動執行過，狀態是 `completed`/`success`。

**這次沒有因為外部回報的三個「發現」就急著動手改程式碼，而是先各自查證**：用完全乾淨的
瀏覽器環境、GitHub API 的公開紀錄這些不受「使用者自己瀏覽器狀態」干擾的方式重新驗證，
確認三個都是誤報或一次性的部署時機巧合，不是真的迴歸。**如果之後又有類似「另一個工具/
另一個人測試回報異常」的情況，先用一個乾淨、獨立的環境重現一次再決定要不要動手修，不要
看到回報就照單全收去改程式碼。**

### 2026-09-11：背景自動預熱機制 + Redis 共用快取，網站隨時保持「現成資料」（`052b811`）
使用者要的不是「使用者觸發後才快」，是「網站自己一直在背景更新，有人點進來就直接看到現成
資料」。這需要兩個東西合起來才會真的有效，缺一個都不行：

1. **背景排程主動預熱**（`052b811`）：新增 `/api/cron/warm-cache`，把 `/highlights`、
   `/search`、首頁焦點排行需要的資料（台股+美股批次報價、技術訊號共振股、大盤指數）全部
   預先算好進快取。**排程機制選 GitHub Actions（`.github/workflows/warm-cache.yml`，每 5
   分鐘觸發一次）而不是 Vercel 自己的 Cron**——Vercel 免費方案的 Cron 一天只能跑一次，遠遠
   不夠頻繁。這個 repo 的預設分支正好就是 `claude/relaxed-curie-c69kp0`，排程檔案推上去就會
   生效，不用額外設定。
   - **過程中發現並修好一個自己造成的回歸 bug**：`src/proxy.ts` 的密碼保護閘門攔截了
     `/api/cron/*`，導致原本运作正常的每日快報排程（`vercel.json`）從密碼保護上線那天起，
     每次觸發都被導去 `/unlock`（307），根本沒有真的執行到。已經在 `proxy.ts` 的 matcher
     排除清單加上 `api/cron`。**教訓：任何排程/webhook 類的路由，本質上不可能帶瀏覽器的
     session cookie，加全站認證閘門時一定要記得排除，不能只想著「使用者走的路徑」。**
2. **Redis/KV 共用快取**：光有背景預熱還不夠——正式站是好幾台各自獨立的 Vercel serverless
   伺服器，沒有共用儲存的話，`lib/data/cache.ts` 的記憶體快取是「每台伺服器自己記自己的」，
   預熱可能只熱到其中一台，真正訪客的請求被導到另一台完全沒被預熱過的伺服器一樣要重新算。
   引導使用者到 Vercel Storage 頁面設定 Upstash Redis（免費方案）並連接到專案——**這步驟是
   使用者透過另一個 Claude（Chrome 擴充功能）在 Vercel 後台實際點擊完成的，我這邊只負責
   引導步驟跟事後驗證**，我自己沒有 Vercel 後台的存取權限。

驗證方式：正式站觸發一次 `/api/cron/warm-cache` 後，同時發送 5 個併發請求打
`/api/search?market=TW`（刻意用併發測試，增加打到不同伺服器實例的機率），全部落在
0.76-1.24 秒，沒有出現任何一個「完全沒被預熱過」的慢請求（之前這種情況會是 2-9+ 秒）——
確認 Redis 真的讓多台伺服器共用同一份預熱結果。

**誠實的邊界**：這個機制讓「幾乎每次造訪都吃到現成資料」變得可靠，但不能讓「真的從來沒人
碰過的全新資料」瞬間出現——那個受限於要跟 TWSE/Yahoo 真的走一次網路的時間，是免費公開
資料源的先天限制，這次沒辦法、也不打算為了這個去改用付費資料服務。

### 2026-09-11：技術訊號共振股再調整——確認硬底線在哪、盡量壓縮周圍空間（`115704d`）
使用者追問技術訊號共振股那 ~5 秒背景載入能不能再快。**先做本機對照實驗，排除正式站網路波動
干擾**：候選股 25→15 檔，本機只省了約 18%（1.73 秒→1.42 秒）——證實這個等待主要是 Vercel
伺服器到 TWSE 之間的網路來回時間，不是候選股數量或本地運算量的問題。TWSE 的 STOCK_DAY
端點沒有「一次查多檔股票、一次查多個月」的介面，`fetchTwseCandles`（`twse.ts`）本來就是
每檔候選股的每個月各發一次請求，這是外部資料源的限制，無法繞過。

**這是這輪效能追查裡第一個確認碰到「無法再優化、只能減少發生頻率」硬底線的項目**，處理方式
從「讓運算更快」改成「讓更少次造訪需要真的付這個代價」：
- `MOMENTUM_CANDIDATE_LIMIT` 25→15（列表只顯示前 10 名，砍掉的名額很少真的會改變顯示結果）
- `MOMENTUM_TTL_MS` 5→10 分鐘（技術訊號晚幾分鐘更新，對私人使用完全不影響體驗）

正式站實測：`/api/momentum?market=TW` 從約 5 秒降到 0.68-0.76 秒（多數請求現在落在快取
命中範圍內）。**誠實地跟使用者說明剩下的限制**：這是外部資料源的網路延遲，不是這次修正能力
所及，只能讓「多久遇到一次」變少，沒辦法讓「遇到的時候」歸零；一天當中真正撞上冷快取的
第一次造訪，仍然會需要幾秒鐘等待。

### 2026-09-11：技術訊號共振股改瀏覽器端抓取，瀏覽器「載入完成」大幅加快（`17e3879`）
上一則 `loading.tsx` 修正解決「不確定有沒有點到」後，使用者接著指出：畫面內容看得到了，但
「整個畫面完整顯示出來且不轉圈圈」（瀏覽器分頁的載入圈圈）還是偏慢。這揭露了 Suspense 串流
本身的限制：技術訊號共振股用 `<Suspense>` 邊界讓內容提早出現在畫面上，但那其實只是同一個
HTTP 回應裡「先送出去的一段」——瀏覽器分頁的載入圈圈要等到整個回應真正結束（連最慢的技術
訊號共振股都送完）才會停，所以「看得到內容」跟「分頁真的載入完成」是兩件事，使用者感受到的
是後者還沒結束。

修法：新增 `src/app/api/momentum/route.ts`，把技術訊號共振股的抓取整個搬到瀏覽器端。新的
`MomentumSection`（`src/components/MomentumSection.tsx`）是 client component，掛載後才用
`fetch` 分別向 `/api/momentum?market=TW`/`US` 要資料，本身內建骨架屏（跟原本 Suspense
fallback 視覺一致）。這樣 `/highlights` 的主要 HTTP 回應（漲跌幅/成交量榜 + 頁面骨架）可以
完整送完、瀏覽器分頁提早判定「載入完成」，技術訊號共振股再用獨立的背景請求慢慢補上。

正式站實測（Playwright 量測瀏覽器原生 `load` 事件，最接近「分頁圈圈停止」的指標）：
**`/highlights` 的 `load` 事件從原本 7-9 秒降到 674-734 毫秒**，漲跌幅/成交量榜內容幾乎瞬間
可見（14 毫秒），技術訊號共振股仍需約 5 秒才會補完資料，但這 5 秒不再讓瀏覽器分頁一直轉，
使用者看到的是「頁面已經載入完成，其中一個區塊還在補資料」而不是「整頁還在載入中」。

**這是效能追查的第四步，跟前面三個修正合起來才是完整的解法**：① 運算並行度太低 → 拉高、
② 快取視窗太短、正常瀏覽幾乎每次都 miss → 拉長、③ 沒有 `loading.tsx`、點擊當下沒有任何
回饋 → 加上、④ 最慢的區塊用 Suspense 串流仍然會拖住瀏覽器的載入完成判定 → 改成瀏覽器端
非同步抓取，徹底跟主頁面的載入生命週期脫鉤。**如果之後同一個頁面又被抱怨「還是有點慢」，
要先確認是這四個層面裡的哪一個還沒處理好，不要重複繞同一圈。**

### 2026-09-11：加上 loading.tsx，解決「點了不確定有沒有點到」的卡頓感（`426cea1`）
上一則快取修正後，使用者說「有變快了，但還是會讓人有種卡頓、不確定自己有沒有點到的感覺」——
這是不同類的問題，不是資料抓取慢，是**點下去那一瞬間畫面完全沒有任何回饋**。首頁、每日焦點、
個股頁都是要 `await` 資料才能渲染的 async Server Component，這個 repo 原本完全沒有用
Next.js 的 `loading.tsx` 慣例，所以 Next.js 的預設行為是讓舊畫面停在原地不動，直到新頁面資料
完全就緒才整個換過去——不管換頁本身多快，中間這段空窗期使用者完全看不到任何反應，才會覺得
「是不是沒點到」。

新增 `app/loading.tsx`、`app/highlights/loading.tsx`、`app/stock/[symbol]/loading.tsx`，
比照現有骨架屏風格（`animate-pulse` + `bg-(--page-plane)`，跟 `WatchlistSection`、
`MomentumSkeleton` 一致的視覺語言），點擊當下立刻顯示骨架畫面，資料就緒後才換成真正內容。
`/search` 沒有加，因為它的 `page.tsx` 本身不是 async component（實際抓資料在 client-side 的
`SearchClient`），沒有東西會卡住 Next.js 的渲染，不需要這個機制。

驗證方式：用 Playwright 搭配 Chrome DevTools Protocol 模擬較慢的網路（500kbps + 300ms 延遲），
點擊「每日焦點」後 400 毫秒截圖，確認骨架畫面（4 個排行榜區塊的灰色佔位方塊）確實立刻出現，
不是空白或舊畫面卡住。

**這是這輪效能追查的第三個、也是性質完全不同的問題**：第一個是運算本身慢（並行度太低），
第二個是快取視窗太短（幾乎每次都要重新抓），第三個是「即使資料還沒回來，畫面也該先給個
反應」的純 UX 問題，跟前兩個的「讓資料回來得更快」方向不同，是「讓使用者在等資料的時候
知道系統有在動」。**如果之後還有「感覺卡頓」的回饋，要先分辨是這三種裡的哪一種，不要都往
同一個方向查。**

### 2026-09-11：效能問題真正的元兇——市場報價快取只有 20 秒（`d794a5d`）
接續上一則效能修正之後，使用者仍然反應很慢，這次沒有自己瞎猜就繼續改，而是**先問清楚問題範圍**
才找到真正原因，過程值得記錄：

追問過程：「連點選已經在網站裡的分頁連結也很慢」→ 排除「特定頁面運算太慢」（那個上一輪已經
處理過）；「載入圓圈會一直轉到內容出現」→ 確認是真的在等網路/伺服器回應，不是瀏覽器端渲染卡住。
這兩個問題縮小了範圍，才想到：`getMarketQuoteMap()`（search/highlights/首頁焦點排行共用的
整個市場批次報價，全站最貴的一次上游呼叫）原本跟單一股票「即時觀看」共用同一個
`QUOTE_TTL_MS = 20 秒`。使用者正常瀏覽的點擊間隔幾乎一定超過 20 秒，等於**幾乎每一次點擊都在
快取失效、都要重新付一次 TWSE/Yahoo 批次抓取的代價**——不管前面把運算本身（並行度、Suspense
串流）再怎麼優化都沒用，因為問題根本不在運算，在「幾乎每次都在重新抓資料」。

新增獨立的 `MARKET_MAP_TTL_MS = 2 分鐘` 給這個市場批次報價專用，跟單一股票即時觀看用的
`QUOTE_TTL_MS`（20 秒，`LiveQuoteHeader`/`LiveIndices` 還是要即時輪詢，維持不變）分開。本機
實測驗證：第一次呼叫 0.77 秒（真的去抓資料），28 秒後（舊 TTL 此時已經過期，新 TTL 還在範圍內）
再呼叫，只要 0.01 秒——確認新的快取視窗真的接住了正常瀏覽節奏的重複請求。正式站因為這次對話
過程中大量測試流量干擾，量到的數字比較雜訊，但方向一致（比修正前的 7-12 秒明顯快很多）。

**教訓：使用者說「怎麼測都很慢」時，不要一直在同一個方向（運算效能）反覆微調，要追問清楚
「哪一種操作」慢、慢的當下瀏覽器行為是什麼樣子，範圍縮小了才找得到真正的原因。這次如果沒有
問「已經在網站裡點選單會不會也慢」跟「載入圓圈會不會一直轉」這兩個問題，很可能會繼續在
串流/並行度這個已經走過的方向瞎繞。**

### 2026-09-11：使用者回報網站整體還是很慢，實測+修正兩個效能問題（`3e69f58`、`97613fa`）
使用者在新功能上線後仍反應「進入網頁、切換頁面都很慢」。這次沒有直接假設是舊有的密碼卡迴圈
（那個已經修好了），而是重新用 Playwright 對正式站每個路由實測，過程中學到一個重要的測量方法
教訓：

**教訓：`page.goto(url, {waitUntil:'networkidle'})` 測出來的數字會被 Next.js 的 `<Link>`
背景預抓取（prefetch）污染，看起來比使用者實際感受到的慢很多。** 首頁的「焦點排行」表格每一列
都是 `<Link href="/stock/xxx">`，瀏覽器會在背景自動 prefetch 這些連結的 RSC payload；如果這時候
使用者導去別的頁面，這些 prefetch 請求會被瀏覽器中止（`net::ERR_ABORTED`），但 `networkidle`
還是會把這些背景雜訊算進「網路沒閒下來」，讓測出來的載入時間虛高好幾秒。**之後要測真實使用者
感受到的載入時間，要嘛測「實際內容何時出現在畫面上」（`waitForSelector` 等真正的內容文字），
要嘛測使用者主動點擊連結的「用戶端導航」時間，不要只看 `networkidle` 的總秒數。**

用正確的測量方式（等真正內容出現）量出來的結果：**點擊已載入頁面之間的分頁切換一直都很快
（30-50 毫秒），這從來就不是問題**；真正慢的是特定頁面的「第一次/重新整理載入」，尤其是
`/highlights`（每日焦點）量到 7-9 秒。追出兩個真正的原因並修正：

1. **`MOMENTUM_CHART_CONCURRENCY` 太保守**（`3e69f58`，`lib/data/index.ts`）：技術訊號共振股
   （`getMultiSignalStocks`，`/highlights` 用兩次、AI 問「有哪些股票不錯」也會用到）對每檔候選股
   都要抓好幾個月的K線圖，原本只用 4 併發去抓 25 檔候選股，等於要排 20+ 輪。這個保守值是網站
   還是公開高流量服務時，為了不被 TWSE/Yahoo 整個網站一起限流才設的；現在是低流量私人使用，
   這個顧慮不成立了，拉高到 25（跟候選股數量一樣，幾乎完全平行）。本機實測 4.6 秒→2.0 秒，且
   確認資料完整沒有因為過度併行被上游擋掉。
2. **`/highlights` 整頁等最慢的區塊才顯示**（`97613fa`，`app/highlights/page.tsx`）：即使加大
   並行度，漲幅/跌幅/成交量榜（只需要 1 個批次報價請求，快）還是被技術訊號共振股（天生較慢）
   拖著一起等，因為原本用一個 `Promise.all` 等全部 8 個資料來源都回來才送出整個頁面。改成技術
   訊號共振股獨立包一個 React `<Suspense>` 邊界（新的 `MomentumSection` + 骨架屏
   `MomentumSkeleton`），三個排行榜維持原本直接 `await` 渲染，資料一到就能送出，技術訊號共振股
   之後再串流進來。正式站實測「真正內容出現在畫面上」的時間：`/highlights` 從 7003ms 降到
   3631ms（上游回應時間本身有波動，有時甚至只要 ~550ms）。

其他頁面實測都在合理範圍（首頁 ~500ms、搜尋頁殼 ~700ms、個股頁 500-900ms），沒有另外處理。
**如果之後又有人反應「還是慢」，先用上面教訓提到的正確測量方式（等真正內容出現，不要信
networkidle 的總秒數）重新量一次，不要直接假設是同一個原因。**

### 2026-09-11：使用者要求的 5 項優化功能，全部完成並在正式站驗證（`d50c9e9`→`9264296`）
使用者問「還有沒有建議優化的地方」，我先看程式碼列出建議清單讓使用者選，使用者選了全部：
擴大股票清單規模、更多技術指標（RSI/MACD）、持股成本追蹤、AI 主動分析關注清單、到價提醒
（先做網頁內顯示，之後要接 LINE 通知）。逐一實作、每做完一項就 build+push+正式站用
Playwright 驗證過才做下一項：

1. **擴大股票清單規模**（`d50c9e9`）：`MAX_TW_UNIVERSE` 100→200；`US_UNIVERSE` 新增約 45 檔
   知名股，補齊半導體設備/雲端資安/保險/工業設備/公用事業/REITs/原物料等原本較少的次產業。
   驗證：`/api/search` 台股 100→199 筆、美股 ~109→170 筆。
2. **RSI/MACD 技術指標**（`d50c9e9`，`lib/signals.ts`）：RSI 14 期簡單平均（超買≥70/超賣≤30才
   顯示），MACD 12/26 EMA + 9 期訊號線的黃金/死亡交叉（只在真正跨越那天觸發，不是每天都顯示
   「在訊號線上方」）。MACD 至少需要 50 根K棒（3個月圖表約 60-65 根），資料不足就不顯示，不是
   假造。這兩個訊號會自動出現在技術訊號共振股跟 AI 問答的個股資料裡。
3. **持股成本追蹤**（`672dfd8`，`lib/watchlist.ts` + 新元件 `WatchlistTable.tsx`）：
   `WatchlistItem` 新增可選的 `costBasis`/`shares`，關注清單表格可以直接編輯，即時算損益金額/
   百分比跟整個清單的總損益。`/api/watchlist` 的 `normalize()` 原本只留 symbol/market/name 三個
   欄位，會把新欄位在雲端同步時整個砍掉，一併修正。
4. **AI 分析關注清單**（`672dfd8`，`lib/ai/ask.ts` + `ChatWidget.tsx`）：關注清單資料只存在瀏覽器
   localStorage，`/api/ask` 伺服器端本來完全看不到，所以聊天視窗新增「分析我的關注清單」按鈕，
   點擊時把 localStorage 的關注清單（含成本/股數）一起送出，後端 `buildHoldingsGrounding()`
   即時查每一檔報價、算損益，讓 AI 逐檔講現況跟賺賠金額。驗證：實際設定台積電成本 2200／
   1000 股後問 AI，正確算出損益 +217,500（+9.9%）並給出明確看法。
5. **到價提醒**（`053db8f`，新增 `lib/priceAlerts.ts` + `PriceAlertForm.tsx` + `PriceAlertWatcher.tsx`）：
   個股頁設定「漲到/跌到 X 元」提醒，`PriceAlertWatcher` 掛在全站 layout（不是個股頁，因為使用者
   設完提醒通常會去看別的頁面）每 30 秒檢查所有未觸發的提醒，觸發時右下角跳提醒。目前只有網頁內
   顯示，沒有真正推播，使用者已知情，之後要加 LINE 通知。

**過程中自己測出來的真實 bug**（`9264296`，不是使用者回報的，是這次對話自己 Playwright 驗證第 5
項時發現整個個股頁打不開）：`PriceAlertForm.tsx` 用 `useSyncExternalStore`，快照函式寫成
`() => alertsFor(symbol, market)`，而 `alertsFor` 內部用 `.filter()` ——每次呼叫都建立新陣列，
即使資料沒變，導致 `useSyncExternalStore` 誤判「store 一直在變」，觸發無限重新渲染
（React error #185 "Maximum update depth exceeded"），整個個股頁面直接打不開。修法：快照函式
改回直接傳 `getAlerts()`（跟 `lib/watchlist.ts` 一樣，資料沒變就回傳同一個參照），篩選邏輯移到
外面用 `useMemo`。**教訓：任何用 `useSyncExternalStore` 的地方，快照函式本身絕對不能做
`.filter()`/`.map()`/展開這類會建立新物件的操作，只能回傳「資料沒變就一定回傳同一個參照」的值，
篩選/轉換都要挪到外面用 `useMemo`，這個坑之後如果又要加新的 localStorage 相關 UI 要記得。**

過程中也順便解決了 PROGRESS.md 之前記錄的殘留疑慮：在台股收盤後（`已收盤`狀態）實際驗證
`/api/search?market=TW`，卡在 0.00% 的比例是 14/199（合理），確認收盤後沒有復發成 92/100
那種規模的問題。

### 2026-09-11：密碼保護上線後兩個真實 bug——密碼進不去、AI 回覆星號沒渲染（`1c5c3d9`、`8bebfd9`）
接續上一則「網站改為密碼保護」之後，使用者實際使用時回報兩個問題，都已修好並用 Playwright
在正式站驗證過：

1. **輸入正確密碼 1118 還是進不去**：用 curl 測試看起來一切正常（密碼驗證通過、cookie 也設好），
   但使用者說打了密碼進不去——這正是提醒自己「curl 測過不等於瀏覽器測過」的活教材，改用
   Playwright 真的跑一次瀏覽器才抓到：`UnlockForm.tsx` 用 `router.push(next)` 導航，但
   `SiteHeader` 的 `<Link>` 在使用者還沒登入時就已經把首頁（會被 proxy 導回 `/unlock` 的那個
   結果）prefetch 進 Next.js 的用戶端路由快取，`router.push` 直接把這個未登入時期的舊結果吐
   回來——網址列/畫面完全沒變化，看起來就像密碼沒作用，但其實 cookie 早就設定成功了。改用
   `window.location.href` 做真正的整頁導航，繞過用戶端路由快取。**教訓：任何「登入/解鎖後
   導頁」的邏輯，只要頁面上有其他 `<Link>` 可能被預先 prefetch 過，用 `router.push` 就有風險
   拿到登入前的舊快取，改用整頁導航（`window.location.href`）比較保險，尤其是像這種一次性的
   閘門頁面，不值得為了 SPA 導航的順暢度冒這個風險。**
2. **AI 回覆看到一堆星號**：AI 常用 `**粗體**`、`*` 條列這類 Markdown 語法，但 `ChatWidget`／
   `DailyBriefCard` 都只是把文字塞進 `whitespace-pre-wrap` 的純文字容器，使用者看到的是原始
   語法符號而不是真正的粗體/條列。新增 `src/components/MarkdownLite.tsx`——不是完整 Markdown
   解析器，只處理 AI 實際會用到的粗體/條列/段落換行，比照 `lib/format.ts` 手寫小函式而不拉
   套件的風格。已用 Playwright 對話截圖確認條列渲染成真正的 `<ul><li>`。

同一輪也回報「切換頁面很久」，我用 Playwright 量測正式站的頁面載入（~1 秒）跟點擊切頁
（30-50 毫秒），都在合理範圍——**判斷這多半是上面第 1 點的密碼卡死迴圈造成的錯覺**，卡在
登入迴圈出不去，體感就會像整個網站都動不了。這點修好密碼問題後還沒有再收到回報，如果之後
使用者又反應慢，要問清楚是哪個頁面、大概多久，不要直接假設又是密碼問題。

### 2026-09-11：Opus 規則三第一輪挖出 9 個資料正確性問題，全部修好並驗證（`9758888`）
派出的 Opus agent 用 Playwright + 直接打上游 API（TWSE MIS、Yahoo）比對，抓到的問題比規則二自測
更深：不只是頁面會不會壞，而是**顯示的數字本身是錯的**。全部自己修好，且每一項都用真實上游資料
（不是假資料/mock）在本機 `npm run start` 打 http://localhost:3100 驗證過一次，再部署到正式站
複驗一次：

1. **美股漲跌幅算錯、方向可能相反**（`src/lib/data/us.ts` `fetchUsQuote`）：K 線用的是「1個月」
   區間，Yahoo 在這個區間下不會回傳 `meta.previousClose`，程式退回 `chartPreviousClose`（一個月前
   的收盤價）當「昨收」去算漲跌——TSLA 那天其實下跌，網站卻顯示大漲近 10%；AAPL 同理，開盤價也因為
   同一顆 `meta.regularMarketOpen` 缺欄位而直接顯示成目前價。改成從同一次抓到的每日 K 棒陣列裡讀
   「今天」跟「昨天」兩根真正的棒子，不再依賴會隨 range 消失的 meta 欄位。驗證：AAPL 改前 +5.94%→
   改後 +3.56%，TSLA 改前 +9.88%（方向錯）→ 改後 -1.16%（正確）。
2. **台股約 92% 個股整天卡在平盤 0.00%、股價可能超出當日高低區間**（`src/lib/data/twse.ts`
   `rowToQuote`）：TWSE MIS 的成交價欄位 `z` 沒成交時是 `"-"`，原本直接退回昨收，導致沒有最新
   成交的股票全部顯示平盤；改成 `z` 無效時退回當下最佳買/賣價（`b`/`a` 欄位）的中價，這兩個欄位
   TWSE 有即時在更新。驗證：`/api/search?market=TW` 100 檔中卡在 0.00% 的數量從 92 檔降到 3 檔。
3. **美股批次報價／基本面整個抓不到**（`src/lib/data/us.ts`）：Yahoo `v7/finance/quote` 現在要求
   cookie + crumb 驗證，沒帶就回 401——這個端點被搜尋頁美股分頁、焦點榜單美股、首頁美股排行、
   個股頁基本面卡片共用，全部因此掛空。新增 `getYahooAuth()`：先跟 `fc.yahoo.com` 換 cookie、
   再跟 `v1/test/getcrumb` 換 crumb（快取 50 分鐘），帶著這兩樣重打原本的端點。驗證：
   `/api/search?market=US` 從 0 筆變 106 筆，AAPL 基本面本益比/殖利率/市值也都拿得到了。
   **教訓：Yahoo Finance 這類非官方端點會不預警換驗證方式，之後如果美股資料又整個消失，
   先懷疑是不是 Yahoo 又改規則，不要照原本邏輯除錯。**
4. **首頁搜尋框打中文公司名稱查不到、還會被誤判成美股**（`src/lib/data/index.ts`
   `normalizeSymbol`）：AI 問答之前已經有 `findSymbolByName`（見上一輪修正），但搜尋框走的是
   `normalizeSymbol` 這條不同路徑，沒接上同一個名稱比對。現在補上。
5. **不存在的台股代號（如 9999）顯示一整頁「非數值」而非資料暫缺**（`twse.ts` `rowToQuote`）：
   MIS 對查無此股的代號回傳的列缺乏有效昨收數字，原本沒判斷直接建構出全 NaN 的 Quote 物件，
   違反「抓不到就回 null」的原則。現在昨收不是有效數字就視為查無此股。
6. **台股清單仍偏傳產小型股，缺聯電/玉山金/台灣大/統一超/元大金/第一金/和碩/緯創/台塑化/中租
   等知名股**（`universe.ts`）：`3202cc7` 那次只優先放了 24 檔，其餘 76 個名額仍照代號順序排，
   還是排擠掉很多常見股。這次擴充到約 44 檔優先名單，補齊電機機械/化學工業/觀光事業/生技醫療/
   貿易百貨等原本 17 個產業分類裡沒有代表股的類別。**這是跟 `3202cc7` 同一類問題的延續，如果
   之後又有人反應「搜不到某檔知名股」，先查是不是又被 76 個代號序名額排擠掉，不是新 bug。**
7. **AI 問答把「2025年」誤判成台股代號 2025（千興）**（`lib/ai/ask.ts`）：`SYMBOL_PATTERN` 對
   4-6 碼數字沒有排除「後面接年」的情況。加上這個排除條件。
8. **大盤指數偶爾整組消失 20 秒，即使上游馬上恢復**（`index.ts` `getIndices`）：原本 4 個指數
   共用一個快取 key，剛好同時全部抓取失敗時會把「空陣列」快取整整一個 TTL。改成每個指數自己的
   快取 key，一個失敗不會拖累其他已經成功的。

以下這幾項一開始看起來像 bug，追查後發現不是（附上如何驗證排除，避免以後重複查一樣的東西）：
- Google 登入按鈕不顯示：正式站沒設定 `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET`/`AUTH_SECRET`，
  `layout.tsx` 的 `authEnabled` 判斷讓整個登入功能優雅隱藏，這是刻意設計（登入本來就選用），
  不是 bug。
- 一開始用 Playwright 測試「找不到登入按鈕/聊天輸入框/產業篩選 checkbox」：後來證實是測試腳本
  選錯選擇器（見上一輪工作日誌），不是網站壞掉。

### 2026-09-11：網站改為密碼保護的私人工具，AI 問答改成直接給推薦（`bfc74c1`→`d85ad6c`）
接續上一則工作日誌（AI 問答太囉唆、查無個股清單那次修正）之後，使用者接連提出三個進一步要求，
過程中有一次重要的方向修正，記錄下來避免以後重複走一樣的路：

1. **要求 AI「查每個股票、直接寫推薦哪檔及為何」**：這是具體個股買賣建議/漲跌預測，屬於證券
   投顧業務，公開網站沒有執照這樣做有法律風險，`getMultiSignalStocks`（`lib/data/index.ts`）
   的既有註解也明確寫著「deliberately not framed as about to rise...regulated investment
   advice」。跟使用者說明後，**先**採用合規版本（`bfc74c1`）：擴大技術訊號共振股回覆數量到
   12 檔、每檔附上具體數字（均量倍數/連漲天數/均線位置）講清楚客觀理由，但不做預測/推薦。
2. **要求拿掉免責聲明，說看起來很亂**：先壓縮成結尾一句極短標籤（`51191bb`），但不能整個
   拿掉——網站當時仍是公開的，完全沒提醒反而更像是在給投資建議，風險更大。
3. **使用者說「這網站基本上只有自己跟家人用」，要求可以直接寫建不建議買**：**這裡是關鍵**——
   網址 `stock-web-blond.vercel.app` 當時完全公開、沒有登入或密碼限制、任何人都能用、也能被
   搜尋引擎收錄，跟「使用者自己覺得只有家人在用」是两回事。台灣證券投顧法規判斷的是網站有沒有
   對不特定多數人公開，不是操作者的主觀意圖。**沒有先跟使用者說清楚這個落差、直接照字面要求
   把 AI 改成會推薦買賣，會是這次修正裡最大的風險**。跟使用者說明後，使用者選擇「先做真正的
   存取限制，再開放推薦」：
   - `19ed951`：新增 `src/proxy.ts`（Next.js 16 用 proxy 取代 middleware，見
     `node_modules/next/dist/docs/.../proxy.md`，這個版本的 Next.js 已經把 middleware 改名
     為 proxy）+ `/unlock` 密碼頁 + `/api/unlock`。攔截所有請求（含 API，不是只擋頁面），
     沒有 `site_unlocked` cookie 就導去 `/unlock`；密碼正確後 cookie 記住一年。密碼寫死在
     `src/app/api/unlock/route.ts`（預設 `1118`，可用 `SITE_PASSWORD` 環境變數蓋掉）——
     **這個 repo 是公開的**，寫死密碼等於密碼本身也公開，這件事已經明確告知使用者、使用者
     知情並接受這個風險才這樣做，不是我自己決定要不要提醒。commit 這個檔案時一度被 Claude Code
     的 auto mode classifier 擋下來（很可能是偵測到程式碼裡有疑似寫死的密碼字串），跟使用者
     確認接受風險後才重新提交成功，**這是防護機制正常運作，不是 bug，不需要想辦法繞過**。
   - 確認正式站密碼保護真的生效（未帶 cookie 打 API 會被導向 `/unlock`、密碼正確才放行）之後，
     才動手改 `d85ad6c`：`lib/ai/ask.ts` 系統提示詞改成直接給明確個人看法/推薦，移除「絕對
     不能說建議買」的限制，也移除免責聲明段落與 canned fallback 的「僅供參考」標籤。
   - 已在正式站用真實 AI 驗證：問「直接推薦一支股票給我」，回答「我推薦彰銀（2801），它今天
     大漲 3.54%...技術面同時創下 3 個月新高並站上 20 日均線，短線動能強勁。」不再迴避、不再
     附加免責聲明。
4. **這個決定綁在「網站現在有密碼保護」這個前提上，不是永久的文案風格改變**：如果之後密碼
   保護被拿掉、或使用者又想把網站公開分享給更多人，AI 問答的系統提示詞務必要改回客觀數據
   描述、不做買賣建議的版本——上面「重要慣例與限制」章節已經記錄這點，接手時看到 AI 直接給
   推薦不要覺得奇怪，但也不要假設這個尺度在網站公開後還適用。

### 2026-09-11：使用者回饋 AI 問答太囉唆、問「有哪些股票不錯」卻回答查無資料（`927aa9d`）
使用者實際用聊天問「今天有哪些股票不錯的嗎?感覺開始要上漲的?」，AI 回答「參考資料中未包含個別
股票的數據與清單」，且整體回覆偏長、條列鋪陳、免責聲明重複。追到 `lib/ai/ask.ts`：這種不是指定
特定股票的問題，`guessSymbolFromText` 找不到目標，完全沒有個股資料可用；但網站其實已經在算
漲幅榜跟技術訊號共振股（`/highlights` 用的就是這份資料），只是沒接進聊天的 grounding。新增
`MOVERS_INTENT_PATTERN` 偵測「有哪些/推薦/強勢股/要漲」這類問題，命中且沒指定股票時撈台股+美股
漲幅榜前 5、技術訊號共振股前 5 帶進去，讓 AI 有真實數據可以具體回答；同時把系統提示詞改成要求
直接講重點、不重複免責聲明、能不條列就不條列。已在正式站用真實 AI 重問同一句話驗證：現在會直接
列出今日漲幅榜前五名（台股陽明/台達化/研華，美股蘋果/Comcast/PayPal 等）並清楚說明是客觀數據
排序不是預測，只在結尾提醒一次免責聲明，語氣明顯更直接。**這個修正屬於使用者當場回饋、單一問題
修正，範圍小、風險低，未依 CLAUDE.md 規則二/三整套跑地毯式全站複查+Opus 驗證**——下次若使用者
對 AI 問答語氣或個股清單功能還有其他回饋，記得延續這個 MOVERS_INTENT_PATTERN 的設計脈絡，不要
重新發明一套。

### 2026-09-11：規則二自測（第一次用真的 Playwright 打開正式站）發現並修正 2 個真實 bug
本機第一次可以連到正式站+裝 Playwright，改用真瀏覽器地毯式檢查每個頁面/功能（見上方「這次的環境
變化」）。過程中一些「找不到按鈕」的假警報後來證實是測試腳本選錯選擇器（登入按鈕文字其實是「使用
Google 登入」、聊天輸入框是 `<input>` 不是 `<textarea>`、產業篩選是兩個各自獨立的 `<details>`
面板要先展開對應面板才看得到 checkbox），不是網站真的壞掉，用修正後的選擇器重測都正常。也確認了
登入按鈕在正式站不顯示是因為 Vercel 沒設定 `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET`/`AUTH_SECRET`，
這是刻意的優雅降級（Google 登入本來就是選用功能），不是 bug。

真的抓到並修好的問題（commit `e95e963`）：
1. **每個個股頁都會觸發 React hydration error #418（100% 重現，TW/US 皆然）**：
   `LiveQuoteHeader.tsx` 用 `new Date(quote.updatedAt).toLocaleString("zh-TW", {timeZone:
   "Asia/Taipei"})` 直接在渲染時格式化「更新時間」文字，但 Vercel 的 Node.js 執行環境跟瀏覽器對
   `zh-TW` 語系的 ICU 資料不一致，同一個時間點兩邊格式化出來的字串不同，導致 SSR 輸出跟 CSR
   首次渲染的文字對不上、每次都觸發 hydration 錯誤（雖然畫面上使用者看不太出來，但主控台一直報錯，
   且屬於不穩定行為，理論上可能導致該文字閃爍或用到過期內容）。修法：在 `format.ts` 新增
   `formatTaipeiDateTime()`，改用固定 UTC+8 位移的手動運算（不呼叫 `Intl`/`toLocaleString`），
   在任何 JS 引擎、有沒有完整 ICU 資料都會得到一模一樣的字串。**教訓：SSR 頁面裡任何用
   `toLocaleString`/`toLocaleDateString` 直接格式化「日期時間」（不是純數字）的地方都要小心，
   Node 環境的語系資料不一定跟瀏覽器一致，數字格式化通常沒事，日期時間格式化風險高很多。**
2. **一般聊天視窗（沒有從個股頁「問AI關於」進入、沒鎖定特定股票時）打中文公司名稱完全查不到資料**：
   實測連續問「鴻海股價多少？」「聯發科呢？」「台達電呢？」，AI 每次都老實回答「並沒有包含該股資料」。
   追到 `lib/ai/ask.ts` 的 `guessSymbolFromText()`：只用 regex 抓數字代號（如 2330）或大寫英文代碼
   （如 AAPL），完全沒有比對中文公司名稱，使用者不打代號、直接打公司名稱這種最自然的問法就會失敗。
   修法：在 `lib/data/universe.ts` 新增 `findSymbolByName()`，用公司名稱做子字串比對（比對股票清單
   裡的中英文名稱，較長的名稱優先比對避免誤判），`guessSymbolFromText()` 改成先用名稱比對、比對不到
   才退回原本的代號 regex。已確認：只有在沒有明確鎖定股票（`contextSymbol` 為 null）時才會呼叫
   `guessSymbolFromText`，鎖定股票時走的是另一條路徑，這次修改不影響「問AI關於」的既有行為。

**目前卡在 push 這一步**（見上方「目前已知問題」），push 成功、部署後還要重新用 Playwright 驗證
這兩個問題真的修好、再走規則三 Opus 獨立驗證，才能回報「更新完成」。

### 2026-09-10：跨裝置文件化
- 建立這份 `PROGRESS.md`，CLAUDE.md 新增規則五（跨裝置接續：每次回覆前更新這份文件並 push）。

### 2026-09-10：規則二/三流程改為「只做一次全面檢查＋之後只複查問題點」
- `bceacb3` 使用者要求把原本「重複全站地毯式檢查直到某輪完全沒問題」的高成本循環，改成規則二、三各自只需一次完整全面檢查，之後只需針對「這次發現並修正的問題」逐一複查，不必每次重新掃整站。

### 2026-09-10：第三輪 Opus 驗證挖出的次要問題 + 深色模式漏改
- `d31ddd0` 修好 4 個次要問題：
  1. `/api/quote`、`/api/chart`、`/api/search`、`/api/indices` 出錯時把內部 `err.message` 原封不動回給前端（可能洩漏內部細節）→ 改成統一中文提示，內部細節用 `console.error` 記錄。
  2. `/api/quote`、`/api/chart`、`/api/sectors` 的 `market` 參數沒驗證，壞值（如 `?market=BOGUS`）會被當合法市場直接用 → 改成壞值視同未提供，落回自動判斷/預設值。
  3. `callAiProviders()` 只處理了「開頭是 assistant」的裁切情況，沒處理呼叫端傳入連續同角色 turn 的情況（兩家 AI API 都會因此 400）→ 加上合併連續同角色 turn 的邏輯。
  4. 深色模式 `--price-down` 對比不足（約 3.9:1，低於 WCAG 4.5:1）→ 調亮成 `#00b300`（約 6-7:1）。
- `737f335` 上面第 4 項只改了「跟隨系統深色」的 CSS 區塊，漏改「手動點擊深色模式切換鈕」實際用到的
  `:root[data-theme="dark"]` 獨立區塊 → 補齊，兩區塊值一致。**教訓：這個網站深色模式有兩套獨立的
  CSS 變數定義區塊（`@media (prefers-color-scheme: dark)` 跟 `:root[data-theme="dark"]`），改深色配色
  一定要兩處都改，globals.css 裡已經有多次因為只改一處而漏掉的前科。**

### 2026-09-10：Opus 全站地毯式驗證挖出的主要功能性 bug（規則三第一輪）
- `3202cc7` 修好 8 個問題，其中最嚴重的兩個：
  1. **台股榜單看不到台積電等權值股**：`universe.ts` 對 TWSE 官方清單用 `slice(0,100)` 截斷，該清單按代號排序，結果只留下 1101-1799 這段冷門的水泥/食品/紡織股，2330/2317/2454/2412 與整個 28xx 金融股全部被濾掉——首頁排行、焦點榜單、每日快報的台股內容因此長期都是冷門小型股，不是真正熱門股。改成優先納入權值股。
  2. **AI 問答聊到第 5 輪就壞掉**：對話歷史裁切邏輯只砍長度，沒處理裁切後開頭變成 assistant 的情況，兩家 AI API 都要求對話必須從 user 開始，結果變成每次 400、靜默退化成罐頭回覆（使用者感覺像 AI 壞了）。
  其他修正：K 線「1個月」實際是「本月至今」（月初技術訊號會因資料點不足而消失）、深色模式切換後 K 線圖 canvas 顏色不會跟著變、技術訊號共振股一次對 TWSE 開約 100 個併發連線（降到峰值 16 個）、搜尋 API 數字參數格式錯誤會讓結果變成「共 0 筆」（`NaN` 比較永遠 false）、AI 問答「聚焦股票」離開個股頁後無法取消、關注清單 PUT 無長度上限、部分 API 未攔例外、搜尋頁無 debounce。

### 2026-09-10：快取層重大修正（規則二 + 規則三初次驗證）
- `fa5d2f6` **最根本的效能問題**：`cached()` 沒有合併同時進行中的重複請求（single-flight），首頁一次載入會對同一個 key（例如台股報價）同時發出 4-6 次完整批次抓取，大幅放大延遲與被上游限流的機率。這是拖垮網站的主因。
- `91f9c2a` Opus 驗證輪又挖出 4 個快取層真實 bug：
  1. **Map 被 Redis 摧毀（最嚴重）**：`market-quotes:TW/US`、`fundamentals:TW:all` 這類值是 `Map`，但 `JSON.stringify(Map)` 是 `"{}"`——Redis 開啟時第一次請求後續全部讀回空物件，`.get()`/`.has()` 都不存在，直接讓首頁、搜尋頁 500。加了 `cachedMap()` 分開處理，並在 `cached()` 裡加防呆（存到 Map/Set 會 `console.error` 而不是靜默寫壞）。
  2. 記憶體快取 TTL 起算點抓錯（`load()` 開始前算，而非結束後才算），慢查詢會讓快取寫入時就已過期，形成「越慢越沒快取、越查越慢」的惡性循環。
  3. single-flight 註冊時機在工作開始之後（因為 `async` 函式同步跑到第一個 `await` 才觸發排程），有短暫的重複請求空窗，改用 microtask 延後排程確保先註冊再開始。
  4. Redis 設定了但暫時連不上時完全沒有備援（原本只在「未設定 Redis」才退回記憶體），改成記憶體當 L1 一律寫入。
  5. 記憶體快取無上限，`/stock/<任意值>` 會產生新 key，加上 null 值也快取，爬蟲可把常駐 instance 撐到 OOM，加了 500 筆上限 + 過期優先淘汰。
  6. Redis 端 `null` 值原本跟「沒這個 key」無法區分（會被誤判成 cache miss），改用 `{v, e}` envelope 包裝。
- `6efdc3b` 共用快取值原本讀回本機記憶體時會重新起算一次完整 TTL（同一份資料最久可能被多服務近兩倍 TTL），改成把到期時間 `e` 一起存，本機只沿用「剩餘」的時間。

### 2026-09-10 之前：功能開發（詳見 git log，重點如下）
- `c2f6116` 降低股票清單規模與批次併發量，修正正式站緩慢/逾時、個股頁抓不到資料的問題。
- `b00425f` 關注清單比對 symbol 忽略大小寫。
- `be034ba` 新增 SEO、擴充股票清單（台股完整上市清單 + 美股 S&P500）、共用快取（Redis 整合先備妥）、Google 登入跨裝置同步。
- `b61b9ba` 移除所有示範/假資料，抓取失敗一律誠實顯示「資料暫缺」（這是網站的核心誠信原則，之後任何修改都不能違反）。
- `477ffaa` 修正清單頁大量資料可靠性問題，新增基本面卡片、技術訊號共振股、深色模式、AI 多輪對話。
- `89cf22d` 新增關注清單、技術訊號標籤、每日焦點榜單、AI 每日快報。
- 再更早：`eb7b09a` 專案從零搭建（StockRadar 骨架：報價、圖表、搜尋、AI 問答）。

## 目前已知問題

**現況：每日快報擴充＋AI 問答追問修正（見工作日誌最新一則）已經自己驗證過（本機測試 +
正式站真實 AI 重現使用者原本的問題句 + Playwright 截圖確認首頁顯示），但這次還沒派 Opus
做規則三的獨立複查——接手的裝置如果要回報「更新完成」，記得先完成這一步再回報。** 除此
之外沒有已知未修的 bug；這次對話（2026-09-11，很長一次）做完的其他事情都已經逐項驗證過，
細節見下方工作日誌，以下只列**還沒做、但已經評估過、之後有需要再處理**的項目：

- **Gemini API 金鑰外洩後，使用者評估過決定不換**：程式碼漏洞本身已經修好（見工作日誌
  「Opus 規則三驗證挖出 2 個真實問題」那則），外洩範圍實際上只有知道密碼、且在漏洞還在時
  真的打開過聊天室的人（也就是這次測試的我自己跟 Opus agent），沒有不相干的外人能接觸到。
  使用者知情後判斷風險可接受，**明確表示不需要重新產生金鑰**——之後接手的裝置看到這裡
  不用再主動提醒或催促這件事，這是使用者已經做過的決定，不是遺留的待辦事項。

- **US 財報資料源是非官方 Yahoo 端點**：`fetchUsEarnings`（`us.ts`）用的 quoteSummary
  跟批次報價一樣靠 crumb+cookie 認證撐著，Yahoo 之前已經無預警鎖過一次 `v7/finance/quote`
  （見工作日誌），這類端點理論上隨時可能又被鎖。如果哪天 AI 又開始說查不到美股財報，先檢查
  是不是 Yahoo 又改規則，不要照舊邏輯除錯。**使用者已表示如果需要可以去申請免費 API**
  （Alpha Vantage 或 Finnhub，見上方工作日誌「AI 問答新增真實新聞與財報資料」那則），目前
  還沒有這麼做，是可選的加強項，不是必要修正。
- **台股季報 EPS 沒有涵蓋金融/保險業**：`fetchTwseQuarterlyEpsAll` 只用了 TWSE 的
  `t187ap06_L_ci`（一般產業綜合損益表代號），金融/保險業 TWSE 用不同的報表代號，這次沒有
  另外接。銀行股/保產股問財報會查不到 EPS（月營收也一樣沒有，金融股本來就不對外公布月營收），
  這是資料源本身分類造成的限制，不是 bug，符合「查不到就誠實顯示沒有」的原則。
- **到價提醒目前只有網頁內顯示**：使用者已知情，之後要接 LINE 通知的話，需要 LINE
  Notify/Messaging API 的串接（這個需要使用者申請 LINE 開發者帳號跟 channel token），目前
  還沒開始做，是使用者自己說「先做網頁內顯示」時就講好的第二階段。
- **台股清單上限 200 檔、美股約 150+ 檔，仍非完整市場**：這是刻意的取捨（見 universe.ts
  註解），不在這 200/150 檔手動優先清單內的冷門股查不到是預期內限制，不是 bug。

## 這次的環境變化（給下一個接手的裝置參考）

這是第一次在**這台本機 Windows PC**上執行這個專案的品保流程，跟先前 PROGRESS.md 記錄的「sandbox
連不到正式站、沒有 Playwright」不同——**這台機器對外網路正常、能連到正式站**，且已經：
- 用 `winget install OpenJS.NodeJS.LTS` 裝好 Node.js（LTS，含 npm）。
- 在系統暫存資料夾（非專案內）裝了 `playwright` npm 套件 + Chromium，可以真的用瀏覽器打開正式站
  點擊操作，不用再退而求其次只用 curl。
- 專案本身 `npm install && npm run build` 在本機也能正常跑，可以先在本機建置驗證再 push。
- **git push 第一次卡在 Git Credential Manager「Select an account」視窗**：這台機器的 Windows
  認證管理員裡存了兩個 GitHub 帳號（`andyzheng-art` 跟這個 repo 真正的擁有者 `hj110b13-Andy`），
  GCM 沒辦法自動判斷要用哪個，所以每次都跳出來問。已經把 `origin` remote URL 改成
  `https://hj110b13-Andy@github.com/hj110b13-Andy/Stock-web.git`（明確指定帳號），之後在這台機器
  上 push/fetch 都不會再跳出選擇視窗。**如果又跳出來（例如帳號密碼過期），跟使用者說一聲請他選一次
  就好，不是程式碼問題。**
如果下一個接手的裝置也是這台本機，以上工具跟 remote 設定應該都還在，不用重新安裝/設定；如果是
別的環境，仍比照舊有說明評估該環境的網路/工具限制。

**2026-09-11 後續更新：這台機器的專案 `node_modules` 跟系統暫存資料夾裡的 `playwright` npm
套件，實際上並不會一直保留**——這次同一天的對話裡，重新接續時發現 `node_modules` 整個不見了
（`npm run build` 直接報 `next` 找不到），Playwright 的 npm 套件（不是瀏覽器執行檔本體）也
一樣消失了，兩個都要重新 `npm install`。瀏覽器執行檔本體（`C:\Users\88691\AppData\Local\
ms-playwright`）目前看來比較持久，但版本可能跟新安裝的 `playwright` npm 套件對不上（曾經
發生新套件要 chromium-1243、本機只有 chromium-1234，要多跑一次 `npx playwright install
chromium` 重新下載，約 1-2 分鐘）。**結論：不要假設上一輪裝好的東西這輪還在，每次開始品保
流程前先跑 `npm run build` 確認能不能動，不能動就照上面步驟重裝，不用大驚小怪。**

**測試腳本如果 payload 含中文，一律用檔案不要用 shell 內嵌字串**：這次對話裡两次獨立撞到同一
類假警報——用 `curl -d '{"question":"中文問題"}'` 這種內嵌 JSON 字串的寫法，在這台機器的
Windows Git Bash 環境下，中文字元偶爾會被錯誤編碼，讓問句裡憑空混入不存在的英文字母，導致
`guessSymbolFromText` 誤判成某個美股代號，測出一個其實不存在的「bug」（分別測到過 `H`
Hyatt Hotels 跟 `E` Eni 這兩個誤判，都跟真正提問內容完全無關）。兩次都是換成
`--data-binary @檔案路徑`（檔案內容用 Write 工具寫成 UTF-8）之後問題就消失，證實是測試方法
的編碼問題，不是網站程式碼的 bug。**之後任何裝置（包含派出去的 Opus/其他 agent）在這台機器
上寫含中文的 API 測試，都要用檔案 + `--data-binary @檔案` 的寫法，不要用 `-d '...內嵌字串'`，
避免浪費時間追查根本不存在的假 bug。**

如果你接手後又發現了新問題，**除了修正之外，記得也在這份文件的「工作日誌」補一筆，並更新這個章節。**
