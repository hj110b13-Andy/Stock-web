# Claude API key for the AI Q&A feature (/api/ask).
# Without it, /api/ask still works but returns a canned answer built from
# the raw quote/chart data instead of an AI-generated response.
ANTHROPIC_API_KEY=
